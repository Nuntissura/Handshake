use super::types::{
    CommandReference, CommandStatus, Manual, ManualFeatureGroup, ManualSafetyConstraint,
    ManualWorkflow,
};
use super::MANUAL_VERSION;

pub static MODEL_MANUAL: Manual = Manual {
    version: MANUAL_VERSION,
    feature_groups: FEATURE_GROUPS,
    command_reference: COMMAND_REFERENCE,
    safety_constraints: SAFETY_CONSTRAINTS,
    workflows: WORKFLOWS,
};

pub fn model_manual() -> &'static Manual {
    &MODEL_MANUAL
}

pub static FEATURE_GROUPS: &[ManualFeatureGroup] = &[
    ManualFeatureGroup {
        id: "hbr_process_diagnostics",
        title: "HBR, Process Ledger, And Diagnostics",
        description: "Build-rule enforcement, typed violation receipts, process lifecycle evidence, and no-context diagnostics entrypoints.",
        commands: &[
            "model_manual_get",
            "model_manual_list_commands",
            "model_manual_search",
            "hbr_violation_emit",
            "hbr_matrix_check",
            "hbr_validator_scan",
            "process_ledger_writer",
            "process_ledger_overflow_event",
            "process_ledger_reclaim",
            "process_ledger_staleness_reclaim",
            "diagnostics_capture_headless",
            "visual_debug_dom_snapshot",
            "visual_debug_console_stream_start",
            "visual_debug_console_stream_stop",
            "backend_inspector_read",
            "kernel_inspector_port",
            "kernel_inspector_list_sessions",
            "kernel_inspector_session_state",
            "kernel_inspector_event_ledger_tail",
            "kernel_inspector_process_ledger_active",
            "kernel_inspector_trace_projection",
            "kernel_inspector_loaded_models",
            "inspector_replay_drive",
            "swarm_harness_run",
            "handshake_swarm_cli",
            "kernel_swarm_run",
            "swarm_scenario_n8_perf",
            "swarm_scenario_session_cancel",
            "swarm_scenario_lease_contention",
        ],
    },
    ManualFeatureGroup {
        id: "sandbox",
        title: "Sandbox",
        description: "KERNEL-004 sandbox adapter surfaces for boxed model-written code and validation runners.",
        commands: &["sandbox_adapter_run", "sandbox_adapter_health"],
    },
    ManualFeatureGroup {
        id: "model_runtime",
        title: "Model Runtime",
        description: "Local model registration, generation, LoRA hot-swap, KV cache controls, and inference-lab technique switches.",
        commands: &[
            "model_runtime_generate",
            "model_runtime_register_model",
            "inference_lab_apply_technique",
        ],
    },
    ManualFeatureGroup {
        id: "memory_self_improvement",
        title: "Memory And Self Improvement",
        description: "Memory capsule retrieval, distillation candidates, and bounded self-improvement loop controls.",
        commands: &[
            "memory_capsule_build",
            "memory_calibration_snapshot",
            "self_improvement_run_iteration",
            "distillation_candidate_review",
        ],
    },
    // ---- WP-KERNEL-005 atelier (Core-Data) surfaces (MT-052..MT-060, MT-073..MT-075) ----
    ManualFeatureGroup {
        id: "atelier_character_core",
        title: "Atelier Character Identity And Sheets",
        description: "Character identity (stable public_id vs internal_id) and append-only character-sheet versioning, edits, and history. (MT-052)",
        commands: &[
            "atelier_create_character",
            "atelier_get_character_by_public_id",
            "atelier_append_sheet_version",
            "atelier_apply_sheet_field_edits",
            "atelier_sheet_version_history",
        ],
    },
    ManualFeatureGroup {
        id: "atelier_media_intake",
        title: "Atelier Media Library And Intake Review",
        description: "Media DAM identity with content-hash dedup, intake batches, lane classification, and media review metadata. (MT-053)",
        commands: &[
            "atelier_materialize_media_asset",
            "atelier_open_intake_batch",
            "atelier_list_intake_batch_items",
            "atelier_apply_intake_classification",
            "atelier_bulk_update_media_review_metadata",
        ],
    },
    ManualFeatureGroup {
        id: "atelier_collections_contact_sheets",
        title: "Atelier Collections And Contact Sheets",
        description: "Named ordered image collections and reproducible contact-sheet manifests with SVG render plus planned raster export. (MT-054)",
        commands: &[
            "atelier_create_collection",
            "atelier_add_images_to_collection",
            "atelier_create_contact_sheet",
            "atelier_render_contact_sheet_svg_artifact",
            "atelier_plan_contact_sheet_raster_export",
        ],
    },
    ManualFeatureGroup {
        id: "atelier_documents_scripts",
        title: "Atelier Character Documents, Story Cards, And Scripts",
        description: "Character note/story documents with append-only text versions, story cards/beats, and data-only character image-sourcing scripts. (MT-055)",
        commands: &[
            "atelier_create_character_document",
            "atelier_append_character_document_version",
            "atelier_add_story_card",
            "atelier_add_story_beat",
            "atelier_create_character_script",
        ],
    },
    ManualFeatureGroup {
        id: "atelier_moodboards",
        title: "Atelier Moodboards",
        description: "Schema-versioned moodboard JSON snapshots, layer operations, and moodboard export requests. (MT-056)",
        commands: &[
            "atelier_record_moodboard_snapshot",
            "atelier_record_moodboard_operation",
            "atelier_request_moodboard_export",
        ],
    },
    ManualFeatureGroup {
        id: "atelier_relationships",
        title: "Atelier Character Relationships",
        description: "Character-to-character relationship edges with endpoint validation and a relationship-map graph projection. (MT-057)",
        commands: &[
            "atelier_create_character_relationship",
            "atelier_update_character_relationship",
            "atelier_character_relationship_graph",
        ],
    },
    ManualFeatureGroup {
        id: "atelier_search_tags_similarity",
        title: "Atelier Search, Tag Palette, And Similarity",
        description: "Global search with lens filters, the tag dictionary/palette and deterministic tag rules, AI tag suggestions, and perceptual-hash similarity search. (MT-058)",
        commands: &[
            "atelier_global_search_with_lens_filters",
            "atelier_ensure_tag",
            "atelier_create_tag_rule",
            "atelier_record_ai_tag_suggestion",
            "atelier_find_similar_assets",
        ],
    },
    ManualFeatureGroup {
        id: "atelier_exports",
        title: "Atelier Exports And Backup",
        description: "Character-sheet/share-pack exports, web portfolio export, deterministic LLM evidence-pack manifests, and backup manifest + restore preflight. (MT-059, MT-073, MT-074, MT-075)",
        commands: &[
            "atelier_request_sheet_export",
            "atelier_build_share_pack_manifest",
            "atelier_request_web_portfolio_export",
            "atelier_build_llm_evidence_pack_manifest",
            "atelier_record_backup_manifest",
            "atelier_backup_restore_preflight",
        ],
    },
    ManualFeatureGroup {
        id: "atelier_reset_recovery",
        title: "Atelier Reset Recovery And Orphan Adoption",
        description: "Bounded atelier reset modes that preserve original media in an orphan manifest, and adoption of retained orphan items back into intake. (MT-060)",
        commands: &[
            "atelier_record_atelier_reset",
            "atelier_list_orphan_manifest_items",
            "atelier_adopt_orphan_manifest_item",
        ],
    },
    // ---- WP-KERNEL-005 Pose / ComfyUI pipeline surfaces (MT-122..MT-125) ----
    ManualFeatureGroup {
        id: "atelier_pose_context_and_rig",
        title: "Atelier Pose Context And Rig",
        description: "Append-only pose editor context state, OpenPose rig ingest/lookup, and BLOCKED-by-default pose calibration; detector execution runs out-of-module as a capability-gated Workflow-Engine job. (MT-122)",
        commands: &[
            "atelier_set_pose_context_state",
            "atelier_current_pose_context_state",
            "atelier_ingest_pose_rig",
            "atelier_list_pose_rigs",
            "atelier_get_pose_rig",
            "atelier_set_pose_calibration",
            "atelier_get_calibration",
        ],
    },
    ManualFeatureGroup {
        id: "atelier_pose_sidecar_and_identity",
        title: "Atelier OpenPose Sidecars And Identity Profiles",
        description: "Typed OpenPose JSON/PNG and conditioning sidecar artifacts (hidden from normal galleries), plus append-only character identity profiles, mutable-field updates, lineage, and 512x512 identity crop artifacts. (MT-123)",
        commands: &[
            "atelier_record_pose_sidecar",
            "atelier_list_pose_sidecars",
            "atelier_pose_sidecar_gallery_projection",
            "atelier_append_identity_profile",
            "atelier_update_identity_profile",
            "atelier_record_identity_crop_artifact",
            "atelier_latest_identity_profile",
        ],
    },
    ManualFeatureGroup {
        id: "atelier_comfy_workflow_receipts",
        title: "Atelier ComfyUI Workflow Receipts, Replay, And Failure Recovery",
        description: "Durable ComfyUI workflow-level receipts, per-job intake receipt summaries, workflow history replay, SaveImage fallback marking, and output-first registration-failure preservation plus retry. (MT-124)",
        commands: &[
            "atelier_record_comfy_workflow_receipt",
            "atelier_get_comfy_workflow_receipt",
            "atelier_list_comfy_workflow_history",
            "atelier_produce_intake_receipt",
            "atelier_mark_saveimage_fallback",
            "atelier_record_comfy_output_registration_failure",
            "atelier_retry_comfy_output_registration_failure",
        ],
    },
    ManualFeatureGroup {
        id: "atelier_pose_comfy_deferred_boundaries",
        title: "Atelier Pose/ComfyUI Deferred Boundaries And Adapters",
        description: "Workflow adapter boundaries: ComfyUI bridge capability registration + capability-rejected output rows (never routed), governed URL image-sourcing intake, and BLOCKED/carry-forward deferrals (calibration panel spec 10.10.4.1.9, WP-0133). (MT-125)",
        commands: &[
            "atelier_register_bridge_capability",
            "atelier_list_capability_rejects",
            "atelier_record_url_image_import",
            "atelier_set_calibration_blocked",
        ],
    },
    // ---- WP-KERNEL-005 Model-Workflow-Diagnostics manual surfaces (MT-132, MT-135, MT-159, MT-164, MT-181, MT-183, MT-185, MT-186, MT-187) ----
    ManualFeatureGroup {
        id: "diagnostics_source_evidence_matrix",
        title: "Diagnostics Source Evidence And Status Matrix",
        description: "Source/evidence matrix tying each automation manual surface to a real backend command map, sessions, leases, visual capture, local LLM, AI tagging, and build/release diagnostics, so a no-context model can see which surface is Wired vs Planned and where its evidence comes from. (MT-132)",
        commands: &[
            "diagnostics_source_evidence_matrix",
            "kernel_action_catalog_view",
            "kernel_inspector_list_sessions",
            "kernel_inspector_loaded_models",
            "diagnostics_problem_store_query",
        ],
    },
    ManualFeatureGroup {
        id: "diagnostics_no_context_manual_structure",
        title: "No-Context Model Manual Structure",
        description: "The built-in ModelManual skeleton itself: typed feature groups, command references, safety constraints, and workflows, retrievable through the wired kernel.model_manual.* IPC bridge plus the catalog-governed manual section edit path, so a no-context model can read and navigate the manual without chat history. (MT-135)",
        commands: &[
            "model_manual_get",
            "model_manual_list_commands",
            "model_manual_search",
            "kernel_model_manual_update_section",
        ],
    },
    ManualFeatureGroup {
        id: "diagnostics_gui_verification_checklist",
        title: "GUI Verification Checklist",
        description: "Model-visible GUI verification surface: DOM snapshots and stable element ids, console/exception streams, and product screenshot capture, covering readability, control discoverability, navigation coherence, no-overlap layout, responsive layout, visible state, and stable identifiers. (MT-159)",
        commands: &[
            "visual_debug_dom_snapshot",
            "visual_debug_console_stream_start",
            "visual_debug_console_stream_stop",
            "kernel_product_screenshot_capture_execute",
            "kernel_visual_debugging_loop_project",
        ],
    },
    ManualFeatureGroup {
        id: "diagnostics_build_rules_read_evidence",
        title: "Build Rules Read Evidence",
        description: "The HBR build-rule gate as model-visible read/acknowledge evidence before build/test/release work: the typed handoff gate, the HBR matrix and validator-scan checks, and typed violation receipts a model must read and clear before closure. (MT-164)",
        commands: &[
            "diagnostics_hbr_handoff_gate_evaluate",
            "hbr_matrix_check",
            "hbr_validator_scan",
            "hbr_violation_emit",
        ],
    },
    ManualFeatureGroup {
        id: "diagnostics_integration_smoke_path",
        title: "Diagnostics Integration Smoke Path",
        description: "One end-to-end no-context model operation path: claim a lease, read session state, run a catalog command through the inspector replay-drive seam, capture a visual snapshot, export a debug bundle, and confirm release. (MT-181)",
        commands: &[
            "kernel_role_mailbox_claim_lease_project",
            "kernel_inspector_session_state",
            "inspector_replay_drive",
            "kernel_inspector_event_ledger_tail",
            "diagnostics_debug_bundle_export",
        ],
    },
    ManualFeatureGroup {
        id: "diagnostics_red_team_drift_guards",
        title: "Diagnostics Red-Team Manual Visual And Command Drift Guards",
        description: "Negative checks that turn manual-command drift and visual-loop drift risks into focused guards: the manual currency constraint, the action-catalog command map, and the visual debugging loop drift/steering surface. (MT-183)",
        commands: &[
            "diagnostics_manual_drift_guard",
            "kernel_action_catalog_view",
            "kernel_visual_debugging_loop_project",
        ],
    },
    ManualFeatureGroup {
        id: "diagnostics_core_row_merge",
        title: "Manual Source Merge For Core Rows",
        description: "Merge anchor that pulls the atelier Core/Data manual source rows (MT-052..MT-060, MT-073..MT-075) into the Diagnostics manual dataset by normalized id, marking any missing source row as a blocker rather than fabricating a row. (MT-185)",
        commands: &[
            "diagnostics_core_row_merge",
            "atelier_create_character",
            "atelier_materialize_media_asset",
            "atelier_request_web_portfolio_export",
        ],
    },
    ManualFeatureGroup {
        id: "diagnostics_pose_row_merge",
        title: "Manual Source Merge For Pose Rows",
        description: "Merge anchor that pulls the Pose/ComfyUI manual source rows (MT-122..MT-125) into the Diagnostics manual dataset by normalized id, marking any missing source row as a blocker rather than fabricating a row. (MT-186)",
        commands: &[
            "diagnostics_pose_row_merge",
            "atelier_ingest_pose_rig",
            "atelier_record_pose_sidecar",
            "atelier_record_comfy_workflow_receipt",
        ],
    },
    ManualFeatureGroup {
        id: "diagnostics_owned_row_merge",
        title: "Manual Source Merge For Diagnostics Rows",
        description: "Merge anchor for the Diagnostics-owned manual/action/state/error/bundle rows: the diagnostics problem store, the action catalog command map, inspector state reads, and debug bundle export, normalized by id with missing rows marked as blockers. (MT-187)",
        commands: &[
            "diagnostics_owned_row_merge",
            "diagnostics_problem_store_query",
            "kernel_action_catalog_view",
            "kernel_inspector_session_state",
            "diagnostics_debug_bundle_export",
        ],
    },
];

pub static COMMAND_REFERENCE: &[CommandReference] = &[
    CommandReference {
        id: "model_manual_get",
        name: "model_manual_get",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.model_manual.get"),
        tauri_command: Some("model_manual_get"),
        schema_fields: &[
            "version",
            "feature_groups",
            "command_reference",
            "safety_constraints",
            "workflows",
        ],
        cli_flag: None,
        description: "Return the full typed ModelManual manifest as JSON for an in-app or no-context model reader.",
        expected_input: "No input.",
        expected_output: "Manual JSON containing version, feature groups, command references, safety constraints, and workflows.",
        common_errors: &[
            "IPC bridge not registered",
            "manual projection consumer expects stale field names",
        ],
        recovery_steps: &[
            "Check MANUAL_VERSION and the MT-013 IPC bridge registration",
            "Use model_manual_list_commands to inspect the current command index",
        ],
    },
    CommandReference {
        id: "model_manual_list_commands",
        name: "model_manual_list_commands",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.model_manual.list_commands"),
        tauri_command: Some("model_manual_list_commands"),
        schema_fields: &["id", "name", "status", "ipc_channel", "tauri_command"],
        cli_flag: None,
        description: "Return the command reference index without the long workflow and recovery text.",
        expected_input: "No input.",
        expected_output: "CommandReference summary rows keyed by stable command id.",
        common_errors: &["Command id not found in a feature group"],
        recovery_steps: &["Run model_manual_tests to verify feature group references resolve"],
    },
    CommandReference {
        id: "model_manual_search",
        name: "model_manual_search",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.model_manual.search"),
        tauri_command: Some("model_manual_search"),
        schema_fields: &["query", "results", "command_id", "workflow_id"],
        cli_flag: None,
        description: "Search command, workflow, safety, and feature group text by substring.",
        expected_input: "A non-empty query string.",
        expected_output: "Search results referencing command ids, workflow ids, and safety constraint ids.",
        common_errors: &[
            "Empty query",
            "manual data not loaded through the IPC bridge",
        ],
        recovery_steps: &[
            "Call model_manual_get for the full manifest",
            "Retry with a concrete command, workflow, or HBR id",
        ],
    },
    CommandReference {
        id: "hbr_violation_emit",
        name: "hbr_violation_emit",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &[
            "receipt_kind",
            "schema_version",
            "receipt_uuid",
            "hbr_id",
            "wp_id",
            "violation_class",
        ],
        cli_flag: Some("--self-test"),
        description: "Emit or validate canonical HBR_VIOLATION JSONL receipts for build-time and handoff-time routing.",
        expected_input: "An HBR_VIOLATION object or self-test flag.",
        expected_output: "Sorted-key UTF-8 JSONL receipt with a UUID v7 receipt_uuid.",
        common_errors: &[
            "Unknown violation_class",
            "receipt_uuid is not UUID v7",
            "freeform notes used as routing data",
        ],
        recovery_steps: &[
            "Validate against hbr-violation.schema.json",
            "Rebuild the receipt from typed HBR fields instead of notes text",
        ],
    },
    CommandReference {
        id: "hbr_matrix_check",
        name: "hbr_matrix_check",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &[
            "acceptance_matrix",
            "hbr",
            "hbr_not_applicable",
            "evidence_pointer",
            "validator_verdict",
        ],
        cli_flag: Some("--all-packets"),
        description: "Verify PACKET_ACCEPTANCE_MATRIX HBR rows block PASS closure when any row remains PENDING, STEER, or BLOCKED.",
        expected_input: "A packet path or all-packets mode.",
        expected_output: "Exit 0 on pass; JSONL failures on matrix violations.",
        common_errors: &[
            "PROVED row has no evidence_pointer",
            "NOT_APPLICABLE row lacks a ledger reason",
        ],
        recovery_steps: &[
            "Hydrate the matrix from HANDSHAKE_BUILD_RULES",
            "Attach runnable evidence or not-applicable reasons",
        ],
    },
    CommandReference {
        id: "hbr_validator_scan",
        name: "hbr_validator_scan",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &[
            "evidence_pointer",
            "hbr_evidence_results",
            "hbr_event_ledger",
        ],
        cli_flag: Some("--packet"),
        description: "Resolve PROVED HBR evidence pointers to tests, receipts, artifacts, or EventLedger rows.",
        expected_input: "A work-packet JSON file with HBR matrix rows.",
        expected_output: "Exit 0 when PROVED evidence resolves; downgraded STEER rows written atomically otherwise.",
        common_errors: &[
            "Unknown evidence URI scheme",
            "test evidence has no recorded PASS result",
        ],
        recovery_steps: &[
            "Use test://, receipt://, artifact://, or event:// pointers",
            "Record the passing proof result before claiming PROVED",
        ],
    },
    CommandReference {
        id: "process_ledger_writer",
        name: "process_ledger_writer",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &[
            "process_uuid",
            "engine_kind",
            "started_at",
            "stopped_at",
            "exit_code",
            "owner_role",
            "owner_wp",
        ],
        cli_flag: None,
        description: "Append START and STOP process lifecycle events through the bounded ProcessOwnershipLedger writer.",
        expected_input: "Typed ProcessStart and ProcessStop records from model engines, sandbox containers, workers, plugins, or helper subprocesses.",
        expected_output: "Postgres kernel_process_lifecycle rows with UUID v7 process_uuid values and matched lifecycle timestamps.",
        common_errors: &[
            "Writer channel saturated",
            "STOP arrives after START overflow",
            "Postgres lifecycle migration missing",
        ],
        recovery_steps: &[
            "Inspect FR_EVT_LEDGER_OVERFLOW events",
            "Use the STOP upsert path to preserve termination evidence",
            "Apply migration 0021_kernel_process_lifecycle.sql",
        ],
    },
    CommandReference {
        id: "process_ledger_overflow_event",
        name: "process_ledger_overflow_event",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &[
            "event_type",
            "overflow_count",
            "capacity",
            "dropped_event_kind",
            "sampled_event_payload",
        ],
        cli_flag: None,
        description: "Emit typed FR_EVT_LEDGER_OVERFLOW EventLedger rows when the process lifecycle writer channel saturates.",
        expected_input: "A dropped LedgerEvent sampled from a full bounded channel.",
        expected_output: "FR_EVT_LEDGER_OVERFLOW NewKernelEvent payload with overflow count, configured capacity, dropped event kind, and sampled lifecycle payload.",
        common_errors: &[
            "Overflow sink unavailable",
            "sampled payload lacks process_uuid",
            "degraded flag not cleared after drain",
        ],
        recovery_steps: &[
            "Verify the overflow sink records the event before returning Ok",
            "Drain queued lifecycle events",
            "Confirm process_ledger::is_degraded() returns false after a successful drain",
        ],
    },
    CommandReference {
        id: "process_ledger_reclaim",
        name: "process_ledger_reclaim",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &[
            "session_id",
            "trigger",
            "processes_reclaimed",
            "kill_result",
            "total_duration_ms",
        ],
        cli_flag: None,
        description: "Reclaim open process lifecycle rows on session close, failure, staleness, or operator cancel.",
        expected_input: "A session id and ReclaimTrigger value: close, failure, stale, or operator_cancel.",
        expected_output: "ReclaimReport with every reclaimed process, kill result, and queued STOP evidence using exit_code -1.",
        common_errors: &[
            "SandboxKill binding missing",
            "kill returns an error",
            "STOP writer channel saturated",
        ],
        recovery_steps: &[
            "Treat kill errors as report data, not as permission to skip STOP",
            "Verify the STOP writer accepted ProcessStop rows",
            "Inspect kernel_process_lifecycle open rows with the reclaim FOR UPDATE query",
        ],
    },
    CommandReference {
        id: "process_ledger_staleness_reclaim",
        name: "process_ledger_staleness_reclaim",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["ttl", "scan_interval", "stale_sessions", "trigger"],
        cli_flag: None,
        description: "Run the background staleness reclaim loop with a configurable heartbeat TTL and scan interval.",
        expected_input: "StalenessReclaimConfig plus a stale-session source.",
        expected_output: "Background task that calls process_ledger_reclaim with trigger stale for each stale session.",
        common_errors: &[
            "TTL configured as zero",
            "scan interval configured as zero",
            "stale source fails",
        ],
        recovery_steps: &[
            "Use the 5 minute default TTL when unset",
            "Use the 30 second default scan interval when unset",
            "Keep stale-source failures isolated to the current scan tick",
        ],
    },
    CommandReference {
        id: "diagnostics_capture_headless",
        name: "diagnostics_capture_headless",
        status: CommandStatus::Planned,
        ipc_channel: Some("kernel.diagnostics.capture"),
        tauri_command: Some("diagnostics_capture_headless"),
        schema_fields: &[
            "viewport",
            "selector",
            "screenshot_ref",
            "console_events",
            "network_events",
        ],
        cli_flag: None,
        description: "Capture GUI state, screenshots, console events, and network events without stealing operator focus.",
        expected_input: "A target route, viewport, and optional selector.",
        expected_output: "Diagnostic capture artifact references and replay metadata.",
        common_errors: &[
            "Target route not mounted",
            "foreground window requested during model-driven testing",
        ],
        recovery_steps: &[
            "Use hidden-window/headless mode",
            "Open the diagnostics panel replay for the failed step",
        ],
    },
    CommandReference {
        id: "visual_debug_dom_snapshot",
        name: "kernel_visual_debug_dom_snapshot",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.visual_debug.dom_snapshot"),
        tauri_command: Some("kernel_visual_debug_dom_snapshot"),
        schema_fields: &[
            "kind",
            "selector",
            "root",
            "node_id",
            "node_name",
            "attributes",
            "children",
            "stable_element_id",
        ],
        cli_flag: None,
        description: "Return a CDP DOM.getDocument or selector-scoped DOM.describeNode snapshot for diagnostics without foreground interaction.",
        expected_input: "DomScope JSON: { kind: 'full' } or { kind: 'selector', selector: '<css selector>' }.",
        expected_output: "DomTree JSON with owned nodes, attributes, children, and stable_element_id populated only from data-testid.",
        common_errors: &[
            "WebView2 CDP port unavailable",
            "selector did not match any element",
            "CDP DOM response omitted node data",
        ],
        recovery_steps: &[
            "Check kernel_visual_debug_launch_config for the active CDP port",
            "Retry with a data-testid selector from the current route",
            "Use the screenshot command to confirm the renderer loaded before snapshotting",
        ],
    },
    CommandReference {
        id: "visual_debug_console_stream_start",
        name: "kernel_visual_debug_console_stream_start",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.visual_debug.console_stream.start"),
        tauri_command: Some("kernel_visual_debug_console_stream_start"),
        schema_fields: &[
            "stream_id",
            "kind",
            "level",
            "message",
            "timestamp",
            "stack",
        ],
        cli_flag: None,
        description: "Start a WebView2 CDP Runtime console/exception stream and emit console_event payloads to the diagnostics panel.",
        expected_input: "ConsoleScope JSON: { kind: 'all' }.",
        expected_output: "Object containing a UUID v7 stream_id; subsequent console_event payloads contain stream_id, kind, message, timestamp, and optional stack.",
        common_errors: &[
            "WebView2 CDP port unavailable",
            "CDP Runtime.enable failed",
            "diagnostics event listener not registered",
        ],
        recovery_steps: &[
            "Check kernel_visual_debug_launch_config for the active CDP port",
            "Restart the stream and subscribe to console_event before injecting diagnostics actions",
        ],
    },
    CommandReference {
        id: "visual_debug_console_stream_stop",
        name: "kernel_visual_debug_console_stream_stop",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.visual_debug.console_stream.stop"),
        tauri_command: Some("kernel_visual_debug_console_stream_stop"),
        schema_fields: &["stream_id", "stopped"],
        cli_flag: None,
        description: "Stop a running WebView2 CDP Runtime console/exception stream and unregister its in-memory task handle.",
        expected_input: "The stream_id returned by kernel_visual_debug_console_stream_start.",
        expected_output: "Object with stopped flag indicating whether a matching stream was found and aborted.",
        common_errors: &[
            "Unknown stream_id",
            "stream already stopped",
            "console stream registry unavailable",
        ],
        recovery_steps: &[
            "Treat false stopped as idempotent cleanup",
            "Start a fresh stream before the next visual diagnostics capture",
        ],
    },
    CommandReference {
        id: "backend_inspector_read",
        name: "backend_inspector_read",
        status: CommandStatus::Planned,
        ipc_channel: Some("kernel.inspector.read"),
        tauri_command: Some("backend_inspector_read"),
        schema_fields: &["surface", "selector", "state_snapshot", "trace_id"],
        cli_flag: None,
        description: "Read structured backend state for model navigation without screen scraping.",
        expected_input: "An inspector surface id and optional selector.",
        expected_output: "Structured state snapshot with stable ids and trace references.",
        common_errors: &[
            "Unknown inspector surface",
            "state snapshot exceeds configured budget",
        ],
        recovery_steps: &[
            "List available inspector surfaces",
            "Narrow the selector before retrying",
        ],
    },
    CommandReference {
        id: "kernel_inspector_port",
        name: "kernel_inspector_port",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.inspector.port"),
        tauri_command: Some("kernel_inspector_port"),
        schema_fields: &["port"],
        cli_flag: None,
        description: "Return the optional local inspector HTTP port exposed to diagnostics tooling.",
        expected_input: "No input.",
        expected_output: "InspectorReadV1-adjacent port response: the current random local inspector port, or null when the HTTP inspector is not running.",
        common_errors: &[
            "Inspector port state is not registered",
            "inspector port state mutex poisoned",
        ],
        recovery_steps: &[
            "Verify Tauri Builder manages InspectorPortState",
            "Fall back to in-process kernel.inspector.* IPC commands when no port is running",
        ],
    },
    CommandReference {
        id: "kernel_inspector_list_sessions",
        name: "kernel_inspector_list_sessions",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.inspector.list_sessions"),
        tauri_command: Some("kernel_inspector_list_sessions"),
        schema_fields: &["id", "state", "model_id", "active_process_count"],
        cli_flag: None,
        description: "Read all visible inspector sessions through the in-process InspectorReadV1 trait.",
        expected_input: "No input.",
        expected_output: "InspectorReadV1 SessionSummary rows serialized for Diagnostics-panel navigation.",
        common_errors: &[
            "InspectorReadV1 state is not registered",
            "session list is empty before a governed run starts",
        ],
        recovery_steps: &[
            "Verify Tauri Builder manages Arc<dyn InspectorReadV1>",
            "Start or resume a governed session before expecting active rows",
        ],
    },
    CommandReference {
        id: "kernel_inspector_session_state",
        name: "kernel_inspector_session_state",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.inspector.session_state"),
        tauri_command: Some("kernel_inspector_session_state"),
        schema_fields: &[
            "session_id",
            "id",
            "state",
            "latest_event_id",
            "active_process_count",
        ],
        cli_flag: None,
        description: "Read a single session state by SessionId through the in-process InspectorReadV1 trait.",
        expected_input: "A SessionId for the governed model session.",
        expected_output: "InspectorReadV1 SessionStateRead JSON or null when the session is unknown.",
        common_errors: &[
            "Unknown session_id",
            "InspectorReadV1 state is not registered",
        ],
        recovery_steps: &[
            "Call kernel_inspector_list_sessions to discover valid ids",
            "Confirm the diagnostics panel is attached to the current app process",
        ],
    },
    CommandReference {
        id: "kernel_inspector_event_ledger_tail",
        name: "kernel_inspector_event_ledger_tail",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.inspector.event_ledger_tail"),
        tauri_command: Some("kernel_inspector_event_ledger_tail"),
        schema_fields: &[
            "n",
            "event_id",
            "event_type",
            "event_sequence",
            "created_at_utc",
            "payload_hash",
            "payload",
        ],
        cli_flag: None,
        description: "Read the newest EventLedger rows through the in-process InspectorReadV1 trait without using the HTTP server.",
        expected_input: "A non-negative tail limit n.",
        expected_output: "InspectorReadV1 EventLedgerRow JSON ordered by the reader implementation's tail semantics.",
        common_errors: &[
            "Tail limit is too small to include the needed event",
            "InspectorReadV1 state is not registered",
        ],
        recovery_steps: &[
            "Increase n for the diagnostic query",
            "Use kernel_inspector_trace_projection for session-scoped reconstruction",
        ],
    },
    CommandReference {
        id: "kernel_inspector_process_ledger_active",
        name: "kernel_inspector_process_ledger_active",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.inspector.process_ledger_active"),
        tauri_command: Some("kernel_inspector_process_ledger_active"),
        schema_fields: &["process_uuid", "session_id", "engine_kind", "status"],
        cli_flag: None,
        description: "Read active process ownership rows through the in-process InspectorReadV1 trait.",
        expected_input: "No input.",
        expected_output: "InspectorReadV1 ProcessRow JSON for currently running owned processes.",
        common_errors: &[
            "No active processes",
            "process ledger writer has not emitted START evidence",
        ],
        recovery_steps: &[
            "Inspect session state and EventLedger tail",
            "Use process reclaim surfaces if stale process rows remain",
        ],
    },
    CommandReference {
        id: "kernel_inspector_trace_projection",
        name: "kernel_inspector_trace_projection",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.inspector.trace_projection"),
        tauri_command: Some("kernel_inspector_trace_projection"),
        schema_fields: &[
            "session_id",
            "opened_at",
            "closed_at",
            "what_task",
            "what_context",
            "what_returns",
            "what_tool_calls",
            "what_artifacts",
            "what_validation",
            "what_promotion",
        ],
        cli_flag: None,
        description: "Read the HBR-VIS-004 session TraceProjection through the in-process InspectorReadV1 trait.",
        expected_input: "A SessionId for the governed model session.",
        expected_output: "InspectorReadV1 TraceProjection JSON or null when the session has no durable trace events.",
        common_errors: &[
            "Unknown session_id",
            "session has no durable EventLedger rows",
        ],
        recovery_steps: &[
            "Call kernel_inspector_event_ledger_tail to confirm events exist",
            "Use the HTTP /inspector/v1/trace/{session_id} route only when in-process IPC is unavailable",
        ],
    },
    CommandReference {
        id: "kernel_inspector_loaded_models",
        name: "kernel_inspector_loaded_models",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.inspector.loaded_models"),
        tauri_command: Some("kernel_inspector_loaded_models"),
        schema_fields: &["model_id", "adapter_id", "process_uuid", "loaded_at_utc"],
        cli_flag: None,
        description: "Read loaded model rows through the in-process InspectorReadV1 trait.",
        expected_input: "No input.",
        expected_output: "InspectorReadV1 ModelLoadedRow JSON for local or registered model adapters.",
        common_errors: &[
            "No model is loaded",
            "model runtime has not registered an adapter row",
        ],
        recovery_steps: &[
            "Register or load a model before expecting rows",
            "Inspect process ledger rows for the backing model process",
        ],
    },
    CommandReference {
        id: "inspector_replay_drive",
        name: "inspector_replay_drive",
        status: CommandStatus::Wired,
        ipc_channel: Some("/inspector/v1/replay-drive"),
        tauri_command: None,
        schema_fields: &[
            "action_id",
            "envelope",
            "schema_id",
            "signer",
            "signature",
            "write_box",
            "event_type",
        ],
        cli_flag: None,
        description: "POST the only inspector-plane mutation path through KernelActionCatalogV1 plus a verified WriteBoxV1 envelope.",
        expected_input: "JSON body with exactly action_id and envelope. Any extra or missing top-level field is a forbidden parallel-mutation attempt.",
        expected_output: "ReplayDriveResponse with dispatched status, write_box_id, result_schema_id, and INSPECTOR_REPLAY_DRIVE EventLedger receipt metadata.",
        common_errors: &[
            "Malformed JSON",
            "forbidden body shape",
            "invalid WriteBoxV1 envelope signature",
            "unknown KernelActionCatalogV1 action_id",
        ],
        recovery_steps: &[
            "Send only { action_id, envelope }",
            "Regenerate the envelope signature from the WriteBoxV1 payload",
            "Check the action catalog before retrying",
        ],
    },
    CommandReference {
        id: "swarm_harness_run",
        name: "swarm_harness_run",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &[
            "scenario_id",
            "n",
            "sessions",
            "contention_events",
            "ledger_overflow_count",
        ],
        cli_flag: None,
        description: "Run a bounded multi-agent harness scenario through the shared ScenarioRegistry and return observable session evidence.",
        expected_input: "A registered scenario_id and positive session count n.",
        expected_output: "SwarmReport with per-session results, ProcessLedger START/STOP rows, contention events, and ledger overflow count.",
        common_errors: &[
            "Unknown scenario_id",
            "n is zero",
            "ProcessLedger writer channel saturated",
        ],
        recovery_steps: &[
            "Call the registry-backed n8-perf, session-cancel, or lease-contention scenario id",
            "Reduce n if host capacity is limited",
            "Inspect ledger_overflow_count before retrying",
        ],
    },
    CommandReference {
        id: "handshake_swarm_cli",
        name: "handshake-swarm",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["scenario_id", "n", "report_path"],
        cli_flag: Some("--scenario"),
        description: "Command-line entrypoint for the KERNEL-004 swarm harness; runs registered scenarios without app-runtime.",
        expected_input: "`handshake-swarm --scenario <n8-perf|session-cancel|lease-contention> --n <count> [--report <path>]`.",
        expected_output: "SwarmReport JSON on stdout, or a JSON receipt containing report_path when --report is provided.",
        common_errors: &[
            "Missing --scenario",
            "Missing --n",
            "Unknown scenario id",
            "Report path parent cannot be created",
        ],
        recovery_steps: &[
            "Run with --help to inspect syntax",
            "Use one of the documented scenario ids",
            "Write reports under the configured artifact root",
        ],
    },
    CommandReference {
        id: "kernel_swarm_run",
        name: "kernel_swarm_run",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.swarm.run"),
        tauri_command: Some("kernel_swarm_run"),
        schema_fields: &["scenario_id", "n", "sessions", "ledger_overflow_count"],
        cli_flag: None,
        description: "Debug-build Tauri IPC bridge that launches the swarm harness from Diagnostics-panel tooling through kernel.swarm.run.",
        expected_input: "JSON arguments with scenario_id and positive n; available only in debug builds compiled with feature swarm_ipc.",
        expected_output: "SwarmReport JSON matching the handshake-swarm CLI output shape.",
        common_errors: &[
            "App not compiled with swarm_ipc",
            "Release build omitted the command",
            "Unknown scenario_id",
            "n is zero",
        ],
        recovery_steps: &[
            "Rebuild the Tauri app in debug mode with --features swarm_ipc",
            "Use handshake-swarm CLI when IPC is unavailable",
            "Retry with n8-perf, session-cancel, or lease-contention",
        ],
    },
    CommandReference {
        id: "swarm_scenario_n8_perf",
        name: "n8-perf",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["scenario_id", "n", "sessions"],
        cli_flag: Some("--scenario"),
        description: "n8-perf scenario: run high-volume catalog-backed mutation steps for each requested swarm session.",
        expected_input: "scenario_id n8-perf and positive n; canonical proof uses n=8 with 100 mutations per session.",
        expected_output: "SwarmReport with 100 completed catalog mutation steps per session and zero ledger overflow for healthy runs.",
        common_errors: &[
            "Host too slow for high-volume proof",
            "Unknown action catalog id",
        ],
        recovery_steps: &[
            "Start with --n 2 for smoke testing",
            "Use the hbr-swarm-n8 proof for the full HBR-SWARM evidence floor",
        ],
    },
    CommandReference {
        id: "swarm_scenario_session_cancel",
        name: "session-cancel",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["scenario_id", "n", "sessions"],
        cli_flag: Some("--scenario"),
        description: "session-cancel scenario: exercise cancellation propagation through the ProcessLedger reclaim seam.",
        expected_input: "scenario_id session-cancel and positive n.",
        expected_output: "SwarmReport with each session completing an OperatorCancel reclaim step and closing with START/STOP process rows.",
        common_errors: &[
            "Reclaim trigger missing from scenario steps",
            "ProcessLedger STOP evidence absent",
        ],
        recovery_steps: &[
            "Inspect per-session reclaim_triggers",
            "Run process_ledger_reclaim_tests if STOP evidence is missing",
        ],
    },
    CommandReference {
        id: "swarm_scenario_lease_contention",
        name: "lease-contention",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["scenario_id", "n", "sessions"],
        cli_flag: Some("--scenario"),
        description: "lease-contention scenario: make every requested session target the shared lease workspace through the catalog lease action.",
        expected_input: "scenario_id lease-contention and positive n.",
        expected_output: "SwarmReport with shared-resource mutation attempts, inspector reads, and closed sessions for contention debugging.",
        common_errors: &[
            "Lease action catalog entry missing",
            "Shared workspace evidence is ambiguous",
        ],
        recovery_steps: &[
            "Verify kernel.role_mailbox_claim_lease.project exists in KernelActionCatalogV1",
            "Use per-session envelope refs to trace the contested resource",
        ],
    },
    CommandReference {
        id: "sandbox_adapter_run",
        name: "sandbox_adapter_run",
        status: CommandStatus::Planned,
        ipc_channel: Some("kernel.sandbox.run"),
        tauri_command: Some("sandbox_adapter_run"),
        schema_fields: &[
            "adapter_id",
            "workspace_ref",
            "command_descriptor",
            "process_uuid",
        ],
        cli_flag: None,
        description: "Run model-written code or validation work inside the selected SandboxAdapter.",
        expected_input: "Adapter id, workspace ref, command descriptor, and ownership context.",
        expected_output: "Sandbox run result with process ledger references and denial evidence when blocked.",
        common_errors: &[
            "Adapter unavailable",
            "command descriptor requests undeclared filesystem or network access",
        ],
        recovery_steps: &[
            "Check sandbox_adapter_health",
            "Repair the command descriptor allowlist and retry",
        ],
    },
    CommandReference {
        id: "sandbox_adapter_health",
        name: "sandbox_adapter_health",
        status: CommandStatus::Planned,
        ipc_channel: Some("kernel.sandbox.health"),
        tauri_command: Some("sandbox_adapter_health"),
        schema_fields: &["adapter_id", "availability", "reason", "host_capabilities"],
        cli_flag: None,
        description: "Report adapter availability and host capability gaps before a sandbox run starts.",
        expected_input: "Optional adapter id filter.",
        expected_output: "Health rows for WSL2 Podman, Windows native jail, Docker compatibility, and future adapters.",
        common_errors: &[
            "Host capability probe failed",
            "adapter is intentionally compatibility-only",
        ],
        recovery_steps: &[
            "Select an available adapter",
            "Use Docker only when explicitly opted in",
        ],
    },
    CommandReference {
        id: "model_runtime_generate",
        name: "model_runtime_generate",
        status: CommandStatus::Planned,
        ipc_channel: Some("kernel.model_runtime.generate"),
        tauri_command: Some("model_runtime_generate"),
        schema_fields: &[
            "model_id",
            "prompt",
            "runtime_adapter",
            "generation_options",
            "process_uuid",
        ],
        cli_flag: None,
        description: "Generate text through the bound local ModelRuntime adapter.",
        expected_input: "Model id, prompt, and generation options.",
        expected_output: "Generated text plus runtime metrics, process ledger refs, and cache metadata.",
        common_errors: &[
            "Model not loaded",
            "runtime adapter does not support requested technique",
        ],
        recovery_steps: &[
            "Register or load the model",
            "Disable unsupported inference-lab toggles",
        ],
    },
    CommandReference {
        id: "model_runtime_register_model",
        name: "model_runtime_register_model",
        status: CommandStatus::Planned,
        ipc_channel: Some("kernel.model_runtime.register_model"),
        tauri_command: None,
        schema_fields: &[
            "model_id",
            "artifact_path",
            "sha256",
            "adapter_kind",
            "capabilities",
        ],
        cli_flag: None,
        description: "Catalog-only governed action for registering a local model artifact with immutable adapter binding and capability metadata.",
        expected_input: "Artifact path, sha256, adapter kind, and capability declaration.",
        expected_output: "Model registry entry available to runtime and inference-lab surfaces after the governed action is wired.",
        common_errors: &[
            "Artifact hash mismatch",
            "adapter kind unsupported for artifact format",
        ],
        recovery_steps: &[
            "Recompute sha256",
            "Choose LlamaCppRuntime for GGUF or CandleRuntime for safetensors where supported",
            "Use catalog action kernel.model_runtime.register_model rather than a Tauri command until the IPC surface is implemented",
        ],
    },
    CommandReference {
        id: "inference_lab_apply_technique",
        name: "inference_lab_apply_technique",
        status: CommandStatus::Planned,
        ipc_channel: Some("kernel.inference_lab.apply_technique"),
        tauri_command: Some("inference_lab_apply_technique"),
        schema_fields: &["technique_id", "enabled", "parameters", "model_id"],
        cli_flag: None,
        description: "Apply a supported inference technique such as LoRA, KV cache controls, steering vectors, or speculative decoding.",
        expected_input: "Technique id, model id, enabled flag, and technique parameters.",
        expected_output: "Updated runtime configuration or a typed unsupported-technique failure.",
        common_errors: &[
            "Technique unsupported by selected adapter",
            "parameter range invalid",
        ],
        recovery_steps: &[
            "Inspect model capabilities",
            "Reset the technique to its default parameters",
        ],
    },
    CommandReference {
        id: "memory_capsule_build",
        name: "memory_capsule_build",
        status: CommandStatus::Planned,
        ipc_channel: Some("kernel.memory_capsule.build"),
        tauri_command: Some("memory_capsule_build"),
        schema_fields: &["task_kind", "budget_tokens", "capsule_refs", "audit_ref"],
        cli_flag: None,
        description: "Build a bounded memory capsule for a task from approved retrieval sources.",
        expected_input: "Task kind, token budget, and retrieval policy parameters.",
        expected_output: "Memory capsule artifact and audit reference.",
        common_errors: &[
            "Capsule exceeds token budget",
            "retrieval policy returns no eligible sources",
        ],
        recovery_steps: &[
            "Lower top-k or capsule size",
            "Inspect retrieval audit refs",
        ],
    },
    CommandReference {
        id: "memory_calibration_snapshot",
        name: "kernel_memory_calibration_snapshot",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.memory_calibration.snapshot"),
        tauri_command: Some("kernel_memory_calibration_snapshot"),
        schema_fields: &[
            "bloat",
            "stale_dominance",
            "trust_drift",
            "embedding_gap",
            "degradation_rate",
            "hygiene_lag",
            "signal_errors",
        ],
        cli_flag: None,
        description: "Return the read-only FEMS calibration snapshot for operator and model diagnostics.",
        expected_input: "No input.",
        expected_output: "CalibrationSnapshot with six typed signal values, thresholds, timestamps, details, and per-signal source errors.",
        common_errors: &[
            "Postgres memory calibration state unavailable",
            "invalid calibration threshold",
            "calibration source IO failed",
        ],
        recovery_steps: &[
            "Check DATABASE_URL and Postgres availability",
            "Inspect signal_errors to identify which collector failed",
            "Run calibration_tests for threshold and snapshot-shape regression coverage",
        ],
    },
    CommandReference {
        id: "self_improvement_run_iteration",
        name: "self_improvement_run_iteration",
        status: CommandStatus::Planned,
        ipc_channel: Some("kernel.self_improvement.run_iteration"),
        tauri_command: Some("self_improvement_run_iteration"),
        schema_fields: &[
            "corpus_id",
            "dev_score",
            "holdout_score",
            "promotion_decision",
        ],
        cli_flag: None,
        description: "Run one bounded self-improvement iteration against the fixed HBR corpus.",
        expected_input: "Corpus id, editable surface parameters, and promotion thresholds.",
        expected_output: "Iteration report with dev and holdout metrics plus promotion decision.",
        common_errors: &[
            "Goodhart sentinel paused the loop",
            "holdout score below promotion floor",
        ],
        recovery_steps: &[
            "Review promotion gate evidence",
            "Adjust only approved retrieval or manual parameters",
        ],
    },
    CommandReference {
        id: "distillation_candidate_review",
        name: "distillation_candidate_review",
        status: CommandStatus::Planned,
        ipc_channel: Some("kernel.distillation.review_candidate"),
        tauri_command: Some("distillation_candidate_review"),
        schema_fields: &[
            "candidate_id",
            "license_tags",
            "pii_scan",
            "operator_decision",
        ],
        cli_flag: None,
        description: "Review a distillation candidate before promotion into a model runtime surface.",
        expected_input: "Candidate id and review decision payload.",
        expected_output: "Promotion, rejection, or remediation receipt with review evidence refs.",
        common_errors: &[
            "PII scan failed",
            "license tag missing",
            "operator decision absent",
        ],
        recovery_steps: &[
            "Run content review again",
            "Attach required provenance and operator decision refs",
        ],
    },
    // ---- WP-KERNEL-005 atelier (Core-Data) command references ----
    // MT-052: character identity + sheet workflows.
    CommandReference {
        id: "atelier_create_character",
        name: "AtelierStore::create_character",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["public_id", "internal_id", "display_name", "created_at_utc"],
        cli_flag: None,
        description: "Create an atelier character with a stable operator-facing public_id distinct from the internal storage id (MT-006).",
        expected_input: "A NewCharacter with display_name and an optional caller-supplied public_id.",
        expected_output: "A Character row with both public_id and internal_id; storage keys never leak through the public_id.",
        common_errors: &[
            "duplicate public_id",
            "display_name empty",
        ],
        recovery_steps: &[
            "Pick a unique public_id",
            "Use get_character_by_public_id to resolve identity before retrying",
        ],
    },
    CommandReference {
        id: "atelier_get_character_by_public_id",
        name: "AtelierStore::get_character_by_public_id",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["public_id", "internal_id"],
        cli_flag: None,
        description: "Resolve a character's internal id from its stable public_id without exposing storage keys.",
        expected_input: "A non-empty public_id string.",
        expected_output: "The matching Character, or a NotFound error.",
        common_errors: &["unknown public_id"],
        recovery_steps: &["List characters or create the character first"],
    },
    CommandReference {
        id: "atelier_append_sheet_version",
        name: "AtelierStore::append_sheet_version",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["version_id", "character_internal_id", "parent_version_id", "created_at_utc"],
        cli_flag: None,
        description: "Append a new immutable character-sheet version with parent linkage and provenance (MT-012); prior versions are never mutated.",
        expected_input: "A NewSheetVersion with character_internal_id, sheet body, and provenance.",
        expected_output: "A SheetVersion row chained to its parent so no edit silently loses prior text.",
        common_errors: &[
            "unknown character_internal_id",
            "stale parent_version_id",
        ],
        recovery_steps: &[
            "Fetch latest_sheet_version before appending",
            "Re-read sheet_version_history to find the current head",
        ],
    },
    CommandReference {
        id: "atelier_apply_sheet_field_edits",
        name: "AtelierStore::apply_sheet_field_edits",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["version_id", "field_edits", "parent_version_id"],
        cli_flag: None,
        description: "Apply typed field edits to a sheet by appending a new version rather than mutating the prior snapshot.",
        expected_input: "The target character/sheet plus a set of field edits.",
        expected_output: "A new SheetVersion reflecting the edits with the prior version preserved.",
        common_errors: &[
            "field path not present in the sheet template",
            "concurrent edit raced the parent version",
        ],
        recovery_steps: &[
            "Re-read the latest version and rebase the edits",
            "Use bulk_apply_sheet_field_edits for multi-field changes",
        ],
    },
    CommandReference {
        id: "atelier_sheet_version_history",
        name: "AtelierStore::sheet_version_history",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["character_internal_id", "version_id", "parent_version_id", "created_at_utc"],
        cli_flag: None,
        description: "Read the append-only version history for a character sheet, newest to oldest.",
        expected_input: "A character_internal_id.",
        expected_output: "An ordered list of SheetVersion rows forming the parent chain.",
        common_errors: &["character has no sheet versions yet"],
        recovery_steps: &["Append an initial sheet version before reading history"],
    },
    // MT-053: media library + intake review workflows.
    CommandReference {
        id: "atelier_materialize_media_asset",
        name: "AtelierStore::materialize_media_asset",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["asset_id", "content_hash", "artifact_ref", "byte_len"],
        cli_flag: None,
        description: "Materialize a media asset in the DAM with content-hash dedup; bytes live in the ArtifactStore (MT-015), never on random filesystem paths.",
        expected_input: "A NewMediaAsset with content_hash, artifact_ref (artifact://...), and byte_len.",
        expected_output: "A stable MediaAsset; re-materializing identical bytes returns the existing asset.",
        common_errors: &[
            "artifact_ref is not an artifact:// ref",
            "byte_len not positive",
        ],
        recovery_steps: &[
            "Store bytes in the ArtifactStore and use the returned artifact:// ref",
            "Look up get_media_asset_by_hash before re-importing",
        ],
    },
    CommandReference {
        id: "atelier_open_intake_batch",
        name: "AtelierStore::open_intake_batch",
        status: CommandStatus::Wired,
        ipc_channel: Some("/atelier/intake/batches"),
        tauri_command: None,
        schema_fields: &["idempotency_key", "source_label", "mode", "profile_mode", "batch_id", "status"],
        cli_flag: None,
        description: "Open (idempotently) an intake batch for media review through the wired Axum atelier route.",
        expected_input: "POST /atelier/intake/batches with idempotency_key, source_label, optional mode (manual|folder_scan|sourcing_run) and profile_mode.",
        expected_output: "An IntakeBatch JSON row; replaying the same idempotency_key returns the existing batch.",
        common_errors: &[
            "invalid_mode",
            "invalid_profile_mode",
            "missing idempotency_key",
        ],
        recovery_steps: &[
            "Send a supported mode/profile_mode token",
            "Reuse the same idempotency_key to resume instead of duplicating",
        ],
    },
    CommandReference {
        id: "atelier_list_intake_batch_items",
        name: "list_intake_batch_items",
        status: CommandStatus::Wired,
        ipc_channel: Some("/atelier/intake/batches/:batch_id/items"),
        tauri_command: None,
        schema_fields: &["lane_counts", "items", "item_id", "lane", "file_name"],
        cli_flag: None,
        description: "List lane counts and a capped page of intake items for a batch through the wired Axum atelier route.",
        expected_input: "GET /atelier/intake/batches/:batch_id/items with a valid batch UUID.",
        expected_output: "Lane counts (pending/accepted/rejected/deferred/skipped/failed) plus capped item rows.",
        common_errors: &[
            "unknown batch_id",
            "malformed batch UUID",
        ],
        recovery_steps: &[
            "List intake batches to find a valid batch_id",
            "Trust lane_counts for true totals; the item list is capped at 200",
        ],
    },
    CommandReference {
        id: "atelier_apply_intake_classification",
        name: "AtelierStore::apply_intake_classification",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["item_id", "lane", "classification", "decided_by"],
        cli_flag: None,
        description: "Apply a review classification to an intake item, moving it into the accepted/rejected/deferred lane with an audit row.",
        expected_input: "An intake item id plus a classification decision.",
        expected_output: "The item with its updated lane and a recorded rejection/decision audit when applicable.",
        common_errors: &[
            "unknown item_id",
            "classification not permitted for the item's current lane",
        ],
        recovery_steps: &[
            "Call classify_intake_item to preview the decision",
            "Inspect list_intake_rejection_audits for prior decisions",
        ],
    },
    CommandReference {
        id: "atelier_bulk_update_media_review_metadata",
        name: "AtelierStore::bulk_update_media_review_metadata",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["asset_ids", "review_status", "review_note", "updated"],
        cli_flag: None,
        description: "Bulk-update review metadata across many media assets in one operation for intake review triage.",
        expected_input: "A set of asset ids and the review metadata to apply.",
        expected_output: "The count of updated assets; the operation targets the full supplied set, not a rendered subset.",
        common_errors: &[
            "empty asset id set",
            "one or more asset ids unknown",
        ],
        recovery_steps: &[
            "Resolve asset ids through get_media_review_metadata first",
            "Re-run with only the valid asset ids",
        ],
    },
    // MT-054: collection + contact-sheet workflows.
    CommandReference {
        id: "atelier_create_collection",
        name: "AtelierStore::create_collection",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["collection_id", "name", "notes", "created_at_utc"],
        cli_flag: None,
        description: "Create a named, ordered image collection with optional notes/tags and character/sheet links (MT-018).",
        expected_input: "A NewCollection with a name and optional metadata.",
        expected_output: "A Collection row addressable by collection_id.",
        common_errors: &["empty collection name"],
        recovery_steps: &["Provide a non-empty name and retry"],
    },
    CommandReference {
        id: "atelier_add_images_to_collection",
        name: "AtelierStore::add_images_to_collection",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["collection_id", "asset_ids", "added_count"],
        cli_flag: None,
        description: "Add media assets to a collection in a stable order, ignoring duplicates already present.",
        expected_input: "A collection_id and a list of media asset ids.",
        expected_output: "The collection membership extended with the new ordered items.",
        common_errors: &[
            "unknown collection_id",
            "unknown asset id in the set",
        ],
        recovery_steps: &[
            "Create the collection first",
            "Materialize the media assets before adding them",
        ],
    },
    CommandReference {
        id: "atelier_create_contact_sheet",
        name: "AtelierStore::create_contact_sheet",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["sheet_id", "collection_id", "member_asset_ids", "member_content_hashes"],
        cli_flag: None,
        description: "Capture a reproducible contact-sheet manifest snapshotting collection membership (asset ids + content hashes) at capture time.",
        expected_input: "A source collection_id.",
        expected_output: "A ContactSheet whose membership stays fixed even as the collection later evolves.",
        common_errors: &[
            "unknown collection_id",
            "collection is empty",
        ],
        recovery_steps: &[
            "Add images to the collection before capturing",
            "List contact sheets to confirm prior snapshots",
        ],
    },
    CommandReference {
        id: "atelier_render_contact_sheet_svg_artifact",
        name: "AtelierStore::render_contact_sheet_svg_artifact",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["sheet_id", "artifact_ref", "content_hash", "byte_len"],
        cli_flag: None,
        description: "Render a contact sheet to a deterministic SVG artifact stored in the ArtifactStore.",
        expected_input: "A contact sheet_id.",
        expected_output: "An artifact:// ref plus content hash for the rendered SVG.",
        common_errors: &[
            "unknown sheet_id",
            "member asset bytes unavailable",
        ],
        recovery_steps: &[
            "Confirm the contact sheet exists",
            "Verify member assets still resolve in the ArtifactStore",
        ],
    },
    CommandReference {
        id: "atelier_plan_contact_sheet_raster_export",
        name: "ContactSheetRasterExportPlan",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["plan_id", "sheet_id", "format", "status", "reason"],
        cli_flag: None,
        description: "Record an explicit planned PNG/JPG contact-sheet raster export (MT-037); no raster renderer is implemented and no output artifact is produced.",
        expected_input: "A contact sheet_id and a raster format (png|jpg).",
        expected_output: "A ContactSheetRasterExportPlan with status planned and a deferral reason; never an artifact ref.",
        common_errors: &[
            "unknown raster format token",
            "caller expects rendered raster bytes",
        ],
        recovery_steps: &[
            "Use render_contact_sheet_svg_artifact for a real rendered artifact",
            "Treat the plan as a durable deferral marker, not a render",
        ],
    },
    // MT-055: character document, story card/beat, character script workflows.
    CommandReference {
        id: "atelier_create_character_document",
        name: "AtelierStore::create_character_document",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["document_id", "character_internal_id", "doc_kind", "title"],
        cli_flag: None,
        description: "Create a character-scoped note/story/moodboard document with stable id and typed metadata (MT-038).",
        expected_input: "A NewCharacterDocument with character_internal_id, doc_kind, and title.",
        expected_output: "A CharacterDocument row whose text lives in append-only versions.",
        common_errors: &[
            "unknown character_internal_id",
            "unsupported doc_kind",
        ],
        recovery_steps: &[
            "Create the character first",
            "Use a supported document kind token",
        ],
    },
    CommandReference {
        id: "atelier_append_character_document_version",
        name: "AtelierStore::append_character_document_version",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["document_id", "version_id", "parent_version_id", "text"],
        cli_flag: None,
        description: "Append a new immutable text version to a character document so prior note/story text is preserved exactly.",
        expected_input: "A document_id and the new text body.",
        expected_output: "A new document version chained to its parent.",
        common_errors: &[
            "unknown document_id",
            "stale parent_version_id",
        ],
        recovery_steps: &[
            "Read latest_character_document_version before appending",
            "Inspect character_document_history for the head",
        ],
    },
    CommandReference {
        id: "atelier_add_story_card",
        name: "AtelierStore::add_story_card",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["card_id", "story_document_id", "title", "seq"],
        cli_flag: None,
        description: "Add an ordered story card to a story document.",
        expected_input: "A NewStoryCard referencing a story_document_id.",
        expected_output: "A StoryCard row ordered within its story document.",
        common_errors: &["unknown story_document_id"],
        recovery_steps: &["Create the story document before adding cards"],
    },
    CommandReference {
        id: "atelier_add_story_beat",
        name: "AtelierStore::add_story_beat",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["beat_id", "story_document_id", "card_id", "seq"],
        cli_flag: None,
        description: "Add an ordered story beat to a story document/card.",
        expected_input: "A NewStoryBeat referencing a story_document_id.",
        expected_output: "A StoryBeat row ordered within its story document.",
        common_errors: &["unknown story_document_id"],
        recovery_steps: &["List story beats to confirm ordering before inserting"],
    },
    CommandReference {
        id: "atelier_create_character_script",
        name: "AtelierStore::create_character_script",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["script_id", "character_internal_id", "authority_mode", "provenance"],
        cli_flag: None,
        description: "Persist a character image-sourcing script as data-only (MT-040); no runner, command, or hidden execution flag is exposed.",
        expected_input: "A NewCharacterScript with character_internal_id, script text, and provenance.",
        expected_output: "A CharacterScript constrained to a data-only authority mode.",
        common_errors: &[
            "unknown character_internal_id",
            "attempt to set an executable authority mode",
        ],
        recovery_steps: &[
            "Create the character first",
            "Keep authority_mode data-only; scripts are not executable here",
        ],
    },
    // MT-056: moodboard workflows.
    CommandReference {
        id: "atelier_record_moodboard_snapshot",
        name: "AtelierStore::record_moodboard_snapshot",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["snapshot_id", "document_id", "schema_id", "schema_version", "source_json"],
        cli_flag: None,
        description: "Record a schema-versioned moodboard JSON snapshot attached to a character document (MT-042); the exact source JSON is preserved for round-trip export.",
        expected_input: "A moodboard document id and a moodboard JSON payload matching hsk.atelier.moodboard@1.",
        expected_output: "A snapshot row plus a validated JSONB projection.",
        common_errors: &[
            "schema_id mismatch",
            "moodboard JSON fails schema validation",
        ],
        recovery_steps: &[
            "Use schema_id hsk.atelier.moodboard@1",
            "Validate the layer model against the moodboard JSON schema",
        ],
    },
    CommandReference {
        id: "atelier_record_moodboard_operation",
        name: "AtelierStore::record_moodboard_operation",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["operation_id", "document_id", "operation_kind", "receipt_schema_id"],
        cli_flag: None,
        description: "Record a moodboard layer operation with a typed operation receipt.",
        expected_input: "A moodboard document id and an operation descriptor.",
        expected_output: "A moodboard operation row with a receipt referencing hsk.atelier.moodboard_operation_receipt@1.",
        common_errors: &[
            "unknown moodboard document",
            "unsupported operation_kind",
        ],
        recovery_steps: &[
            "Record a moodboard snapshot before applying operations",
            "List moodboard operations to confirm ordering",
        ],
    },
    CommandReference {
        id: "atelier_request_moodboard_export",
        name: "AtelierStore::request_moodboard_export",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["export_request_id", "document_id", "manifest_schema_id", "status"],
        cli_flag: None,
        description: "Request a moodboard export and record its manifest/receipt for later rendering.",
        expected_input: "A moodboard document id.",
        expected_output: "A moodboard export request referencing hsk.atelier.moodboard_export_manifest@1.",
        common_errors: &["no moodboard snapshot recorded yet"],
        recovery_steps: &["Record a moodboard snapshot before requesting an export"],
    },
    // MT-057: character relationship workflows.
    CommandReference {
        id: "atelier_create_character_relationship",
        name: "AtelierStore::create_character_relationship",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["relationship_id", "from_character_internal_id", "to_character_internal_id", "relationship_kind"],
        cli_flag: None,
        description: "Create a character-to-character relationship edge with explicit endpoint validation (MT-044).",
        expected_input: "A from/to character internal id pair and a relationship kind.",
        expected_output: "A relationship edge row that both endpoints resolve to.",
        common_errors: &[
            "endpoint character not found",
            "self-referential edge rejected",
        ],
        recovery_steps: &[
            "Confirm both characters exist",
            "Use distinct from/to endpoints",
        ],
    },
    CommandReference {
        id: "atelier_update_character_relationship",
        name: "AtelierStore::update_character_relationship",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["relationship_id", "relationship_kind", "notes"],
        cli_flag: None,
        description: "Update an existing relationship edge's kind/notes without changing its validated endpoints.",
        expected_input: "A relationship_id and the updated fields.",
        expected_output: "The updated relationship edge.",
        common_errors: &["unknown relationship_id"],
        recovery_steps: &["List character relationships to find a valid relationship_id"],
    },
    CommandReference {
        id: "atelier_character_relationship_graph",
        name: "AtelierStore::character_relationship_graph",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["nodes", "edges", "character_internal_id"],
        cli_flag: None,
        description: "Project the stored relationship edges into a relationship-map graph for a character neighbourhood.",
        expected_input: "A seed character_internal_id.",
        expected_output: "A graph projection of nodes and validated edges.",
        common_errors: &["seed character has no relationships"],
        recovery_steps: &["Create relationship edges before projecting the graph"],
    },
    // MT-058: search tag palette + similarity.
    CommandReference {
        id: "atelier_global_search_with_lens_filters",
        name: "AtelierStore::global_search_with_lens_filters",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["query", "lens_filters", "results"],
        cli_flag: None,
        description: "Run global search across the atelier graph with lens filters over characters, media, tags, and documents.",
        expected_input: "A query string and optional lens filters.",
        expected_output: "Ranked search results spanning the atelier entity kinds.",
        common_errors: &["empty query with no lens filters"],
        recovery_steps: &["Provide a query or at least one lens filter"],
    },
    CommandReference {
        id: "atelier_ensure_tag",
        name: "AtelierStore::ensure_tag",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["tag_id", "normalized_text", "text"],
        cli_flag: None,
        description: "Ensure a tag exists in the deduplicated tag dictionary (palette), normalizing by text.",
        expected_input: "A tag text string.",
        expected_output: "The existing or newly created Tag from the palette dictionary.",
        common_errors: &["empty tag text"],
        recovery_steps: &["Provide non-empty tag text; list_all_tags shows the palette"],
    },
    CommandReference {
        id: "atelier_create_tag_rule",
        name: "AtelierStore::create_tag_rule",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["rule_id", "match_spec", "tag_text"],
        cli_flag: None,
        description: "Create a deterministic tag rule; derived tags are recomputed in rule_id order so output is reproducible across runs.",
        expected_input: "A NewTagRule with a match spec and target tag text.",
        expected_output: "A TagRule applied deterministically by recompute_derived_tags.",
        common_errors: &[
            "invalid match spec",
            "empty target tag text",
        ],
        recovery_steps: &[
            "Fix the match spec",
            "Run recompute_derived_tags to reapply rules deterministically",
        ],
    },
    CommandReference {
        id: "atelier_record_ai_tag_suggestion",
        name: "AtelierStore::record_ai_tag_suggestion",
        status: CommandStatus::Wired,
        ipc_channel: Some("/atelier/ai-tag-suggestions"),
        tauri_command: None,
        schema_fields: &["suggestion_id", "character_internal_id", "tag_text", "model_receipt_ref", "tool_receipt_ref", "status"],
        cli_flag: None,
        description: "Record an AI-proposed tag suggestion with model/tool receipt provenance through the wired Axum atelier route; accept/reject/apply transition it.",
        expected_input: "POST /atelier/ai-tag-suggestions with character_internal_id, tag_text, model_receipt_ref, tool_receipt_ref, and suggested_by.",
        expected_output: "An AiTagSuggestion in the proposed status, addressable by suggestion_id.",
        common_errors: &[
            "missing model_receipt_ref or tool_receipt_ref",
            "unknown character_internal_id",
        ],
        recovery_steps: &[
            "Attach both model and tool receipt refs",
            "Use the accept/reject/apply routes to transition the suggestion",
        ],
    },
    CommandReference {
        id: "atelier_find_similar_assets",
        name: "AtelierStore::find_similar_assets",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["asset_internal_id", "dhash_hex", "max_distance", "neighbours"],
        cli_flag: None,
        description: "Find near-duplicate/similar media assets by Hamming distance over the 64-bit perceptual hash (dHash) similarity projection.",
        expected_input: "A seed asset id (or dhash hex) and a max Hamming distance.",
        expected_output: "Nearest-neighbour assets ranked by perceptual-hash distance.",
        common_errors: &[
            "seed asset has no similarity projection",
            "invalid dhash hex",
        ],
        recovery_steps: &[
            "Run project_similarity_from_image_bytes to populate the projection",
            "Provide a valid 64-bit hex dHash",
        ],
    },
    // MT-059, MT-073, MT-074, MT-075: exports + backup.
    CommandReference {
        id: "atelier_request_sheet_export",
        name: "AtelierStore::request_sheet_export",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["export_id", "character_internal_id", "sheet_version_id", "format", "status"],
        cli_flag: None,
        description: "Open a character-sheet export request pinned to an immutable sheet version (MT-199), in a requested format (txt|md|json|pdf).",
        expected_input: "A NewExportRequest with character_internal_id, the pinned sheet_version_id, and a format.",
        expected_output: "An ExportRequest in status pending; the export reflects exactly the pinned snapshot.",
        common_errors: &[
            "sheet version does not belong to the character",
            "unknown sheet_version_id",
        ],
        recovery_steps: &[
            "Pin a sheet_version_id that belongs to the character",
            "Append a sheet version before exporting",
        ],
    },
    CommandReference {
        id: "atelier_build_share_pack_manifest",
        name: "AtelierStore::build_share_pack_manifest",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["export_id", "entries", "selected_media_count", "pack_path"],
        cli_flag: None,
        description: "Assemble a share-pack manifest (sheet + selected media + usage README) into ordered ArtifactStore-backed entries with stable pack paths (MT-073).",
        expected_input: "A SharePackBuildRequest with an export_id, a subset selector, and a usage README artifact.",
        expected_output: "A SharePackBuildResult with ordered manifest entries and the selected media count.",
        common_errors: &[
            "selector includes the sheet but no export result is recorded",
            "selector includes neither the sheet nor any media",
            "usage_readme.artifact_ref is not an artifact:// ref",
        ],
        recovery_steps: &[
            "Record an export result before including the sheet",
            "Select the sheet or at least one media asset",
        ],
    },
    CommandReference {
        id: "atelier_request_web_portfolio_export",
        name: "AtelierStore::request_web_portfolio_export",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["portfolio_export_id", "source_collection_id", "slug", "title", "status"],
        cli_flag: None,
        description: "Open a portable web portfolio export request for a source collection with a no-space portable slug (MT-048/MT-059).",
        expected_input: "A NewWebPortfolioExportRequest with source_collection_id, a portable slug, and a title.",
        expected_output: "A WebPortfolioExportRequest in status pending, later rendered to manifest hsk.atelier.web_portfolio_export_manifest@1.",
        common_errors: &[
            "slug contains blank spaces or non-portable characters",
            "unknown source_collection_id",
        ],
        recovery_steps: &[
            "Use a lowercase, no-space slug of letters/digits/hyphens/underscores",
            "Create the source collection before exporting",
        ],
    },
    CommandReference {
        id: "atelier_build_llm_evidence_pack_manifest",
        name: "exports::build_llm_evidence_pack_manifest",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["schema_id", "pack_id", "files", "source_anchors", "redaction_required"],
        cli_flag: None,
        description: "Build and validate a deterministic LLM evidence-pack manifest (README, evidence, redactions, source-index) for no-context model ingestion (MT-072/MT-074).",
        expected_input: "A pack_id, requested_by, and the four required pack files with source anchors.",
        expected_output: "A validated LlmEvidencePackManifest (schema hsk.atelier.llm_evidence_pack_manifest@1) in deterministic file order.",
        common_errors: &[
            "missing one of the four required files",
            "redaction_required file not marked redacted",
            "duplicate file kind or pack_path",
        ],
        recovery_steps: &[
            "Include README.md, evidence.json, redactions.json, and source-index.json",
            "Mark redaction_required files as redacted before validation",
        ],
    },
    CommandReference {
        id: "atelier_record_backup_manifest",
        name: "AtelierStore::record_backup_manifest",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["backup_id", "app_version", "spec_version", "schema_version", "manifest_hash", "files"],
        cli_flag: None,
        description: "Record a durable backup manifest with app/spec/schema version and per-file checksum traceability (MT-049/MT-075).",
        expected_input: "A NewBackupManifest with version tokens, an artifact ref, and at least one file row.",
        expected_output: "A BackupManifestRecord (schema hsk.atelier.backup_manifest@1) with a manifest hash.",
        common_errors: &[
            "empty file list",
            "duplicate logical_path",
            "version token contains blank spaces",
        ],
        recovery_steps: &[
            "Include at least one file with a portable logical_path",
            "Use no-space version tokens",
        ],
    },
    CommandReference {
        id: "atelier_backup_restore_preflight",
        name: "AtelierStore::preflight_backup_restore",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["preflight_id", "backup_id", "status", "refusal_reason", "current_app_version"],
        cli_flag: None,
        description: "Run and record a backup restore compatibility preflight; a newer-versioned backup is refused with a typed reason (MT-075).",
        expected_input: "A BackupRestorePreflightRequest with the backup_id and the current app/spec/schema versions.",
        expected_output: "A BackupRestorePreflight with status accepted or refused plus a refusal_reason when refused.",
        common_errors: &[
            "newer app/spec/schema backup cannot be restored",
            "unknown backup_id",
        ],
        recovery_steps: &[
            "Upgrade the current app/spec/schema before restoring a newer backup",
            "Record the backup manifest before preflighting",
        ],
    },
    // MT-060: reset recovery + orphan adoption.
    CommandReference {
        id: "atelier_record_atelier_reset",
        name: "AtelierStore::record_atelier_reset",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["reset_id", "mode", "orphan_manifest_id", "recoverable_counts"],
        cli_flag: None,
        description: "Execute a bounded atelier reset; full_preserve_original_media keeps original media in a durable orphan manifest (MT-060).",
        expected_input: "An AtelierResetRequest with mode preferences_only or full_preserve_original_media.",
        expected_output: "An AtelierResetRecord; a full reset records an orphan_manifest_id (schema hsk.atelier.orphan_manifest@1).",
        common_errors: &[
            "unsupported reset mode",
            "caller expects original media to be destroyed",
        ],
        recovery_steps: &[
            "Use a supported reset mode token",
            "Recover media via list_orphan_manifest_items after a full reset",
        ],
    },
    CommandReference {
        id: "atelier_list_orphan_manifest_items",
        name: "AtelierStore::list_orphan_manifest_items",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["orphan_manifest_id", "items", "adoption_status"],
        cli_flag: None,
        description: "List retained orphan manifest items preserved by a full reset, with their orphaned/adopted status.",
        expected_input: "An orphan_manifest_id.",
        expected_output: "Orphan items still available for adoption back into intake.",
        common_errors: &["unknown orphan_manifest_id"],
        recovery_steps: &["Run a full_preserve_original_media reset to produce an orphan manifest"],
    },
    CommandReference {
        id: "atelier_adopt_orphan_manifest_item",
        name: "AtelierStore::adopt_orphan_manifest_item",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["orphan_item_id", "adopted_batch_id", "adopted_item_id", "adopted_by"],
        cli_flag: None,
        description: "Adopt a retained orphan manifest item back into an intake batch, flipping its adoption status (MT-060).",
        expected_input: "An OrphanAdoptionRequest naming the orphan item and the target intake batch.",
        expected_output: "An OrphanAdoptionResult linking the orphan item to its adopted intake batch/item.",
        common_errors: &[
            "orphan item already adopted",
            "target intake batch not open",
        ],
        recovery_steps: &[
            "List orphan manifest items to confirm an item is still orphaned",
            "Open an intake batch before adopting",
        ],
    },
    // ---- WP-KERNEL-005 Pose / ComfyUI pipeline command references ----
    // MT-122: pose context + rig CRUD + calibration.
    CommandReference {
        id: "atelier_set_pose_context_state",
        name: "AtelierStore::set_pose_context_state",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["context_id", "state_seq", "workspace_ref", "kind", "selected_rig_id"],
        cli_flag: None,
        description: "Append a typed pose editor context state (blank/single_image/character_linked/collection_linked) for a workspace; append-only so switching modes never deletes rigs, source media, or linked collections.",
        expected_input: "A NewPoseContextState with workspace_ref, a PoseContextKind, and the mode-appropriate image/character/collection/rig links.",
        expected_output: "A persisted PoseContextState with an incremented state_seq.",
        common_errors: &[
            "blank context carries image/character/collection/rig links",
            "single_image context missing source_asset_id",
        ],
        recovery_steps: &[
            "Match the context kind to its required links",
            "Read current_pose_context_state to inspect the active head before appending",
        ],
    },
    CommandReference {
        id: "atelier_current_pose_context_state",
        name: "AtelierStore::current_pose_context_state",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["context_id", "state_seq", "workspace_ref", "kind"],
        cli_flag: None,
        description: "Read the newest (head) pose context state for a workspace from the append-only context history.",
        expected_input: "A workspace_ref.",
        expected_output: "The latest PoseContextState, or None when the workspace has no context history yet.",
        common_errors: &["workspace has no pose context history"],
        recovery_steps: &["Set an initial pose context state with set_pose_context_state"],
    },
    CommandReference {
        id: "atelier_ingest_pose_rig",
        name: "AtelierStore::ingest_pose_rig",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["rig_id", "character_internal_id", "source_ref", "content_hash", "keypoints_json", "detector_status"],
        cli_flag: None,
        description: "Store a detected/authored OpenPose rig (body-18/face-70/hand-21 keypoint arrays) a Workflow-Engine detection job writes through; idempotent per (character, source_media, content_hash). No detector runs here.",
        expected_input: "A NewPoseRig with canvas geometry, the OpenPose keypoints_json payload, the detector descriptor, and DetectorStatus (detected|fallback|failed).",
        expected_output: "A PoseRig row; keypoint payload shape is structurally validated (pose_keypoints_2d=18 triples; face/hands zero-filled when absent).",
        common_errors: &[
            "pose_keypoints_2d does not have 18 triples",
            "error_reason missing when detector_status is fallback or failed",
        ],
        recovery_steps: &[
            "Fix the keypoint array cardinalities (body 18, face 70, hand 21 triples)",
            "Provide an error_reason for fallback/failed detector status",
        ],
    },
    CommandReference {
        id: "atelier_list_pose_rigs",
        name: "AtelierStore::list_pose_rigs",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["character_internal_id", "rig_id", "detector_status", "created_at_utc"],
        cli_flag: None,
        description: "List pose rigs for a character (rig CRUD navigation).",
        expected_input: "A character_internal_id.",
        expected_output: "PoseRig rows for the character.",
        common_errors: &["character has no rigs"],
        recovery_steps: &["Ingest a pose rig with ingest_pose_rig first"],
    },
    CommandReference {
        id: "atelier_get_pose_rig",
        name: "AtelierStore::get_pose_rig",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["rig_id", "canvas", "keypoints_json", "sidecar_ref"],
        cli_flag: None,
        description: "Read one pose rig by id, including its OpenPose keypoint arrays and deterministic sidecar ref (keypoint schema inspect).",
        expected_input: "A rig_id.",
        expected_output: "The PoseRig, or a NotFound error.",
        common_errors: &["unknown rig_id"],
        recovery_steps: &["List rigs for the character to find a valid rig_id"],
    },
    CommandReference {
        id: "atelier_set_pose_calibration",
        name: "AtelierStore::set_pose_calibration",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["rig_id", "state", "block_reason", "marker_visibility", "hand_rows"],
        cli_flag: None,
        description: "Set the full typed calibration record for a rig. The spec Calibration Panel (10.10.4.1.9) is not yet implementable, so calibration is preserved as an explicit unresolved/BLOCKED record with a block_reason rather than faking calibrated values.",
        expected_input: "A NewPoseCalibration; unresolved state requires a non-empty block_reason and resolved state must not carry one.",
        expected_output: "A Calibration row preserving marker visibility/colors, typed hand rows (marker_count=21), and history refs without flattening to a status string.",
        common_errors: &[
            "unresolved calibration missing a block_reason",
            "hand row marker_count is not 21",
        ],
        recovery_steps: &[
            "Record a block_reason for the BLOCKED calibration state",
            "Use exactly 21 markers per hand row",
        ],
    },
    CommandReference {
        id: "atelier_get_calibration",
        name: "AtelierStore::get_calibration",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["rig_id", "state", "block_reason"],
        cli_flag: None,
        description: "Read the calibration record for a rig (capability/calibration state check).",
        expected_input: "A rig_id.",
        expected_output: "The Calibration record, or None when calibration was never set.",
        common_errors: &["rig has no calibration record"],
        recovery_steps: &["Set a (typically BLOCKED) calibration with set_pose_calibration"],
    },
    // MT-123: OpenPose sidecars + identity profiles.
    CommandReference {
        id: "atelier_record_pose_sidecar",
        name: "AtelierStore::record_pose_sidecar",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["sidecar_id", "rig_id", "kind", "artifact_ref", "manifest_ref", "content_hash"],
        cli_flag: None,
        description: "Register a typed pose sidecar artifact (openpose_json | openpose_png | conditioning_png) emitted by an out-of-module pose/OpenPose job into the ArtifactStore.",
        expected_input: "A NewPoseSidecar with a PoseSidecarKind, an artifact:// ref + matching artifact.json manifest_ref, content_hash, byte_len, mime, and PoseSidecarStatus.",
        expected_output: "A PoseSidecar row; mime must match the kind (application/json for openpose_json, image/png for png kinds).",
        common_errors: &[
            "mime does not match the sidecar kind",
            "manifest_ref does not point at the artifact_ref's artifact.json",
        ],
        recovery_steps: &[
            "Use the kind's expected mime",
            "Derive manifest_ref by replacing /payload with /artifact.json on the artifact_ref",
        ],
    },
    CommandReference {
        id: "atelier_list_pose_sidecars",
        name: "AtelierStore::list_pose_sidecars",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["rig_id", "sidecar_id", "kind", "artifact_ref"],
        cli_flag: None,
        description: "List the typed sidecar artifacts (export JSON/PNG, conditioning) linked to a rig.",
        expected_input: "A rig_id.",
        expected_output: "PoseSidecar rows for the rig.",
        common_errors: &["rig has no sidecars"],
        recovery_steps: &["Record a sidecar with record_pose_sidecar first"],
    },
    CommandReference {
        id: "atelier_pose_sidecar_gallery_projection",
        name: "AtelierStore::pose_sidecar_gallery_projection",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["sidecar_id", "rig_id", "gallery_visible", "hidden_reason", "jump_target"],
        cli_flag: None,
        description: "Project pose sidecars for discovery while keeping them hidden from normal media galleries; each row carries gallery_visible=false, a hidden_reason, and a jump_target back to the rig.",
        expected_input: "A rig_id.",
        expected_output: "PoseSidecarGalleryProjection rows marked hidden-from-gallery with a jump target.",
        common_errors: &["rig has no sidecars to project"],
        recovery_steps: &["Record sidecars before requesting the gallery projection"],
    },
    CommandReference {
        id: "atelier_append_identity_profile",
        name: "AtelierStore::append_identity_profile",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["profile_id", "character_internal_id", "seq", "version", "kind", "provenance"],
        cli_flag: None,
        description: "Append a versioned face/reference identity profile for a character (append-only per character); free-form provenance/lineage is redacted of secret material before storage.",
        expected_input: "A NewIdentityProfile with an IdentityProfileKind, name, reference_ref, and provenance.",
        expected_output: "An IdentityProfile row with an incremented seq; raw cookies/tokens/auth are never persisted.",
        common_errors: &[
            "empty name",
            "empty reference_ref",
        ],
        recovery_steps: &[
            "Provide a non-empty name and reference_ref",
            "Read latest_identity_profile to inspect the current head version",
        ],
    },
    CommandReference {
        id: "atelier_update_identity_profile",
        name: "AtelierStore::update_identity_profile",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["profile_id", "name", "description", "crop_ref", "artifact_ref"],
        cli_flag: None,
        description: "Update the mutable fields (name/description/source/crop/artifact refs) of an identity profile without changing its append sequence.",
        expected_input: "An UpdateIdentityProfile naming the profile_id and the new mutable fields.",
        expected_output: "The updated IdentityProfile row.",
        common_errors: &[
            "unknown profile_id",
            "padded or non-portable optional ref",
        ],
        recovery_steps: &[
            "List identity profiles to find a valid profile_id",
            "Use unpadded portable refs",
        ],
    },
    CommandReference {
        id: "atelier_record_identity_crop_artifact",
        name: "AtelierStore::record_identity_crop_artifact",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["crop_id", "profile_id", "profile_version", "crop_box", "artifact_ref", "manifest_ref"],
        cli_flag: None,
        description: "Record a normalized 512x512 identity crop artifact (with crop_box + landmarks) bound to the current profile version; the profile version is read from PostgreSQL and pinned on the crop record.",
        expected_input: "A NewIdentityCropArtifact with a crop_box, landmarks inside the 512x512 crop, an artifact:// ref + matching manifest_ref, content_hash, and image/png mime.",
        expected_output: "An IdentityCropArtifact (manifest schema hsk.atelier.identity_crop_artifact_manifest@1) pinned to the profile version at capture.",
        common_errors: &[
            "crop artifact is not exactly 512x512",
            "landmark coordinates fall outside the 512x512 crop",
        ],
        recovery_steps: &[
            "Produce a 512x512 image/png artifact",
            "Clamp landmarks inside the crop and set confidence in 0..=1",
        ],
    },
    CommandReference {
        id: "atelier_latest_identity_profile",
        name: "AtelierStore::latest_identity_profile",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["character_internal_id", "profile_id", "seq", "version"],
        cli_flag: None,
        description: "Read the newest identity profile (head of the append-only lineage) for a character.",
        expected_input: "A character_internal_id.",
        expected_output: "The latest IdentityProfile, or None when the character has no profiles.",
        common_errors: &["character has no identity profiles"],
        recovery_steps: &["Append an identity profile with append_identity_profile first"],
    },
    // MT-124: ComfyUI workflow receipts, replay, history, and failure recovery.
    CommandReference {
        id: "atelier_record_comfy_workflow_receipt",
        name: "AtelierStore::record_comfy_workflow_receipt",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["receipt_id", "system_id", "workflow_run_id", "workflow_json_ref", "prompt_ref", "status"],
        cli_flag: None,
        description: "Record the durable workflow-level ComfyUI receipt (schema hsk.atelier.comfy.workflow_receipt@1) with spec/json/prompt refs and run status; all refs are by-reference, secrets are never persisted.",
        expected_input: "A NewComfyWorkflowReceipt with a system_id, workflow_run_id, workflow_spec/json/prompt refs, status, and (for failed runs) an error_ref.",
        expected_output: "A ComfyWorkflowReceipt row; failed status requires a non-empty error_ref.",
        common_errors: &[
            "failed workflow receipt missing error_ref",
            "localhost/runtime ref rejected in a stored ref field",
        ],
        recovery_steps: &[
            "Attach an error_ref for failed runs",
            "Use ArtifactStore/source refs, never localhost runtime endpoints",
        ],
    },
    CommandReference {
        id: "atelier_get_comfy_workflow_receipt",
        name: "AtelierStore::get_comfy_workflow_receipt",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["receipt_id", "workflow_run_id", "status", "outputs"],
        cli_flag: None,
        description: "Read one ComfyUI workflow receipt by id for replay/inspection of exactly what a run recorded.",
        expected_input: "A receipt_id.",
        expected_output: "The ComfyWorkflowReceipt, or None when unknown.",
        common_errors: &["unknown receipt_id"],
        recovery_steps: &["List comfy workflow history to find a valid receipt_id"],
    },
    CommandReference {
        id: "atelier_list_comfy_workflow_history",
        name: "AtelierStore::list_comfy_workflow_history",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["system_id", "character_ref", "status", "receipt_id", "created_at_utc"],
        cli_flag: None,
        description: "List ComfyUI workflow receipts over a filter contract (system_id, character_ref, status) for workflow history replay, ordered oldest to newest.",
        expected_input: "A ComfyWorkflowHistoryQuery with optional system_id/character_ref/status filters.",
        expected_output: "ComfyWorkflowReceipt rows matching the filters in deterministic order.",
        common_errors: &["invalid filter token in the history query"],
        recovery_steps: &["Use a supported status/system_id filter token"],
    },
    CommandReference {
        id: "atelier_produce_intake_receipt",
        name: "AtelierStore::produce_intake_receipt",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["workflow_run_id", "probe_outcome", "registered_output_count", "materialized_artifact_refs", "fallback_engaged"],
        cli_flag: None,
        description: "Produce the per-job ComfyIntakeReceiptV1 (Section 6.9.5) summarizing probe, registration, materialized outputs, and fallback state so a no-context model can reconstruct exactly what intake produced. By-reference only; no bytes or credentials.",
        expected_input: "A workflow_run_id.",
        expected_output: "An IntakeReceipt with probe outcome, registered output count, materialized artifact refs, and fallback_engaged.",
        common_errors: &["no intake evidence recorded for the run yet"],
        recovery_steps: &["Record probe/registration/outputs for the run before summarizing"],
    },
    CommandReference {
        id: "atelier_mark_saveimage_fallback",
        name: "AtelierStore::mark_saveimage_fallback",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["workflow_run_id", "fallback_reason"],
        cli_flag: None,
        description: "Mark that intake degraded to the SaveImage scan fallback (Section 6.9.6) for a run; idempotent per workflow_run_id and records the fallback_reason so the operator sees why the bridge path was not taken.",
        expected_input: "A workflow_run_id and a non-empty fallback_reason.",
        expected_output: "A recorded fallback marker; replaying the same run updates the reason.",
        common_errors: &["empty fallback_reason"],
        recovery_steps: &["Provide a concrete non-empty fallback_reason"],
    },
    CommandReference {
        id: "atelier_record_comfy_output_registration_failure",
        name: "AtelierStore::record_comfy_output_registration_failure",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["failure_id", "workflow_run_id", "artifact_ref", "content_hash", "failure_stage", "status"],
        cli_flag: None,
        description: "Preserve a generated output whose intake registration failed after save (output-first persistence), idempotent on (workflow_run_id, content_hash, failure_stage); provenance evidence is scrubbed of secrets on the way in.",
        expected_input: "A NewComfyOutputRegistrationFailure with the artifact/manifest/content refs, routing intent, failure_stage, failure_reason, and evidence.",
        expected_output: "A ComfyOutputRegistrationFailure row (status retryable) so a saved output is never lost to a registration error.",
        common_errors: &[
            "empty node/output-slot/mime fields",
            "registration-failure shape validation failed",
        ],
        recovery_steps: &[
            "Populate the node, output slot, and mime fields",
            "Retry the preserved output with retry_comfy_output_registration_failure",
        ],
    },
    CommandReference {
        id: "atelier_retry_comfy_output_registration_failure",
        name: "AtelierStore::retry_comfy_output_registration_failure",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["failure_id", "registration_id", "resolved_intake_output_id", "retry_count"],
        cli_flag: None,
        description: "Retry a preserved registration-failure into a normal intake output, reusing the saved artifact (output-first recovery); flips the failure row to registered and links the resolved intake output.",
        expected_input: "A failure_id and an optional registration_id.",
        expected_output: "A RecordOutputOutcome for the recovered output; the failure row is marked registered with an incremented retry_count.",
        common_errors: &[
            "unknown failure_id",
            "failure is not retryable",
        ],
        recovery_steps: &[
            "Record the registration failure first",
            "Only retry rows in the retryable status",
        ],
    },
    // MT-125: deferred PoseKit + workflow adapter boundaries.
    CommandReference {
        id: "atelier_register_bridge_capability",
        name: "AtelierStore::register_bridge_capability",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["registration_id", "workflow_run_id", "accepted_outputs", "rejected_outputs", "capability_grant_ref"],
        cli_flag: None,
        description: "Register the ComfyUI bridge capability for a job (Section 6.9.3), idempotent per workflow_run_id. Declared outputs that pass the engine.comfyui capability gate are routable; every gate-rejected output becomes a typed reject row that is never routed.",
        expected_input: "A NewCapabilityRegistration with accepted/rejected declared outputs and a capability_grant_ref naming an engine.comfyui-granted profile.",
        expected_output: "A CapabilityRegistration with the routable accepted set; rejected outputs emit CAPABILITY_REJECTED and are not routed.",
        common_errors: &[
            "capability_grant_ref profile is not granted engine.comfyui",
            "capability_grant_ref missing profile/evidence",
        ],
        recovery_steps: &[
            "Grant the profile the engine.comfyui capability",
            "Inspect rejected outputs with list_capability_rejects",
        ],
    },
    CommandReference {
        id: "atelier_list_capability_rejects",
        name: "AtelierStore::list_capability_rejects",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["workflow_run_id", "output_slot", "reason"],
        cli_flag: None,
        description: "List the capability-gate-rejected declared outputs for a run (adapter boundary evidence); rejected outputs are recorded as typed rows and are never routed into the ArtifactStore.",
        expected_input: "A workflow_run_id.",
        expected_output: "Capability-reject rows naming the output slot and rejection reason.",
        common_errors: &["no capability registration recorded for the run"],
        recovery_steps: &["Register the bridge capability before listing rejects"],
    },
    CommandReference {
        id: "atelier_record_url_image_import",
        name: "AtelierStore::record_url_image_import",
        status: CommandStatus::Wired,
        ipc_channel: Some("/atelier/image-import/url"),
        tauri_command: None,
        schema_fields: &["idempotency_key", "source_url", "capability_profile_id", "capability_grant_ref", "import_id"],
        cli_flag: None,
        description: "Record a governed URL image-sourcing import through the wired Axum atelier route; the import is capability-gated (profile + grant ref) and is the image-sourcing-lane boundary for the pose/ComfyUI pipeline.",
        expected_input: "POST /atelier/image-import/url with idempotency_key, source_url, capability_profile_id, capability_grant_ref, and the x-hsk-actor-id header.",
        expected_output: "An ImageImportRecord addressable by import_id; replaying the idempotency_key resolves to the existing import.",
        common_errors: &[
            "missing x-hsk-actor-id actor header",
            "capability profile not granted the import capability",
        ],
        recovery_steps: &[
            "Send the x-hsk-actor-id header",
            "Use a capability_profile_id/grant_ref granted the image-sourcing capability",
        ],
    },
    CommandReference {
        id: "atelier_set_calibration_blocked",
        name: "AtelierStore::set_calibration",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["rig_id", "state", "block_reason"],
        cli_flag: None,
        description: "Convenience boundary that records a rig's calibration as unresolved/BLOCKED with a block_reason (carry-forward deferral). The spec Calibration Panel (10.10.4.1.9) and full PoseKit activation (WP-0133) are not yet implementable, so this preserves the deferral instead of faking calibrated values.",
        expected_input: "A rig_id, a CalibrationState (typically Unresolved), and a block_reason.",
        expected_output: "A Calibration row recording the BLOCKED/carry-forward state; values are never fabricated.",
        common_errors: &[
            "unknown rig_id",
            "unresolved calibration missing a block_reason",
        ],
        recovery_steps: &[
            "Ingest the rig before setting calibration",
            "Always record a block_reason for the deferred BLOCKED state",
        ],
    },
    // ---- WP-KERNEL-005 Model-Workflow-Diagnostics command references ----
    // MT-132: source/evidence + status matrix.
    CommandReference {
        id: "diagnostics_source_evidence_matrix",
        name: "DiagnosticsSourceEvidenceMatrix",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["surface_id", "source_kind", "evidence_pointer", "command_status", "blocker"],
        cli_flag: None,
        description: "Build the source/evidence + status matrix mapping each automation manual surface (command map, sessions, leases, visual capture, local LLM, AI tagging, build/release diagnostics) to its real backend source and Wired-vs-Planned status. No IPC route exists yet; the matrix is assembled from the typed ModelManual command_reference plus the kernel.action_catalog.view catalog.",
        expected_input: "The ModelManual command_reference set and the registered KernelActionCatalogV1 actions.",
        expected_output: "A matrix row per surface with source_kind, evidence_pointer, command_status, and a blocker flag for any surface lacking a resolvable source.",
        common_errors: &[
            "surface has no resolvable backend source",
            "command marked Wired without an ipc_channel or registered action",
        ],
        recovery_steps: &[
            "Resolve the surface to a real command, route, or catalog action before claiming Wired",
            "Mark unresolved surfaces as blockers rather than fabricating evidence",
        ],
    },
    CommandReference {
        id: "kernel_action_catalog_view",
        name: "kernel.action_catalog.view",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.action_catalog.view"),
        tauri_command: None,
        schema_fields: &["action_id", "authority_effect", "approval_posture", "role_eligibility", "capability_requirements"],
        cli_flag: None,
        description: "Registered KernelActionCatalogV1 projection action (src/kernel/action_catalog.rs catalog_view_action) that lists the model-facing command map: every registered action_id, its authority effect, approval posture, and write-box effects. ProjectionOnly, NoApprovalRequired.",
        expected_input: "An action_catalog_view input envelope (hsk.kernel.action_catalog_view_input@1).",
        expected_output: "A read-only projection (hsk.kernel.action_catalog_view_result@1) of registered actions with authority_effect and approval_posture.",
        common_errors: &[
            "caller lacks the kernel.catalog.read capability",
            "catalog schema not present",
        ],
        recovery_steps: &[
            "Grant the kernel.catalog.read capability",
            "Run the catalog_schema_present validation hook",
        ],
    },
    CommandReference {
        id: "diagnostics_problem_store_query",
        name: "diagnostics::DiagnosticsStore",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["fingerprint", "severity", "source", "surface", "status", "count"],
        cli_flag: None,
        description: "Query the deterministic diagnostics problem store (src/diagnostics/mod.rs DiagnosticsStore trait: record_diagnostic, list_problems, get_diagnostic, list_diagnostics) for fingerprinted, aggregated diagnostics. The trait is real but no IPC route is registered yet, so this is Planned.",
        expected_input: "A DiagFilter (severity, source, surface, wsid, job_id, time range, fingerprint, limit).",
        expected_output: "ProblemGroup rows with a stable DIAG-SCHEMA-003 fingerprint, count, and first/last seen timestamps.",
        common_errors: &[
            "invalid diagnostic source token",
            "storage backend unavailable",
        ],
        recovery_steps: &[
            "Use a supported DiagnosticSource value",
            "Confirm the DiagnosticsStore implementation is bound to PostgreSQL storage",
        ],
    },
    // MT-135: no-context manual structure.
    CommandReference {
        id: "kernel_model_manual_update_section",
        name: "kernel.model_manual.update_section",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.model_manual.update_section"),
        tauri_command: None,
        schema_fields: &["manual_section_id", "before_text_hash", "after_text_hash", "validation_state"],
        cli_flag: None,
        description: "Registered KernelActionCatalogV1 action (src/kernel/action_catalog.rs model_manual_update_section_action) for proposing a ModelManual section edit as pre-promotion write-box evidence. Edits route through the editable-surface guard and promotion gate; this is the governed write path for the no-context manual structure read through kernel.model_manual.get.",
        expected_input: "A model_manual section update envelope (hsk.kernel.model_manual_section_update_input@1).",
        expected_output: "A ModelManualSectionBox write-box (hsk.write_box.model_manual_section@1) pending promotion.",
        common_errors: &[
            "editable surface guard rejected the section",
            "before_text_hash does not match current content",
        ],
        recovery_steps: &[
            "Edit only catalog-declared editable manual sections",
            "Re-read the current section through kernel.model_manual.get before proposing",
        ],
    },
    // MT-159: GUI verification checklist surfaces.
    CommandReference {
        id: "kernel_product_screenshot_capture_execute",
        name: "kernel.product_screenshot_capture.execute",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.product_screenshot_capture.execute"),
        tauri_command: None,
        schema_fields: &["request_id", "scope", "screenshot_artifact_ref", "execution_proof", "stored_in_artifact_system"],
        cli_flag: None,
        description: "Registered KernelActionCatalogV1 action (src/kernel/action_catalog.rs, product_screenshot_capture execute action) that captures a product screenshot through a browser adapter for GUI verification (readability, no-overlap layout, visible state) without stealing operator foreground. Backed by execute_product_screenshot_capture in src/kernel/product_screenshot_capture.rs.",
        expected_input: "A ProductScreenshotRequestV1 with a capture scope.",
        expected_output: "A ProductScreenshotExecutionResultV1 with a screenshot artifact ref and durable execution proof stored in the artifact system.",
        common_errors: &[
            "browser adapter unavailable",
            "capture requested a foreground window",
        ],
        recovery_steps: &[
            "Use the headless browser adapter capture path",
            "Confirm the screenshot artifact ref was stored before asserting GUI state",
        ],
    },
    CommandReference {
        id: "kernel_visual_debugging_loop_project",
        name: "kernel.visual_debugging_loop.project",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.visual_debugging_loop.project"),
        tauri_command: None,
        schema_fields: &["loop_id", "trigger_kinds", "visual_diff_artifact_refs", "threshold_exceeded_evidence_ids", "validator_steer_required"],
        cli_flag: None,
        description: "Registered KernelActionCatalogV1 projection action (src/kernel/action_catalog.rs) over the VisualDebuggingLoopV1 (src/kernel/visual_debugging_loop.rs): post-commit/post-action visual triggers, pixel/structural diff thresholds, evidence artifacts, and validator steering when a threshold is exceeded.",
        expected_input: "A visual_debugging_loop projection envelope.",
        expected_output: "A VisualDebuggingLoopProjectionV1 with trigger kinds, visual diff artifact refs, threshold-exceeded evidence ids, and validator_steer_required.",
        common_errors: &[
            "no baseline screenshot recorded for the trigger",
            "threshold config refs are missing",
        ],
        recovery_steps: &[
            "Capture a baseline before comparing",
            "Attach a VisualDebuggingThresholdConfigV1 before projecting",
        ],
    },
    // MT-164: build rules read/acknowledge evidence.
    CommandReference {
        id: "diagnostics_hbr_handoff_gate_evaluate",
        name: "hbr::handoff_gate::HandoffGate",
        status: CommandStatus::Planned,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["hbr_id", "evidence_kind", "failing_rules", "handoff_block", "gate_uuid"],
        cli_flag: None,
        description: "Evaluate the typed HBR handoff gate (src/hbr/handoff_gate.rs HandoffGate) that a model reads and acknowledges before build/test/release handoff: each HandoffRule's required evidence kinds are checked and any unmet rule produces a HandoffBlock. No IPC route is registered yet, so this is Planned; the real method is HandoffGate::new + evaluate.",
        expected_input: "An HbrPacket with an HbrAcceptanceMatrix and the recorded HandoffEvidence.",
        expected_output: "A HandoffBlock listing failing rules (with KTR/SR receipt refs) or a clear gate when all build-rule evidence resolves.",
        common_errors: &[
            "required evidence kind missing for an HBR rule",
            "evidence recorded under the wrong evidence_kind",
        ],
        recovery_steps: &[
            "Read the failing HandoffRule evidence kinds and attach matching evidence",
            "Re-run hbr_matrix_check and hbr_validator_scan after recording evidence",
        ],
    },
    // MT-181: integration smoke path surfaces.
    CommandReference {
        id: "kernel_role_mailbox_claim_lease_project",
        name: "kernel.role_mailbox_claim_lease.project",
        status: CommandStatus::Wired,
        ipc_channel: Some("kernel.role_mailbox_claim_lease.project"),
        tauri_command: None,
        schema_fields: &["thread_id", "claim_id", "lease_state", "lease_expires_at", "takeover_legality"],
        cli_flag: None,
        description: "Registered KernelActionCatalogV1 projection action (src/kernel/action_catalog.rs) over RoleMailboxClaimLeaseV1 (src/kernel/role_mailbox_claim_lease.rs): the lease claim/takeover seam a model uses at the start of the integration smoke path to claim work before reading session state.",
        expected_input: "A role_mailbox_claim_lease projection envelope for a thread.",
        expected_output: "A RoleMailboxClaimLeaseProjectionV1 with current claimant, lease state, expiry, and takeover legality.",
        common_errors: &[
            "lease already held by another claimant",
            "lease expired mid-operation",
        ],
        recovery_steps: &[
            "Check takeover_legality before claiming an expired lease",
            "Re-claim and re-read session state if the lease expired",
        ],
    },
    CommandReference {
        id: "diagnostics_debug_bundle_export",
        name: "AtelierStore::record_kernel_diagnostic_bundle_manifest",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["manifest_id", "subject_kind", "subject_ref", "failure_summary", "error_taxonomy", "severity", "sections", "reproduction_steps", "isolation_hints"],
        cli_flag: None,
        description: "Executable debug-bundle export at the end of the integration smoke path (src/diagnostics/bundle_manifest.rs AtelierStore::record_kernel_diagnostic_bundle_manifest): persists a kernel diagnostic bundle manifest -- subject, failure summary, error taxonomy, severity, typed evidence sections, reproduction steps, isolation hints -- into kernel_diagnostic_bundle_manifest (migration 0120) and mirrors it through the kernel.diagnostics.bundle_manifest_recorded EventLedger family, re-readable by manifest id or subject for no-context handoff. Backend library surface; no IPC route.",
        expected_input: "A NewDiagnosticBundleManifest with a portable subject_ref, at least one evidence section, and at least one reproduction step.",
        expected_output: "A persisted DiagnosticBundleManifest (manifest_id) plus its bundle_manifest_recorded EventLedger event.",
        common_errors: &[
            "subject_ref or section content cites a non-portable .GOV / SQLite / local-path ref",
            "manifest has no evidence sections or no reproduction steps",
        ],
        recovery_steps: &[
            "Replace non-portable refs with governed artifact refs",
            "Add at least one evidence section and one reproduction step before exporting",
        ],
    },
    // MT-183: red-team manual + visual drift guards.
    CommandReference {
        id: "diagnostics_manual_drift_guard",
        name: "DiagnosticsManualDriftGuard",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["manual_version", "wired_surface_diff", "missing_manual_row", "drift_kind"],
        cli_flag: None,
        description: "Executable negative drift guard (src/atelier/model_manual_merge.rs run_manual_drift_guard + AtelierStore::record_manual_drift_guard_run): cross-checks that every Wired ModelManual command resolves to a registered kernel action-catalog action or registered IPC/Tauri route, flags missing manual rows, orphan manual rows, and id-normalization collisions, and detects a wired-surface diff without a MANUAL_VERSION bump across persisted guard runs (HBR-MAN-001). Runs persist to atelier_model_manual_drift_guard and mirror through the atelier.model_manual.drift_guard_recorded EventLedger family. Backend library surface; no IPC route.",
        expected_input: "The ModelManual command_reference set, the registered action catalog, and the current MANUAL_VERSION.",
        expected_output: "Drift findings: any Wired command with no resolvable surface, any orphan manual row, or a wired-surface diff without a MANUAL_VERSION bump.",
        common_errors: &[
            "Wired command references a surface that is not registered",
            "MANUAL_VERSION not bumped after a wired-surface diff",
        ],
        recovery_steps: &[
            "Demote unresolved commands to Planned or register the surface",
            "Bump MANUAL_VERSION in the same commit as the wired-surface diff",
        ],
    },
    // MT-185: Core/Data manual source merge.
    CommandReference {
        id: "diagnostics_core_row_merge",
        name: "DiagnosticsCoreRowMerge",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["source_mt", "normalized_command_id", "merged", "missing_blocker"],
        cli_flag: None,
        description: "Executable merge (src/atelier/model_manual_merge.rs merge_manual_source_rows with ManualMergeSourceKind::CoreData) that pulls the atelier Core/Data manual source rows (MT-052..MT-060, MT-073..MT-075 feature groups and commands) into the Diagnostics manual dataset by normalized id. The merge normalizes ids, detects normalization collisions, and marks any missing Core/Data feature group or source row as a blocker rather than fabricating it. Merge runs persist via AtelierStore::record_manual_row_merge to atelier_model_manual_row_merge and mirror through the atelier.model_manual.row_merge_recorded EventLedger family. Backend library surface; no IPC route.",
        expected_input: "The atelier Core/Data feature groups and command references.",
        expected_output: "Normalized merged rows keyed by command id, with a blocker entry for any expected Core/Data row that is absent.",
        common_errors: &[
            "expected Core/Data source row missing",
            "id normalization collision",
        ],
        recovery_steps: &[
            "Mark the missing Core/Data row as a blocker, do not invent it",
            "Resolve id collisions before merging",
        ],
    },
    // MT-186: Pose/ComfyUI manual source merge.
    CommandReference {
        id: "diagnostics_pose_row_merge",
        name: "DiagnosticsPoseRowMerge",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["source_mt", "normalized_command_id", "merged", "missing_blocker"],
        cli_flag: None,
        description: "Executable merge (src/atelier/model_manual_merge.rs merge_manual_source_rows with ManualMergeSourceKind::PoseComfy) that pulls the Pose/ComfyUI manual source rows (MT-122..MT-125 feature groups and commands) into the Diagnostics manual dataset by normalized id. The merge normalizes ids, detects normalization collisions, and marks any missing Pose/ComfyUI feature group or source row as a blocker rather than fabricating it. Merge runs persist via AtelierStore::record_manual_row_merge to atelier_model_manual_row_merge and mirror through the atelier.model_manual.row_merge_recorded EventLedger family. Backend library surface; no IPC route.",
        expected_input: "The Pose/ComfyUI feature groups and command references.",
        expected_output: "Normalized merged rows keyed by command id, with a blocker entry for any expected Pose/ComfyUI row that is absent.",
        common_errors: &[
            "expected Pose/ComfyUI source row missing",
            "id normalization collision",
        ],
        recovery_steps: &[
            "Mark the missing Pose/ComfyUI row as a blocker, do not invent it",
            "Resolve id collisions before merging",
        ],
    },
    // MT-187: Diagnostics-owned manual source merge.
    CommandReference {
        id: "diagnostics_owned_row_merge",
        name: "DiagnosticsOwnedRowMerge",
        status: CommandStatus::Wired,
        ipc_channel: None,
        tauri_command: None,
        schema_fields: &["row_kind", "normalized_command_id", "merged", "missing_blocker"],
        cli_flag: None,
        description: "Executable merge (src/atelier/model_manual_merge.rs merge_manual_source_rows with ManualMergeSourceKind::DiagnosticsOwned) for the Diagnostics-owned manual/action/state/error/bundle rows (the manual structure reads, action-catalog command map, inspector state reads, diagnostics problem store, and debug bundle export) into the manual dataset by normalized id, classifying each merged row under a supported row_kind and marking any missing Diagnostics-owned row as a blocker rather than fabricating it. Merge runs persist via AtelierStore::record_manual_row_merge to atelier_model_manual_row_merge and mirror through the atelier.model_manual.row_merge_recorded EventLedger family. Backend library surface; no IPC route.",
        expected_input: "The Diagnostics-owned feature groups and command references (problem store, catalog view, inspector reads, bundle export).",
        expected_output: "Normalized merged rows keyed by command id across manual/action/state/error/bundle kinds, with a blocker entry for any absent expected row.",
        common_errors: &[
            "expected Diagnostics-owned row missing",
            "row_kind not one of manual/action/state/error/bundle",
        ],
        recovery_steps: &[
            "Mark the missing Diagnostics-owned row as a blocker, do not invent it",
            "Classify each row under a supported row_kind",
        ],
    },
];

pub static SAFETY_CONSTRAINTS: &[ManualSafetyConstraint] = &[
    ManualSafetyConstraint {
        id: "manual_same_commit_currency",
        constraint_text: "HBR-MAN-001 requires every wired surface diff to update ModelManual content and bump MANUAL_VERSION in the same commit.",
        enforcement_point: "hbr-man-001 paired-diff check",
    },
    ManualSafetyConstraint {
        id: "manual_no_context_operation",
        constraint_text: "HBR-MAN-002 requires a no-context model to operate core workflows from ModelManual alone without chat history.",
        enforcement_point: "manual no-context harness",
    },
    ManualSafetyConstraint {
        id: "quiet_model_operation",
        constraint_text: "HBR-QUIET rules require diagnostics, GUI testing, and model-driven interaction to stay non-intrusive and observable.",
        enforcement_point: "visual debugging and process-ledger checks",
    },
    ManualSafetyConstraint {
        id: "typed_hbr_receipts",
        constraint_text: "HBR violations route through typed HBR_VIOLATION receipts; notes may add context but never carry routing authority.",
        enforcement_point: "hbr-violation.schema.json",
    },
];

pub static WORKFLOWS: &[ManualWorkflow] = &[
    ManualWorkflow {
        id: "startup",
        title: "Startup",
        prerequisites: &[
            "Open the active worktree",
            "Read local AGENTS.md and packet authority",
            "Confirm the active microtask claim",
        ],
        steps: &[
            "Read the ModelManual manifest through kernel.model_manual.get when the IPC bridge is available.",
            "List command references and identify which feature group owns the task area.",
            "Run the required proof commands from the active microtask before claiming completion.",
        ],
        expected_outcome: "The model knows the command surface, safety constraints, and verification path without prior chat context.",
        failure_modes: &[
            "Manual IPC bridge not registered",
            "Command reference is planned and not yet callable",
        ],
    },
    ManualWorkflow {
        id: "governed_session_run",
        title: "Basic Governed Session Run",
        prerequisites: &[
            "Active work packet",
            "Claimed microtask",
            "Known proof commands",
        ],
        steps: &[
            "Use the feature group to find command references relevant to the microtask.",
            "Execute only wired commands unless a planned entry is explicitly being implemented.",
            "Emit typed receipts or events for HBR failures and rerun verification after repair.",
        ],
        expected_outcome: "The governed session completes with traceable evidence and no stale manual assumptions.",
        failure_modes: &[
            "HBR matrix row remains PENDING",
            "Evidence pointer cannot be resolved",
            "MANUAL_VERSION not bumped after a wired-surface diff",
        ],
    },
    ManualWorkflow {
        id: "diagnostics_panel_triage",
        title: "Diagnostics Panel Triage",
        prerequisites: &[
            "Diagnostics capture surface available",
            "Target app route or backend state selector",
        ],
        steps: &[
            "Capture a headless visual or backend snapshot without foreground focus.",
            "Inspect stable element ids, logs, traces, and process ledger refs.",
            "Attach screenshot, event, artifact, or receipt evidence to the relevant HBR row.",
        ],
        expected_outcome: "A no-context model can isolate UI or runtime failures without screen-scraping guesses.",
        failure_modes: &[
            "Capture attempts to open a foreground window",
            "Snapshot lacks stable identifiers",
            "Process ledger START/STOP evidence is missing",
        ],
    },
    // ---- WP-KERNEL-005 atelier (Core-Data) workflows (MT-052..MT-060, MT-073..MT-075) ----
    ManualWorkflow {
        id: "atelier_character_identity_and_sheet",
        title: "Atelier Character Identity And Sheet Editing (MT-052)",
        prerequisites: &["Atelier PostgreSQL store reachable"],
        steps: &[
            "Create a character with create_character; use its public_id for operator-facing references.",
            "Append an initial sheet version with append_sheet_version.",
            "Apply edits with apply_sheet_field_edits, which appends a new immutable version.",
            "Read sheet_version_history to confirm prior versions were preserved.",
        ],
        expected_outcome: "A character exists with a stable public_id and an append-only sheet history where no edit loses prior text.",
        failure_modes: &["duplicate public_id", "stale parent version on edit"],
    },
    ManualWorkflow {
        id: "atelier_media_library_and_intake_review",
        title: "Atelier Media Library And Intake Review (MT-053)",
        prerequisites: &["ArtifactStore configured", "Atelier route server running for intake endpoints"],
        steps: &[
            "Materialize media into the DAM with materialize_media_asset (content-hash dedup).",
            "Open an intake batch through POST /atelier/intake/batches.",
            "List lane counts and items through GET /atelier/intake/batches/:batch_id/items.",
            "Classify items with apply_intake_classification and bulk-edit review metadata with bulk_update_media_review_metadata.",
        ],
        expected_outcome: "Imported media is deduped and triaged through intake lanes with durable review metadata.",
        failure_modes: &["non-artifact:// ref", "invalid intake mode", "unknown batch_id"],
    },
    ManualWorkflow {
        id: "atelier_collections_and_contact_sheets",
        title: "Atelier Collections And Contact Sheets (MT-054)",
        prerequisites: &["Media assets materialized in the DAM"],
        steps: &[
            "Create a collection with create_collection.",
            "Add ordered images with add_images_to_collection.",
            "Capture a reproducible contact_sheet with create_contact_sheet (snapshots membership).",
            "Render with render_contact_sheet_svg_artifact, or record a planned PNG/JPG raster export.",
        ],
        expected_outcome: "A reproducible contact sheet exists whose membership is fixed even as the collection evolves.",
        failure_modes: &["empty collection", "member asset bytes unavailable", "caller expects rendered raster bytes"],
    },
    ManualWorkflow {
        id: "atelier_documents_story_scripts",
        title: "Atelier Character Documents, Story Cards, And Scripts (MT-055)",
        prerequisites: &["A character exists"],
        steps: &[
            "Create a character document with create_character_document.",
            "Append text with append_character_document_version (append-only).",
            "Add ordered story structure with add_story_card and add_story_beat.",
            "Persist a data-only character image-sourcing script with create_character_script.",
        ],
        expected_outcome: "Character notes/stories keep full version history and scripts stay data-only with no execution authority.",
        failure_modes: &["stale document parent version", "attempt to set an executable script authority mode"],
    },
    ManualWorkflow {
        id: "atelier_moodboard_snapshot_and_export",
        title: "Atelier Moodboard Snapshot And Export (MT-056)",
        prerequisites: &["A moodboard character document exists"],
        steps: &[
            "Record a moodboard snapshot with record_moodboard_snapshot using schema hsk.atelier.moodboard@1.",
            "Record layer operations with record_moodboard_operation (typed receipts).",
            "Request an export with request_moodboard_export.",
        ],
        expected_outcome: "The exact moodboard JSON is preserved for round-trip export with a validated projection and operation receipts.",
        failure_modes: &["schema_id mismatch", "moodboard JSON fails schema validation"],
    },
    ManualWorkflow {
        id: "atelier_relationship_graph",
        title: "Atelier Character Relationship Graph (MT-057)",
        prerequisites: &["At least two characters exist"],
        steps: &[
            "Create relationship edges with create_character_relationship (endpoints validated).",
            "Adjust edges with update_character_relationship.",
            "Project the neighbourhood with character_relationship_graph.",
        ],
        expected_outcome: "A validated relationship-map graph projects from the stored edges.",
        failure_modes: &["endpoint character not found", "self-referential edge rejected"],
    },
    ManualWorkflow {
        id: "atelier_search_palette_and_similarity",
        title: "Atelier Search, Tag Palette, And Similarity (MT-058)",
        prerequisites: &["Media/characters indexed", "Similarity projections populated for similarity search"],
        steps: &[
            "Search with global_search_with_lens_filters.",
            "Curate the tag palette with ensure_tag and deterministic create_tag_rule plus recompute_derived_tags.",
            "Record AI tag suggestions through POST /atelier/ai-tag-suggestions and accept/reject/apply them.",
            "Find near-duplicates with find_similar_assets over the perceptual-hash projection.",
        ],
        expected_outcome: "Search, deterministic tag rules, AI-suggested tags, and perceptual-hash similarity all resolve over the atelier graph.",
        failure_modes: &["empty query with no lens filters", "seed asset has no similarity projection"],
    },
    ManualWorkflow {
        id: "atelier_web_portfolio_export",
        title: "Atelier Web Portfolio Export (MT-059)",
        prerequisites: &["A source collection with media exists"],
        steps: &[
            "Open a web portfolio export with request_web_portfolio_export using a no-space portable slug.",
            "Render to manifest hsk.atelier.web_portfolio_export_manifest@1 with ArtifactStore-backed items.",
        ],
        expected_outcome: "A portable web portfolio export request and machine-readable manifest are recorded.",
        failure_modes: &["slug contains blank spaces", "unknown source_collection_id"],
    },
    ManualWorkflow {
        id: "atelier_share_pack_export",
        title: "Atelier Share-Pack Export (MT-073)",
        prerequisites: &["An export request with a recorded sheet result", "Selected media assets"],
        steps: &[
            "Open a sheet export with request_sheet_export pinned to an immutable sheet version.",
            "Record its rendered result, then assemble the pack with build_share_pack_manifest (sheet + media + usage README).",
        ],
        expected_outcome: "An ordered share-pack manifest of ArtifactStore-backed entries with stable pack paths is produced.",
        failure_modes: &["selector includes sheet but no export result is recorded", "selector includes nothing"],
    },
    ManualWorkflow {
        id: "atelier_llm_evidence_pack_export",
        title: "Atelier LLM Evidence-Pack Export (MT-074)",
        prerequisites: &["Evidence files with source anchors prepared in the ArtifactStore"],
        steps: &[
            "Assemble the four required files (README.md, evidence.json, redactions.json, source-index.json).",
            "Build and validate with build_llm_evidence_pack_manifest (schema hsk.atelier.llm_evidence_pack_manifest@1).",
        ],
        expected_outcome: "A deterministic, redaction-checked evidence pack manifest is produced for no-context model ingestion.",
        failure_modes: &["missing a required file", "redaction_required file not marked redacted"],
    },
    ManualWorkflow {
        id: "atelier_backup_restore_preflight",
        title: "Atelier Backup Manifest And Restore Preflight (MT-075)",
        prerequisites: &["A backup artifact with per-file checksums"],
        steps: &[
            "Record a backup manifest with record_backup_manifest (app/spec/schema versions + file checksums).",
            "Before restoring, run backup_restore_preflight against the current app/spec/schema versions.",
        ],
        expected_outcome: "A versioned, checksummed backup manifest is recorded and a newer-versioned backup is refused with a typed reason.",
        failure_modes: &["empty file list", "newer app/spec/schema backup cannot be restored"],
    },
    ManualWorkflow {
        id: "atelier_reset_recovery_and_orphan_adoption",
        title: "Atelier Reset Recovery And Orphan Adoption (MT-060)",
        prerequisites: &["Atelier store reachable"],
        steps: &[
            "Run a bounded reset with record_atelier_reset; full_preserve_original_media keeps media in an orphan manifest.",
            "List retained items with list_orphan_manifest_items.",
            "Adopt an orphan item back into an open intake batch with adopt_orphan_manifest_item.",
        ],
        expected_outcome: "A reset never destroys original media; retained orphans can be adopted back into intake.",
        failure_modes: &["unsupported reset mode", "orphan item already adopted", "target intake batch not open"],
    },
    // ---- WP-KERNEL-005 Pose / ComfyUI pipeline workflows (MT-122..MT-125) ----
    ManualWorkflow {
        id: "atelier_pose_context_and_rig",
        title: "Atelier Pose Context And Rig (MT-122)",
        prerequisites: &["Atelier PostgreSQL store reachable", "Detection runs out-of-module as a capability-gated Workflow-Engine job"],
        steps: &[
            "Set a typed pose editor context with set_pose_context_state (append-only).",
            "Ingest an OpenPose rig the detection job produced with ingest_pose_rig; the body-18/face-70/hand-21 keypoint shape is validated.",
            "Read rigs with list_pose_rigs and inspect keypoints with get_pose_rig.",
            "Record calibration with set_pose_calibration; preserve it as unresolved/BLOCKED with a block_reason rather than faking values, and read it back with get_calibration.",
        ],
        expected_outcome: "Pose context, rigs, and BLOCKED-by-default calibration are governed in PostgreSQL with no in-module detector execution.",
        failure_modes: &["keypoint cardinality mismatch", "unresolved calibration missing a block_reason"],
    },
    ManualWorkflow {
        id: "atelier_pose_sidecar_and_identity",
        title: "Atelier OpenPose Sidecars And Identity Profiles (MT-123)",
        prerequisites: &["A pose rig exists", "ArtifactStore configured"],
        steps: &[
            "Register typed sidecars (openpose_json/openpose_png/conditioning_png) with record_pose_sidecar.",
            "List them with list_pose_sidecars and project them hidden-from-gallery with pose_sidecar_gallery_projection.",
            "Append a versioned identity profile with append_identity_profile and adjust mutable fields with update_identity_profile.",
            "Record a 512x512 identity crop artifact with record_identity_crop_artifact and read the head with latest_identity_profile.",
        ],
        expected_outcome: "OpenPose sidecars are exportable yet hidden from normal galleries, and identity profiles keep append-only lineage with redacted provenance.",
        failure_modes: &["sidecar mime/kind mismatch", "identity crop not exactly 512x512"],
    },
    ManualWorkflow {
        id: "atelier_comfy_workflow_receipts",
        title: "Atelier ComfyUI Workflow Receipts, Replay, And Failure Recovery (MT-124)",
        prerequisites: &["A ComfyUI workflow_run_id exists", "Outputs materialized through the ArtifactStore"],
        steps: &[
            "Record the durable workflow receipt with record_comfy_workflow_receipt and read it with get_comfy_workflow_receipt.",
            "Replay workflow history with list_comfy_workflow_history and summarize a run with produce_intake_receipt.",
            "When the bridge path is not taken, mark mark_saveimage_fallback with a reason.",
            "Preserve a save-but-failed-registration output with record_comfy_output_registration_failure, then recover it with retry_comfy_output_registration_failure (output-first persistence).",
        ],
        expected_outcome: "Every ComfyUI run is reconstructable from receipts, and a saved output is never lost to a registration failure.",
        failure_modes: &["failed receipt missing error_ref", "retrying a non-retryable failure"],
    },
    ManualWorkflow {
        id: "atelier_pose_comfy_deferred_boundaries",
        title: "Atelier Pose/ComfyUI Deferred Boundaries And Adapters (MT-125)",
        prerequisites: &["engine.comfyui capability profile available for bridge registration"],
        steps: &[
            "Register the bridge capability with register_bridge_capability; only gate-accepted outputs are routable.",
            "Inspect the adapter boundary with list_capability_rejects (rejected outputs are never routed).",
            "Record governed URL image-sourcing intake through POST /atelier/image-import/url (record_url_image_import).",
            "Record carry-forward deferrals with set_calibration as unresolved/BLOCKED (Calibration Panel 10.10.4.1.9 and full PoseKit activation WP-0133 are not yet implementable).",
        ],
        expected_outcome: "Workflow adapter boundaries are explicit: capability-gated routing, governed image-sourcing, and BLOCKED carry-forward deferrals are recorded, never faked.",
        failure_modes: &["capability_grant_ref not granted engine.comfyui", "BLOCKED deferral missing a block_reason"],
    },
    // ---- WP-KERNEL-005 Model-Workflow-Diagnostics workflows (MT-132, MT-135, MT-159, MT-164, MT-181, MT-183, MT-185, MT-186, MT-187) ----
    ManualWorkflow {
        id: "diagnostics_source_evidence_matrix",
        title: "Diagnostics Source Evidence And Status Matrix (MT-132)",
        prerequisites: &["ModelManual reachable through kernel.model_manual.get", "Action catalog reachable through kernel.action_catalog.view"],
        steps: &[
            "List the command map with kernel.action_catalog.view and the manual command_reference with model_manual_list_commands.",
            "For each surface, resolve its source (catalog action, IPC route, or backend method) and record an evidence_pointer with diagnostics_source_evidence_matrix.",
            "Inspect live sessions and loaded models with the inspector reads to confirm runtime evidence; query diagnostics_problem_store_query for recorded problems.",
            "Mark any surface with no resolvable source as a blocker; never claim Wired without a registered action or route.",
        ],
        expected_outcome: "A no-context model has a source/evidence + status matrix showing which diagnostics surfaces are Wired vs Planned and where each surface's evidence comes from.",
        failure_modes: &["surface marked Wired without a registered source", "evidence pointer cannot be resolved"],
    },
    ManualWorkflow {
        id: "diagnostics_no_context_manual_structure",
        title: "No-Context Model Manual Structure (MT-135)",
        prerequisites: &["kernel.model_manual.* IPC bridge registered"],
        steps: &[
            "Read the full manual manifest with model_manual_get (version, feature_groups, command_reference, safety_constraints, workflows).",
            "List the command index with model_manual_list_commands and search by area with model_manual_search.",
            "Propose any manual edit as governed write-box evidence through kernel.model_manual.update_section.",
        ],
        expected_outcome: "A no-context model can read and navigate the built-in manual skeleton and propose edits only through the governed catalog action.",
        failure_modes: &["IPC bridge not registered", "edit attempted outside the editable-surface guard"],
    },
    ManualWorkflow {
        id: "diagnostics_gui_verification_checklist",
        title: "GUI Verification Checklist (MT-159)",
        prerequisites: &["WebView2 CDP port available", "Product screenshot browser adapter available"],
        steps: &[
            "Snapshot the DOM with visual_debug_dom_snapshot and confirm stable element ids (data-testid) for discoverability and stable identifiers.",
            "Start visual_debug_console_stream_start to catch console/exception noise, then stop it with visual_debug_console_stream_stop.",
            "Capture a product screenshot with kernel.product_screenshot_capture.execute to verify readability, no overlap, responsive layout, and visible state without stealing focus.",
            "Project the visual debugging loop with kernel.visual_debugging_loop.project to compare against the baseline.",
        ],
        expected_outcome: "GUI readability, discoverability, navigation, no-overlap layout, responsiveness, visible state, and stable ids are all verified through observable, non-intrusive surfaces.",
        failure_modes: &["capture opened a foreground window", "snapshot lacked stable element ids", "no baseline to diff against"],
    },
    ManualWorkflow {
        id: "diagnostics_build_rules_read_evidence",
        title: "Build Rules Read Evidence (MT-164)",
        prerequisites: &["An HBR acceptance matrix exists for the work packet"],
        steps: &[
            "Evaluate the typed handoff gate with diagnostics_hbr_handoff_gate_evaluate to see which build-rule evidence kinds are required before build/test/release.",
            "Run hbr_matrix_check so PENDING/STEER/BLOCKED HBR rows block PASS closure.",
            "Resolve PROVED evidence pointers with hbr_validator_scan; emit typed receipts with hbr_violation_emit for any failure.",
        ],
        expected_outcome: "A model reads and acknowledges the build-rule gate, with typed evidence, before build/test/release work proceeds.",
        failure_modes: &["required evidence kind missing", "PROVED row has no resolvable evidence pointer"],
    },
    ManualWorkflow {
        id: "diagnostics_integration_smoke_path",
        title: "Diagnostics Integration Smoke Path (MT-181)",
        prerequisites: &["A governed session and thread exist", "Inspector and action catalog reachable"],
        steps: &[
            "Claim the lease with kernel.role_mailbox_claim_lease.project, then read session state with kernel_inspector_session_state.",
            "Run the only inspector-plane mutation through inspector_replay_drive (action_id + verified WriteBoxV1 envelope) and confirm the receipt in kernel_inspector_event_ledger_tail.",
            "Capture a visual snapshot for GUI evidence, then export a redacted bundle with diagnostics_debug_bundle_export and confirm lease release.",
        ],
        expected_outcome: "One end-to-end no-context operation path (session -> lease -> state -> command -> receipt -> visual -> bundle -> release) is provable.",
        failure_modes: &["lease held by another claimant", "replay-drive envelope signature invalid", "bundle redaction failed"],
    },
    ManualWorkflow {
        id: "diagnostics_red_team_drift_guards",
        title: "Diagnostics Red-Team Manual Visual And Command Drift Guards (MT-183)",
        prerequisites: &["ModelManual and action catalog reachable"],
        steps: &[
            "Run diagnostics_manual_drift_guard to find any Wired manual command that does not resolve to a registered kernel.action_catalog.view action or IPC route.",
            "Confirm MANUAL_VERSION was bumped on the wired-surface diff (HBR-MAN-001).",
            "Project the visual debugging loop with kernel.visual_debugging_loop.project to catch visual-loop drift against the baseline.",
        ],
        expected_outcome: "Manual-command drift and visual-loop drift are caught as focused negative checks rather than silent regressions.",
        failure_modes: &["Wired command with no registered surface", "MANUAL_VERSION not bumped", "visual baseline missing"],
    },
    ManualWorkflow {
        id: "diagnostics_core_row_merge",
        title: "Manual Source Merge For Core Rows (MT-185)",
        prerequisites: &["The atelier Core/Data manual rows (MT-052..MT-060, MT-073..MT-075) exist in the manual"],
        steps: &[
            "Enumerate the atelier Core/Data feature groups and commands with diagnostics_core_row_merge.",
            "Normalize ids and merge them into the Diagnostics manual dataset.",
            "Mark any expected Core/Data source row that is absent as a blocker; never fabricate a row.",
        ],
        expected_outcome: "Core/Data manual source rows are merged into the Diagnostics dataset with normalized ids and explicit blockers for anything missing.",
        failure_modes: &["expected Core/Data row missing", "id normalization collision"],
    },
    ManualWorkflow {
        id: "diagnostics_pose_row_merge",
        title: "Manual Source Merge For Pose Rows (MT-186)",
        prerequisites: &["The Pose/ComfyUI manual rows (MT-122..MT-125) exist in the manual"],
        steps: &[
            "Enumerate the Pose/ComfyUI feature groups and commands with diagnostics_pose_row_merge.",
            "Normalize ids and merge them into the Diagnostics manual dataset.",
            "Mark any expected Pose/ComfyUI source row that is absent as a blocker; never fabricate a row.",
        ],
        expected_outcome: "Pose/ComfyUI manual source rows are merged into the Diagnostics dataset with normalized ids and explicit blockers for anything missing.",
        failure_modes: &["expected Pose/ComfyUI row missing", "id normalization collision"],
    },
    ManualWorkflow {
        id: "diagnostics_owned_row_merge",
        title: "Manual Source Merge For Diagnostics Rows (MT-187)",
        prerequisites: &["The Diagnostics-owned manual/action/state/error/bundle rows exist in the manual"],
        steps: &[
            "Enumerate the Diagnostics-owned rows (problem store, action-catalog command map, inspector state reads, bundle export) with diagnostics_owned_row_merge.",
            "Normalize ids across manual/action/state/error/bundle kinds and merge them into the manual dataset.",
            "Mark any expected Diagnostics-owned row that is absent as a blocker; never fabricate a row.",
        ],
        expected_outcome: "Diagnostics-owned manual/action/state/error/bundle rows are merged with normalized ids and explicit blockers for anything missing.",
        failure_modes: &["expected Diagnostics-owned row missing", "row not classifiable under a supported row_kind"],
    },
];
