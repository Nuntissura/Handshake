{
  "schema_id": "hsk.sandbox_cross_adapter_parity@1",
  "name": "kill_signal_int",
  "description": "Trap SIGINT inside the process and exit 42; send SIGINT via adapter.kill(Int). The shell uses `trap 'exit 42' INT; sleep 60`. Exit code 42 verifies signal delivery + trap handler ran; stop_reason carries `kill:int`.",
  "spec": {
    "id": "placeholder",
    "image_or_root": "alpine:3.20",
    "cmd": ["sh", "-c", "trap 'exit 42' INT; sleep 60 & wait"],
    "env": {},
    "cwd": null,
    "binds": [],
    "net_policy": "deny_all",
    "resource_limits": {
      "memory_bytes": null,
      "cpu_cores": null,
      "timeout_ms": null
    },
    "required_capabilities": [],
    "metadata": {}
  },
  "expected": {
    "exit_code_class": "any",
    "stop_reason_contains": "kill:int"
  },
  "kill_signal": "int",
  "kill_timeout_ms": 10000
}
