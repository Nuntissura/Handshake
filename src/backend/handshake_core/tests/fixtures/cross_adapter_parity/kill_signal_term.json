{
  "schema_id": "hsk.sandbox_cross_adapter_parity@1",
  "name": "kill_signal_term",
  "description": "Spawn `sleep 60`, send SIGTERM via adapter.kill(Term), assert termination within 10s. The harness drives kill via fixture.kill_signal=term so the STOP ledger row carries stop_reason=`kill:term`.",
  "spec": {
    "id": "placeholder",
    "image_or_root": "alpine:3.20",
    "cmd": ["sleep", "60"],
    "env": {},
    "cwd": null,
    "binds": [],
    "net_policy": "deny_all",
    "resource_limits": {
      "memory_bytes": null,
      "cpu_cores": null,
      "timeout_ms": null
    },
    "required_capabilities": [],
    "metadata": {}
  },
  "expected": {
    "exit_code_class": "any",
    "stop_reason_contains": "kill:term"
  },
  "kill_signal": "term",
  "kill_timeout_ms": 10000
}
