[
  {
    "scenario_id": "OP-01",
    "edge": "stage",
    "pillar": "Pillar 17 (Stage)",
    "description": "Route a rich-text selection to the Stage pane, then embed-back the produced Stage artifact into the originating note as an MT-014 hsLink NodeView carrying SHA-256 manifest provenance of the routed payload. Provable-NOW (non-ignored): the route-leg payload builds + the embed-back NodeView's sha256 EQUALS the recomputed SHA-256 of the exact routed bytes (CTRL-3). REQUIRES_PG (live, --ignored): the Stage capture-store route round-trip POST/GET against real PostgreSQL (the routes now EXIST). The STAGE_EMBED_BACK Flight Recorder event is a FRONTEND-emission follow-up (event_emitter.rs has no StageEmbedBack action), not a backend gap.",
    "surfaces_involved": ["rich-text editor", "Stage pane (Pillar 17)", "command bus (interop.route-to-stage)", "Stage artifact store (api/stage.rs)", "EventLedger / Flight Recorder"],
    "backend_apis_called": ["POST /workspaces/{ws}/stage/artifacts (EXISTS, api/stage.rs MT-066)", "GET /workspaces/{ws}/stage/artifacts/{id} (EXISTS, api/stage.rs MT-066)", "PUT /workspaces/{ws}/knowledge/documents/{doc_id} (EXISTS)", "GET /workspaces/{ws}/knowledge/documents/{doc_id} (EXISTS)", "GET /api/flight_recorder (EXISTS)", "POST /api/flight_recorder/native_editor_event (EXISTS; accepts the 5 interop kinds)"],
    "accesskit_ids": ["stage-pane", "stage-routed-content", "stage-capture-embed-back"],
    "expected_fr_events": ["STAGE_ROUTE", "STAGE_EMBED_BACK"],
    "fr_events_emission": "pending_frontend: STAGE_ROUTE maps to the existing NativeEditorAction::RouteToStage; STAGE_EMBED_BACK has NO NativeEditorAction variant and the production transport still posts to /flight_recorder/runtime_chat_event, not the /flight_recorder/native_editor_event route that now accepts the interop kinds. Backend FR ingestion route EXISTS.",
    "proof_fn": "other_pillar_op01_stage_route_embed_back",
    "live_proof_fn": "other_pillar_op01_stage_route_embed_back_live",
    "status": "REQUIRES_PG",
    "follow_up": "fr_event_not_emitted_frontend[STAGE_EMBED_BACK]: wire event_emitter.rs to emit the interop kind through the native_editor_event route (MT-036 emitter + MT-066 stage wiring). The route round-trip is proven by the requires_pg live proof; the provable-NOW sha256 provenance half passes non-ignored.",
    "provable_now": "route-leg payload + embed-back MT-014 NodeView sha256 == recomputed SHA-256 of routed bytes (CTRL-3); proven by other_pillar_op01_stage_route_embed_back (non-ignored). The stage capture-store route round-trip is proven by other_pillar_op01_stage_route_embed_back_live under a managed PG."
  },
  {
    "scenario_id": "OP-02",
    "edge": "calendar",
    "pillar": "Pillar 2 (Calendar)",
    "description": "Bind a daily note to a real CalendarEvent row (bidirectional reference) and correlate an ActivitySpan for the bound day to the set of edited documents. Provable-NOW (non-ignored): the open-or-create daily-note binding is idempotent and DELEGATES to the MT-019 service (single doc/date), and the ActivitySpan correlation returns the edited documents. REQUIRES_PG (live, --ignored): the calendar activity-span POST/GET + events-window round-trip against real PostgreSQL (the routes now EXIST). The CALENDAR_EVENT_BOUND/ACTIVITY_SPAN_CORRELATED Flight Recorder events are a FRONTEND-emission follow-up.",
    "surfaces_involved": ["rich-text daily note", "daily-journal panel (Pillar 2)", "MT-019 daily-note service", "calendar activity-span store (api/calendar.rs)", "EventLedger / Flight Recorder"],
    "backend_apis_called": ["PUT /workspaces/{ws}/loom/journals/{date} (MT-019, EXISTS)", "GET /workspaces/{ws}/calendar/events?from=&to= (EXISTS, api/calendar.rs MT-067)", "GET /workspaces/{ws}/calendar/activity-spans?event_id= (EXISTS, api/calendar.rs MT-067)", "POST /workspaces/{ws}/calendar/activity-spans (EXISTS, api/calendar.rs MT-067)", "GET /api/flight_recorder (EXISTS)"],
    "accesskit_ids": ["daily-journal-panel", "daily-journal-calendar-event-chip", "daily-journal-activity-strip"],
    "expected_fr_events": ["CALENDAR_EVENT_BOUND", "ACTIVITY_SPAN_CORRELATED"],
    "fr_events_emission": "pending_frontend: neither CALENDAR_EVENT_BOUND nor ACTIVITY_SPAN_CORRELATED has a NativeEditorAction variant, and the production transport targets /flight_recorder/runtime_chat_event. Backend FR ingestion route EXISTS and accepts both interop kinds.",
    "proof_fn": "other_pillar_op02_calendar_bind_activity_span",
    "live_proof_fn": "other_pillar_op02_calendar_bind_activity_span_live",
    "status": "REQUIRES_PG",
    "follow_up": "fr_event_not_emitted_frontend[CALENDAR_EVENT_BOUND, ACTIVITY_SPAN_CORRELATED]: wire event_emitter.rs to emit both interop kinds via the native_editor_event route (MT-036 emitter + MT-067 calendar wiring). The activity-span + events-window route round-trips are proven by the requires_pg live proof.",
    "provable_now": "idempotent daily-note binding delegates to MT-019 (single DocId across two opens) + ActivitySpan correlation returns edited_documents; proven by other_pillar_op02_calendar_bind_activity_span (non-ignored). The calendar activity-span/events routes now EXIST and are round-tripped by other_pillar_op02_calendar_bind_activity_span_live under a managed PG."
  },
  {
    "scenario_id": "OP-03",
    "edge": "locus",
    "pillar": "Pillar 6 (Locus)",
    "description": "Resolve a locus:// cross-reference in a note to its WP/MT target, and reverse-lookup the WP/MT to list the referencing document(s) from the persisted reverse index. Provable-NOW (non-ignored): the locus:// ref parses + resolves (200 projection -> non-empty title) and the reverse lookup lists the referencing note keyed on the single normalized key, driven through the REAL MT-034 find_notes_with pipeline. REQUIRES_PG (live, --ignored): the locus work-packet/microtask resolve route round-trip against real PostgreSQL (the routes now EXIST). The LOCUS_REF_RESOLVED/LOCUS_REVERSE_LOOKUP Flight Recorder events are a FRONTEND-emission follow-up.",
    "surfaces_involved": ["rich-text editor (locus-ref chip)", "Locus resolve routes (api/locus.rs)", "MT-034 find_notes_with reverse index (loom/search-v2)", "EventLedger / Flight Recorder"],
    "backend_apis_called": ["GET /workspaces/{ws}/locus/work-packets/{id} (EXISTS, api/locus.rs MT-068)", "GET /workspaces/{ws}/locus/microtasks/{id} (EXISTS, api/locus.rs MT-068)", "Loom Search v2 (find_notes_with, EXISTS) - the reverse index", "GET /api/flight_recorder (EXISTS)"],
    "accesskit_ids": ["locus-ref-chip-wp-WP-KERNEL-012", "locus-ref-chip-mt-MT-066", "locus-refby-{document_id}"],
    "expected_fr_events": ["LOCUS_REF_RESOLVED", "LOCUS_REVERSE_LOOKUP"],
    "fr_events_emission": "pending_frontend: neither LOCUS_REF_RESOLVED nor LOCUS_REVERSE_LOOKUP has a NativeEditorAction variant, and the production transport targets /flight_recorder/runtime_chat_event. Backend FR ingestion route EXISTS and accepts both interop kinds.",
    "proof_fn": "other_pillar_op03_locus_resolve_reverse",
    "live_proof_fn": "other_pillar_op03_locus_resolve_reverse_live",
    "status": "REQUIRES_PG",
    "follow_up": "fr_event_not_emitted_frontend[LOCUS_REF_RESOLVED, LOCUS_REVERSE_LOOKUP]: wire event_emitter.rs to emit both interop kinds via the native_editor_event route (MT-036 emitter + MT-068 locus wiring). The locus resolve route round-trip is proven by the requires_pg live proof (needs a seeded work_packets row).",
    "provable_now": "locus:// parse + 200 resolve projection (non-empty title) + reverse lookup lists referencing doc keyed on locus://mt/mt-066 via the REAL MT-034 pipeline; proven by other_pillar_op03_locus_resolve_reverse (non-ignored). The /locus/ HTTP resolve routes now EXIST and are round-tripped by other_pillar_op03_locus_resolve_reverse_live under a managed PG."
  },
  {
    "scenario_id": "OP-04",
    "edge": "swarm",
    "pillar": "Pillar 17 + 2 + 6 (all three edges, swarm parity)",
    "description": "An out-of-process-style agent reaches AND activates each of the three interop edges (Stage, Calendar, Locus) PURELY via stable AccessKit author_ids (no coordinates, no label-scraping), verifying each id is present + STABLE across two re-queries + the AccessKit Click dispatch reaches the node. The swarm reachability/stability/dispatch half is PROVEN (non-ignored). The live edge-drive that hits the backend routes is proven by the OP-01/02/03 requires_pg live proofs; the per-edge Flight Recorder events are a FRONTEND-emission follow-up (same as OP-01..03).",
    "surfaces_involved": ["Stage pane", "daily-journal panel", "rich-text editor (locus chip)", "AccessKit registry (MT-041/042/066/067/068)", "EventLedger / Flight Recorder"],
    "backend_apis_called": ["(none for the provable-NOW reachability half)", "the OP-01/02/03 routes for the live edge-drive (now EXIST)", "GET /api/flight_recorder (EXISTS)"],
    "accesskit_ids": ["stage-capture-embed-back", "stage-pane", "stage-routed-content", "daily-journal-calendar-event-chip", "daily-journal-panel", "daily-journal-activity-strip", "locus-ref-chip-wp-WP-KERNEL-012"],
    "expected_fr_events": ["STAGE_ROUTE", "STAGE_EMBED_BACK", "CALENDAR_EVENT_BOUND", "ACTIVITY_SPAN_CORRELATED", "LOCUS_REF_RESOLVED", "LOCUS_REVERSE_LOOKUP"],
    "fr_events_emission": "pending_frontend: the 5 interop kinds (STAGE_EMBED_BACK, CALENDAR_EVENT_BOUND, ACTIVITY_SPAN_CORRELATED, LOCUS_REF_RESOLVED, LOCUS_REVERSE_LOOKUP) have no NativeEditorAction variant; only STAGE_ROUTE is emittable today. Backend FR ingestion route EXISTS and accepts all 5.",
    "proof_fn": "other_pillar_op04_swarm_accesskit",
    "status": "PASS",
    "follow_up": "The swarm reachability/stability/dispatch half passes non-ignored. The live edge-drive against the now-existing routes is proven by the OP-01/02/03 requires_pg live proofs; the per-edge FR events await the frontend-emission follow-up (fr_event_not_emitted_frontend).",
    "provable_now": "all 3 interop-edge triggers reached + role-confirmed + stable across two frame re-queries + activated via an AccessKit Click dispatch, purely by author_id (CTRL-5); proven by other_pillar_op04_swarm_accesskit (non-ignored)."
  }
]
