# MT-005 - Native GUI Deterministic Build Hotfix

Status: COMPLETED

Authority: `.GOV/task_packets/WP-CKC-posekit-overhaul/MT-005.json`

Scope: make `cargo build --manifest-path src/frontend/handshake_native/Cargo.toml --bin handshake-native` produce a GUI executable that launches without first running a test build.

Target files:
- `src/frontend/handshake_native/Cargo.toml`
- `src/frontend/handshake_native/Cargo.lock`

Acceptance:
- Fresh non-test cargo build succeeds into `../Handshake_Artifacts/ckc-overhaul-launch/native-target-mt005`.
- The resulting `handshake-native.exe` launches without the wgpu backend feature panic.
- Process path proves the running GUI came from the MT-owned current-source target.
- Backend health remains OK.

Do not touch:
- `../wtc-native-editors-v1`
- `../handshake_main`
- inherited diagnostics/Palmistry files

Verification:
- `cargo tree --manifest-path src/frontend/handshake_native/Cargo.toml -e features -i wgpu`
- `cargo build --manifest-path src/frontend/handshake_native/Cargo.toml --bin handshake-native --target-dir ../Handshake_Artifacts/ckc-overhaul-launch/native-target-mt005`
- Launch the built exe and verify process path/window/stderr/backend health.

Result:
- Product files changed: `src/frontend/handshake_native/Cargo.toml`, `src/frontend/handshake_native/Cargo.lock`.
- Fresh non-test build passed into `../Handshake_Artifacts/ckc-overhaul-launch/native-target-mt005`.
- Fresh GUI launch passed from that target, PID `26328`, window title `Handshake`.
- stderr log was empty; the previous wgpu backend feature panic did not recur.
- Backend health remained OK on `127.0.0.1:37501`.
