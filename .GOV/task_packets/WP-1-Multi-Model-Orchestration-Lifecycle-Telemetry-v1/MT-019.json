{
  "schema_id": "hsk.microtask_contract@1",
  "schema_version": "microtask_contract_v1",
  "contract_authority": "PRIMARY_MACHINE_READABLE",
  "wp_id": "WP-1-Multi-Model-Orchestration-Lifecycle-Telemetry-v1",
  "mt_id": "MT-019",
  "created_at_utc": "2026-07-29T03:23:21Z",
  "updated_at_utc": "2026-09-02T20:42:14.419Z",
  "authority_files": {
    "microtask_contract": ".GOV/task_packets/WP-1-Multi-Model-Orchestration-Lifecycle-Telemetry-v1/MT-019.json",
    "packet_contract": ".GOV/task_packets/WP-1-Multi-Model-Orchestration-Lifecycle-Telemetry-v1/packet.json"
  },
  "markdown_projection": {
    "path": "",
    "status": "SKIPPED_MINIMAL_GOVERNANCE"
  },
  "lifecycle": {
    "status": "CLAIMED",
    "depends_on": [
      "MT-007"
    ],
    "blocks": [],
    "active": true,
    "validator_verdict": "PENDING",
    "claimed_by": "KERNEL_BUILDER-20260902-180244",
    "claimed_at_utc": "2026-09-02T20:42:14.419Z",
    "ready_for_validation_by": null,
    "ready_for_validation_at_utc": null,
    "completed_by": null,
    "blocker": null,
    "missing_resource": null,
    "proof_status": "V6_REMEDIATION_CLAIMED_2026_09_02: claimed by KERNEL_BUILDER-20260902-180244 (parent) for delegated sub-agent implementation in the declared product worktree; dependency gate satisfied inside SESSION_MT_BATCH MT-002..MT-024 (predecessor MTs are remediated in the same batch under CX-503C disjoint-work rule). No proof claimed yet.",
    "hbr_status": "PENDING_PARENT_RUNTIME_PROOF: source and focused tests cover exact process-set ownership, one-field/missing-scope denial, same-instance and foreign-owner safety, two-sample dead-owner corroboration, set-drift rejection, bounded kill timeout, STOP-reservation rejection, crash-after-kill no-rekill recovery, concurrent one-kill/one-STOP fencing, production-runtime fail-open reporting, durable Surreal STOP closure, periodic retry, production-runtime timeout, and real-process identity fencing. Runtime verdict is intentionally unclaimed.",
    "kb_ready_checklist_receipt_id": null,
    "product_worktree_root_resolution": "../wtc-multi-model-orchestration-v1",
    "pre_implementation_adversarial_review_status": "COMPLETE_2026_07_30_PLAN_REQUIRES_REWORK_P1_P2_P3_P4_P5_PREREQUISITES",
    "post_implementation_adversarial_review_status": "V6_CONTRACT_ONLY_STATIC_ADVERSARIAL_REAUDIT_COMPLETE",
    "adversarial_findings_status": "BLOCKED_ON_DEPENDENCY_RUNTIME_AND_TWO_COUNTERFACTUAL_PROOFS_PENDING",
    "validated_by": null,
    "validated_at_utc": null,
    "last_runtime_status_ref": null
  },
  "origin": "Deferred from MT-007 post-implementation adversarial review (2026-07-29).",
  "scope": {
    "summary": "Harden the spawned-process reclaim reaper beyond the MT-007 crash->restart boot case: cover the running-app window, resilient boot, periodic retry, exact ownership fencing, and restart continuity over embedded SurrealDB. Current source is substantially implemented; V6 runtime and newly required inspection-boundary proof remain pending.",
    "root_cause": "Historical defect, now remediated in source: restart-orphan surfacing formerly ran only at boot and a live-instance Official-CLI orphan could remain open until a later boot. Current source composes a bounded boot pass, periodic re-surfacing, and an exact-owner running-app hook over one injected SurrealStorage.",
    "allowed_paths": [
      "src/backend/handshake_core/src/process_ledger/",
      "src/backend/handshake_core/src/storage/surreal/process_ledger.rs",
      "src/backend/handshake_core/src/storage/surreal/process_ledger_schema.surql",
      "src/backend/handshake_core/Cargo.toml",
      "src/backend/handshake_core/src/main.rs",
      "src/backend/handshake_core/src/model_runtime/",
      "src/backend/handshake_core/src/user_manual/",
      "src/backend/handshake_core/src/api/mod.rs",
      "src/backend/handshake_core/tests/"
    ],
    "owned_files": [
      "src/backend/handshake_core/src/process_ledger/reclaim.rs",
      "src/backend/handshake_core/src/process_ledger/runtime.rs",
      "src/backend/handshake_core/src/model_runtime/cloud/official_cli_bridge.rs",
      "src/backend/handshake_core/src/storage/surreal/process_ledger.rs",
      "src/backend/handshake_core/src/storage/surreal/process_ledger_schema.surql",
      "src/backend/handshake_core/Cargo.toml",
      "src/backend/handshake_core/tests/process_reclaim_real_lifecycle_surreal_tests.rs"
    ],
    "forbidden_paths": [
      "front-end UI changes",
      "app/src, app/src-tauri, React/TypeScript",
      "weakening the pid + creation-time + exe-sha256 process identity fence",
      "fabricating a STOP from unproven kill evidence",
      "any relational database implementation or compatibility path",
      "database migrations (MT-019 requires none)"
    ],
    "acceptance_criteria": [
      "F1 (MAJOR): a same-instance mid-run official-CLI (no model_lanes row) orphan whose process terminated/reap-failed is reaped by the RUNNING app (staleness task or a CLI-session-end reclaim trigger), not only at next boot; add a runtime proof.",
      "F2 (MAJOR): after the bounded inline boot pass, every periodic tick re-runs the same restart-orphan reconcile before stale-lane reclaim so skipped, failed-open, or newly visible orphans are retried without another boot; prove the shared path and bounded cancellation.",
      "F3 (operator decision): resilient boot is fail-open only for KillOutcome::Failed, leaving the row open, claim released, fence cleared, no false STOP, processes_kill_failed surfaced, and periodic retry scheduled. Store/surfacing errors and boot timeout fail closed; prove both postures.",
      "F4 (MINOR): HANDSHAKE_STARTUP_RECOVERY_ONLY=1 composes ProcessReclaimRuntime from the same injected SurrealStorage, runs the bounded restart-orphan pass, surfaces its report, drains reclaim and writer, and only then returns; prove this production branch.",
      "F5 (MINOR): docs and counters remain truthful: processes_reclaimed counts only Killed or KilledPendingStop, processes_kill_failed counts Failed, and no failure path fabricates a STOP.",
      "F6 (MINOR): boot and periodic restart reconciliation use one shared implementation with caller-selected error policy and cancellation, and no in-progress sweep reclaim error is silently dropped.",
      "F7 (NIT): production registry, with_registry kill dispatch, production_with_lease timeout, real child termination, and PID-generation mismatch are exercised without weakening the identity fence or accepting a zero-test filter.",
      "P-1 (PREREQUISITE, before F1/F2): stale_sessions excludes a null parent_session_id without aborting the scan; an embedded-Surreal regression test proves a session-less adapter row cannot suppress a genuinely reclaimable sibling session.",
      "P-2 (PREREQUISITE, before F1): the running-app reap can actually claim the target rows - either parent_session_id is populated for auth-status/preflight/login CLI children, or a claim-by-process_uuid path exists. The F1 proof asserts the exact process_uuid appears in processes_reclaimed with KillOutcome::Killed, never merely is_ok().",
      "P-3 (PREREQUISITE, before F1): SurrealProcessLedgerStore performs an exact single-row claim with all five ResourceScope predicates, and an embedded-Surreal test proves a sibling row's reclaim metadata is unchanged.",
      "P-4 (PREREQUISITE, before F2): the periodic restart pass cannot kill a live instance's healthy children - the UDP lease is not released before process exit (or only after proving zero open rows), the dead-owner probe requires two samples at least one scan interval apart, and the claim path carries an explicit owner_runtime_instance_id predicate.",
      "P-5: the reclaim hook is invoked only from record_stop's two branches and the terminate_and_collect reap failure, never from Drop; it runs after the STOP permit is released, via the wait_stop_durability_blocking thread + current_thread-runtime pattern, under an explicit timeout well below 30s, and guards lifecycle.is_none().",
      "P-6 (PROOF HONESTY, applies to every proof command in this MT): each --exact proof asserts a non-zero executed-test count or greps --nocapture output for the test name, because cargo exits 0 when an --exact filter matches nothing."
    ],
    "proof_commands": [
      "$env:CARGO_TARGET_DIR = 'D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\Handshake\\_Artifacts\\mt019-v6-target'; cargo test --locked -j 1 --manifest-path 'D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\wtc-multi-model-orchestration-v1\\src\\backend\\handshake_core\\Cargo.toml' --features test-utils,surreal-test-support --test process_ledger_surreal_tests -- --nocapture",
      "$env:CARGO_TARGET_DIR = 'D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\Handshake\\_Artifacts\\mt019-v6-target'; cargo test --locked -j 1 --manifest-path 'D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\wtc-multi-model-orchestration-v1\\src\\backend\\handshake_core\\Cargo.toml' --features test-utils,surreal-test-support --test process_reclaim_real_lifecycle_surreal_tests -- --nocapture",
      "$env:CARGO_TARGET_DIR = 'D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\Handshake\\_Artifacts\\mt019-v6-target'; cargo test --locked -j 1 --manifest-path 'D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\wtc-multi-model-orchestration-v1\\src\\backend\\handshake_core\\Cargo.toml' --features test-utils,surreal-test-support --test process_runtime_owner_identity_surreal_tests -- --nocapture",
      "$env:CARGO_TARGET_DIR = 'D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\Handshake\\_Artifacts\\mt019-v6-target'; cargo test --locked -j 1 --manifest-path 'D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\wtc-multi-model-orchestration-v1\\src\\backend\\handshake_core\\Cargo.toml' --features test-utils,surreal-test-support --test restart_resume_surreal_tests -- --nocapture",
      "$env:CARGO_TARGET_DIR = 'D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\Handshake\\_Artifacts\\mt019-v6-target'; cargo test --locked -j 1 --manifest-path 'D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\wtc-multi-model-orchestration-v1\\src\\backend\\handshake_core\\Cargo.toml' --features test-utils,surreal-test-support --test restart_resume_boot_bound_surreal_tests -- --nocapture",
      "git diff --check -- .GOV/task_packets/WP-1-Multi-Model-Orchestration-Lifecycle-Telemetry-v1/MT-019.json"
    ],
    "risk_if_missed": "A live owner can be misclassified and killed, an orphan can remain open without retry, a false STOP can be recorded, or a registry consumer can widen ownership because it lacks an exact-scope canonical inspection boundary.",
    "reference_only_paths": []
  },
  "implementation_notes": [
    "F1 current source: the official-CLI reap-failure hook calls run_owned_process with the exact five-field ReclaimResourceScope, process_uuid, and runtime_instance_id after releasing the STOP permit. It does not relax stale-session selection.",
    "F1 DO NOT relax reclaim.rs stale_sessions None=>false to true: the row carries no liveness evidence and is owned by the LIVE instance, so that would kill healthy in-flight official-CLI children. A negative test must prove a live same-instance child is never killed.",
    "F2 current source: production_with_lease runs the bounded boot reconcile inline, then spawn_managed_staleness_reclaim_task_after_boot skips only the duplicate immediate pass; every periodic tick re-runs the shared restart reconcile before stale-lane reclaim.",
    "F3 current source and operator decision: resilient boot is fail-open only for a kill refusal/failure, with the row open, claim released, fence cleared, no false STOP, processes_kill_failed incremented, warning surfaced, and periodic retry. Store/surfacing errors and the outer boot timeout fail closed.",
    "F4 current composition: main.rs startup-recovery-only constructs ProcessReclaimRuntime::production from surreal_storage.clone(), reads boot_reconcile_report, drains the reclaim task and writer, then returns. The source gap is closed; production-branch runtime proof remains parent-owned.",
    "F5 current source: RestartOrphanBootReconcileReport counts only Killed or KilledPendingStop as reclaimed, counts Failed separately, and ProcessReclaimRuntime retains and surfaces the boot report.",
    "F6 current source: boot and periodic restart reconciliation share reconcile_restart_orphans with caller-selected abort/log-and-continue policy, cancellation, and explicit sweep reclaim-error reporting.",
    "F7 source coverage now includes production_process_sandbox_registry, ProductionSandboxKill::with_registry, a real-process identity-fence proof, and ProcessReclaimRuntime::production_with_lease timeout behavior; execution remains parent-owned.",
    "HBR-MAN-004 current source: user_manual/seed.rs documents restart_sessions, bounded boot reconcile, periodic retry, exact owner/process-set fencing, running-app reclaim, fail-open kill failure, recovery-only composition, and lease retention. Runtime/manual gate execution remains pending.",
    "NO MIGRATION REQUIRED: processes_kill_failed is an in-memory struct field; nothing in F1-F7 changes a table."
  ],
  "required_build_rules": [
    "HBR-INT-001",
    "HBR-INT-005",
    "HBR-INT-009",
    "HBR-MAN-004",
    "HBR-QUIET-003",
    "HBR-PRIV-005"
  ],
  "hbr_obligations": [
    "INT",
    "MAN"
  ],
  "handoff": {
    "coder_session": "KERNEL_BUILDER-20260902-180244",
    "wp_validator_session": null,
    "post_implementation_adversarial_review_required": true,
    "post_implementation_review_lenses": [
      "process-ownership kill safety / no-wrong-kill",
      "boot availability and fail-open vs fail-closed blast radius",
      "running-app reap window safety against live children",
      "doc-vs-code honesty (fail-open claims, counter semantics)",
      "HBR-QUIET-003 process ownership and orphan reclamation"
    ]
  },
  "red_team": {
    "required": true,
    "profile": "process-ownership kill safety and boot availability",
    "risks": [
      "F1 running-app reap kills a LIVE same-instance official-CLI child because the ledger row carries no liveness evidence",
      "F3 fail-closed would brick boot permanently off-Windows where the native reclaim adapter always returns Failed",
      "Weakening the pid + creation-time + exe-sha256 identity fence to force a kill, turning a pid-reuse victim into a wrong kill",
      "Writing a STOP that was never proven, producing false durable evidence",
      "F5 counter change silently altering an existing consumer"
    ],
    "minimum_controls": [
      "A negative runtime proof shows a live same-instance official-CLI child is never killed by the new running-app reap path",
      "The pid-reuse guard proof (pid_reuse_guard_refuses_to_kill_a_mismatched_generation) stays green and still yields KillOutcome::Failed with the row left open",
      "No STOP is written on any Failed outcome; the claim is released and the fence cleared",
      "processes_kill_failed is proven to count Failed while processes_reclaimed counts only Killed and KilledPendingStop",
      "Boot completes (does not Err) on an un-reapable orphan, per the recorded F3 operator decision"
    ]
  },
  "handshake_compatibility": {
    "native_rust_backend_first": true,
    "durable_authority": "kernel_process_lifecycle (ProcessOwnershipLedger) + EventLedger in the injected embedded SurrealStorage namespace/database",
    "rust_only_implementation": "Rust backend plus embedded SurrealDB provider; no relational compatibility path.",
    "spec_realism_gate": "Every F1-F7 proof must exercise the production Surreal reclaim provider on the same injected storage clone. F1, F3, and F7 additionally require the production composition/registry and a real spawned child where the identity fence is the acceptance subject."
  },
  "validator_verdict": null,
  "remediation_v6_2026_09_01": {
    "status": "BLOCKED_ON_DEPENDENCY",
    "storage_authority": "One injected embedded SurrealStorage clone shared by ProcessLedger writes, reclaim claims, stale/restart selection, production kill identity reads, and STOP closure.",
    "source_changes": [
      "Retired the obsolete relation-backed MT-019 integration targets.",
      "Added process_reclaim_real_lifecycle_surreal_tests with exact five-field scope bindings on every durable fixture read/mutation.",
      "Added one-field mismatch, missing-scope veto, null-session continuation, sibling non-mutation, all-process ownership, foreign-owner preservation, set-drift, two-sample dead-owner, STOP-reservation rejection, bounded kill timeout, crash-after-kill no-rekill recovery, concurrent one-kill/one-STOP fencing, production-runtime fail-open reporting, periodic retry, production-runtime timeout, real-process kill, and pid-generation mismatch probes.",
      "Renamed the active stored-pid conversion helper so the production Surreal path carries no relational compatibility naming."
    ],
    "proof_not_run": "Operator prohibited Cargo/build/test/clean in this contract-only lane. JSON parsing, active-surface relational-symbol scans, source/test name inspection, and contract diff checks are the only current evidence.",
    "external_blockers": [
      "Parent must run every focused target with -j 1 under D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\Handshake\\_Artifacts and independently inspect non-zero runtime output.",
      "ProcessLedger provider owner must add same-store read-only exact-scope process-UUID and latest-by-artifact inspection, including stopped rows and canonical EventLedger verification, for the registry API lane.",
      "ProcessLedger test owner must add a real storage shutdown/reopen proof over the same namespace/database and a receipt-without-lifecycle counterfactual; source-level clone sharing and transaction text are not substitutes.",
      "Parfit/main owner must provide production-branch runtime evidence for startup-recovery-only reconcile and ordered reclaim/writer drain before embedded-store close; the composition is present in source but unexecuted."
    ],
    "required_provider_api": {
      "status": "MISSING",
      "owner": "UNASSIGNED_PROCESS_LEDGER_PROVIDER_OWNER",
      "consumer": "registry API lane",
      "projection": "pub struct ProcessOwnershipInspection containing process_uuid, model_artifact_sha256, started_at, stopped_at, stop_reason, runtime_owner, exact ResourceScope attribution, and canonical event_ledger_event_id",
      "signatures": [
        "pub async fn inspect_ownership_by_process_uuid(&self, resource_scope: &ReclaimResourceScope, process_uuid: Uuid) -> Result<Option<ProcessOwnershipInspection>, ProcessLedgerError>",
        "pub async fn inspect_latest_ownership_by_artifact(&self, resource_scope: &ReclaimResourceScope, model_artifact_sha256: &str) -> Result<Option<ProcessOwnershipInspection>, ProcessLedgerError>"
      ],
      "semantics": [
        "Run against self.storage; no second store, in-memory authority, or compatibility provider.",
        "Bind owner_account_id, actor_principal_id, authenticated_session_id, access_space_id, and workspace_id on every lifecycle and EventLedger read.",
        "Include stopped lifecycle rows; latest-by-artifact ordering is durable and deterministic.",
        "Return None only when no exact-scope row exists; malformed scope, duplicate canonical process identity, ambiguous latest identity, missing receipt, or mismatched EventLedger linkage returns ProcessLedgerError without widening or partial data."
      ]
    },
    "readiness_rule": "Do not set READY_FOR_VALIDATION_V6 until all external blockers are closed and every focused target executes a non-zero test count with no failures."
  },
  "v6_contract_reaudit_2026_09_01": {
    "status": "BLOCKED_ON_DEPENDENCY",
    "risk_tier": "HIGH",
    "evidence_scope": "Contract-only read-through of current product source and tests; no product/test edits and no Cargo/build/test/clean execution.",
    "historical_v5_evidence_policy": "All 2026-07/08 implementation, proof, diagnosis, waiver, and integration-validation objects below are provenance-only. Their relation-backed commands, fixtures, statuses, commits, and verdicts are superseded and are not active V6 requirements, proofs, readiness, or validator evidence.",
    "source_complete": [
      "SurrealProcessLedgerStore writes lifecycle projection plus canonical kernel_event_ledger receipt in one transaction, verifies final receipt/projection identity, and rejects identity or verification mismatch before commit.",
      "Every active reclaim mutation and restart-resume query binds all five ReclaimResourceScope fields; malformed, partial, mixed-scope, stale-claimant, set-drift, foreign-owner, and current-instance rows are denied without widening.",
      "main.rs opens one SurrealStorage and passes clones to SurrealRestartResumeRunner, ProcessReclaimRuntime, AppState, model registry, and downstream API composition; ProcessReclaimRuntime reuses clones for writer, killer, stale source, reclaim, and open-row lease-release proof.",
      "ProcessReclaimRuntime runs bounded boot reconciliation, retains its report, starts periodic retry, and shutdown_and_drain quiesces reclaim before draining the writer; main requires those outcomes before dropping the final AppState owner and returning.",
      "The renamed Surreal tests cover exact scope, idempotent START/STOP, projection tamper rollback, single-row/sibling safety, concurrency, stale claimant fencing, bounded kill, crash-after-kill recovery, live-owner denial, fail-open kill reporting, fail-closed timeout, restart-resume idempotency, interleaved-event scope isolation, and bounded-abort evidence."
    ],
    "DIFF_ATTACK_SURFACES": [
      "Uncommitted ProcessLedger provider/schema and reclaim/runtime/restart-resume source in the product worktree.",
      "Uncommitted renamed embedded-Surreal integration targets and their shared injected-storage harness.",
      "Shared main/API composition ordering and ownership-clone lifetime.",
      "Canonical EventLedger receipt/projection atomicity and deterministic process identity.",
      "New registry-lane demand for read-only same-store ownership inspection, which current public provider APIs do not expose.",
      "Current V6 fields versus retained V5 governance provenance in this contract."
    ],
    "INDEPENDENT_CHECKS_RUN": [
      "Static scan of the inspected active production/provider/schema/reclaim/restart files and four renamed targets found zero PostgreSQL, SQLite, sqlx, PgPool, PostgresDatabase, psql, DATABASE_URL, ON CONFLICT, or migration-0310 symbols.",
      "Public API scan found SurrealProcessLedgerStore::new, open, and storage, but no exact-scope read-only process-UUID or latest-by-artifact ownership inspection method.",
      "Test-function inventory resolved the named exact-scope, concurrency, stale/mixed denial, bounded, real-process, runtime-owner, restart-resume, and idempotency probes to concrete current Rust functions.",
      "Source trace confirmed main startup-recovery-only and normal boot both pass surreal_storage.clone() into ProcessReclaimRuntime::production; restart-resume uses the same clone before either branch.",
      "Source trace confirmed APPLY_EVENT_BATCH brackets receipt and lifecycle projection writes with BEGIN/COMMIT TRANSACTION and verifies both before commit."
    ],
    "COUNTERFACTUAL_CHECKS": [
      "Identical committed START and STOP retries are accepted idempotently; a changed immutable identity or tampered final lifecycle is rejected and the transaction rolls back.",
      "A canonical receipt pre-existing without its lifecycle projection is rejected by provider transaction text, but no dedicated runtime test currently seeds that orphan-receipt state; proof remains owed.",
      "Changing one ResourceScope field returns no row or rejects the mutation; missing all five scope fields fails before storage access.",
      "Changing claimant UUID/generation cannot release or advance a newer claim; changing PID generation prevents kill and leaves STOP absent.",
      "A same session with foreign-owner or non-sandbox siblings cannot widen the selected process set; process-set drift vetoes the whole claim."
    ],
    "BOUNDARY_PROBES": [
      "START, STOP, reclaim, restart selection, kill identity, receipt, and shutdown lease proof all reach the injected embedded Surreal provider rather than a relational compatibility path.",
      "Within one process lifetime, source and fixtures use cloned handles for the same SurrealStorage namespace/database.",
      "No inspected target proves shutdown then reopen of that same namespace/database while preserving lifecycle, receipt, and restart/reclaim identity; this remains a runtime boundary gap.",
      "main source orders restart-resume before runtime exposure and runtime drain before final owner drop/return, but the production branch has no fresh execution evidence.",
      "Latest-by-artifact ownership inspection, including stopped rows, is not currently a public same-store provider boundary."
    ],
    "NEGATIVE_PATH_CHECKS": [
      "Missing/invalid one-field scope, incomplete scope, mixed scope, null session, foreign owner, non-sandbox sibling, current live owner, stale claimant, changed authorized process set, STOP reservation rejection, unresponsive kill, kill failure, boot timeout, PID-generation mismatch, and projection tamper were mapped to fail-closed source/test paths.",
      "Kill failure leaves the row open and retryable without a false STOP; only store/surfacing errors and bounded boot timeout abort startup.",
      "Drain failure or an unproven nonzero open-row count retains the OS liveness lease rather than advertising a live owner as dead."
    ],
    "INDEPENDENT_FINDINGS": [
      "The stale F4 composition blocker is closed in current source and was removed from active contract state.",
      "The current source/test boundary is embedded-Surreal-only and substantially source-complete, but mandatory runtime evidence is absent.",
      "The registry lane's required read-only ProcessLedger inspection is a real missing public API, not a composition-only issue.",
      "Same-handle clone sharing is proven statically; durable shutdown/reopen continuity and orphan-receipt retry behavior are not runtime-proven by the inspected targets.",
      "BLOCKED_ON_DEPENDENCY is the only honest lifecycle state; READY and validator verdict fields remain clear."
    ],
    "RESIDUAL_UNCERTAINTY": [
      "No compiler or runtime verdict exists for the dirty concurrent product worktree.",
      "Windows real-process timing and identity lookup remain host-load-sensitive until the focused target runs under the serialized slot.",
      "The production startup-recovery-only and graceful-shutdown branches have source evidence only.",
      "The proposed ownership-inspection projection and method names require implementation/consumer agreement, but the exact scope, same-store, stopped-row, duplicate, malformed, and EventLedger semantics are mandatory."
    ]
  },
  "operator_decisions": [
    {
      "id": "F3-BOOT-KILL-FAILURE-POLICY",
      "decided_at_utc": "2026-07-30T00:00:00Z",
      "decided_by": "OPERATOR",
      "question": "When boot cannot reap a spawned-process orphan (kill fails), should boot fail open or fail closed?",
      "decision": "RESILIENT_BOOT_FAIL_OPEN_PLUS_SURFACE_PLUS_RETRY",
      "rationale": "The Failed path is already truthful and idempotently retryable (claim released, fence cleared, no false STOP). Fail-closed would permanently brick boot off-Windows because the native reclaim adapter is Windows-only and always returns Failed, and it cannot distinguish a correct refusal-to-kill from a genuine reap failure.",
      "implements_acceptance_criterion": "F3 / acceptance_criteria[2]"
    }
  ],
  "contract_repair_2026_07_30": {
    "repaired_by": "KERNEL_BUILDER-20260729-225554",
    "reason": "MT-019 was authored as a copy-paste hybrid: owned_files, allowed_paths, forbidden_paths[2], implementation_notes, red_team, review lenses, and handshake_compatibility.durable_authority all carried MT-018 CRDT text unrelated to the reclaim subject, and proof_commands was empty.",
    "preserved_verbatim": [
      "scope.summary",
      "scope.root_cause",
      "scope.acceptance_criteria",
      "scope.risk_if_missed"
    ],
    "operator_disclosure_required": true
  },
  "pre_implementation_adversarial_review_2026_07_30": {
    "performed_by": "KERNEL_BUILDER-20260729-225554 (delegated read-only reviewer, findings verified against cited source)",
    "verdict": "PLAN_REQUIRES_REWORK_BEFORE_IMPLEMENTATION",
    "summary": "The F1/F2 plan as written cannot be honestly implemented or proven, and F2 as written introduces a continuous wrong-kill exposure across parallel Handshake instances. Three blockers must land BEFORE the F1 hook is wired. Sequencing changed: P-1, P-2, P-3 are prerequisites, not follow-ups.",
    "blocking_findings": [
      {
        "id": "P-1",
        "severity": "CRITICAL",
        "title": "stale_sessions panics on NULL parent_session_id and permanently kills the staleness task",
        "detail": "reclaim.rs:4065 decodes parent_session_id with the panicking accessor row.get::<String>, and the query at reclaim.rs:4040-4043 has no IS NOT NULL filter. The column is nullable (migration 0021_kernel_process_lifecycle.sql:8). A real production path writes exactly such a row: access_config.rs:389-408 (auth_status_invocation) sets session_id but never parent_session_id, while still carrying sandbox_adapter_id and the live owner_runtime_instance_id. Opening Settings spawns that CLI child; the 30s staleness tick then hits UnexpectedNullError inside the tokio::spawn task (reclaim.rs:4312) and the periodic reclaim task is silently dead for the rest of the process lifetime. If the row stays open, every subsequent boot re-arms the panic.",
        "why_it_blocks": "F1's target row class (official-CLI orphan with no model_lanes record) IS the NULL-parent_session_id class, and F2's deliverable is a periodic tick on the very task that dies. The feature would be dead on arrival in the exact case it exists for.",
        "required_fix": "Skip a null parent_session_id without aborting the scan; add an embedded-Surreal regression test with an adapter-owned, self-owned, session-less open row plus a genuinely reclaimable sibling; keep the periodic task resilient to scan failures."
      },
      {
        "id": "P-2",
        "severity": "CRITICAL",
        "title": "run_process claims by parent_session_id, which is None for exactly the rows F1 targets",
        "detail": "run_process filters WHERE parent_session_id = $1 (reclaim.rs:2790, SQL at reclaim.rs:49). For swarm lanes parent_session_id is the shared coordinator session (production_factory.rs:1794-1806); for auth-status/preflight CLI children it is None (access_config.rs:389-408). Passing None or an empty string matches zero rows, so run_process returns an empty report and the test goes green while nothing was reclaimed. Those rows are also invisible to restart_sessions, which requires parent_session_id IS NOT NULL (reclaim.rs:3945-3946), so they are not reaped at next boot either.",
        "why_it_blocks": "F1's acceptance criterion cannot be met, and the natural test (assert is_ok) would be a false green.",
        "required_fix": "Either populate parent_session_id in the auth-status/preflight/login contexts, or add a store method that claims by process_uuid alone. The F1 proof MUST assert report.processes_reclaimed contains the exact process_uuid with KillOutcome::Killed - never merely is_ok()."
      },
      {
        "id": "P-3",
        "severity": "HIGH",
        "title": "run_process mass-claims every sibling row of the parent session and can corrupt a concurrent reclaim",
        "detail": "The retired relational store lacked an exact active_process_for_session override and transiently claimed the whole session set, allowing a targeted running-app reap to perturb sibling reclaim generations. The Surreal provider must claim the one exact process row with all five ResourceScope fields.",
        "why_it_blocks": "Today run_process is called only on rare failure paths; F1 puts it on EVERY CLI teardown that cannot prove a STOP, turning a rare race into a routine one.",
        "required_fix": "Implement an exact single-row Surreal claim binding process_uuid, parent_session_id, and all five ResourceScope fields. Add an embedded-Surreal test asserting sibling reclaim metadata is unchanged."
      },
      {
        "id": "P-4",
        "severity": "HIGH",
        "title": "F2's periodic restart pass can kill another live Handshake instance's healthy children",
        "detail": "restart_sessions infers 'prior owner is dead' by binding its ephemeral loopback UDP port (reclaim.rs:3993-4016, try_claim_udp_lease at :405-425). That lease is released while the host process is still alive, at runtime.rs:172-177 (shutdown_and_drain) and runtime.rs:199-201 (Drop). The Tauri shell builds its own ProcessReclaimRuntime (app/src-tauri/src/commands/swarm_runtime.rs:494) and calls shutdown_and_drain (:1209) while continuing to run with live official-CLI children and open rows. The backend's restart pass then probes the freed port, concludes the owner is dead, and kills those live healthy children. The identity fence gives NO protection: it proves the right process generation, not liveness or ownership, so it matches perfectly and the kill proceeds.",
        "why_it_blocks": "Boot-only reclaim hits this window at most once per boot. A 30s periodic tick hits it roughly 2880 times per day. The Operator runs parallel Handshake sessions, so this is a live exposure, not a theoretical one.",
        "required_fix": "Do not release the UDP lease before process exit (or release only after proving zero open rows for the instance); require the dead-owner probe to observe the port free across two samples separated by at least one scan interval; and add an explicit owner_runtime_instance_id != self predicate inside the claim SQL, which today has no owner filter at all (reclaim.rs:49-63) - the veto lives only in restart_sessions, so any future caller of Reclaim::run bypasses it."
      },
      {
        "id": "P-5",
        "severity": "HIGH",
        "title": "The Drop path cannot safely call an async reclaim; hook record_stop and terminate_and_collect only",
        "detail": "leave_open_for_reconciliation is a sync fn (official_cli_bridge.rs:2256). Handle::current().block_on from Drop during unwind is a double panic -> abort; futures::executor::block_on on a tokio worker stalls it and panics without a runtime context, and Reclaim needs a real runtime (tokio::spawn reclaim.rs:3113, interval/sleep :3097/:3107, spawn_blocking handshake_native.rs:472). The only safe in-file precedent is wait_stop_durability_blocking (official_cli_bridge.rs:1594-1620): thread::spawn + current_thread runtime + block_on + join, mapping join panic to an error. Cost is real: RECLAIM_KILL_TIMEOUT is 30s (reclaim.rs:2086) plus a 5s stop-ack, and auth_status is a sync trait method (access_config.rs:310-340) whose axum caller would block a worker ~35s.",
        "why_it_blocks": "A naive implementation aborts the process or stalls the runtime. Also an ordering hazard: if the reclaim is called before lifecycle.leave_open_for_reconciliation() at :2261, the guard still holds its STOP permit and run_claimed aborts at reclaim.rs:2808-2818 on a saturated writer, reclaiming nothing.",
        "required_fix": "Call reclaim only AFTER the permit is released; use the wait_stop_durability_blocking thread + current_thread-runtime pattern; apply an explicit timeout well under 30s; and do NOT hook Drop (:2320, :2325) - hook only record_stop's two branches (:2240, :2251) and the terminate_and_collect reap failure (:2282), leaving Drop cases to the periodic pass. Guard lifecycle.is_none(): the :2320 branch is reachable only when no START row exists (early return :2227-2229), so there is no process_uuid to reclaim."
      }
    ],
    "additional_findings": [
      "STOP truthfulness race: on the DurabilityUnconfirmed branch the true STOP with the real exit code may still commit later (writer.rs:501-504) while a reclaim writes exit_code -1 / stop_reason 'reclaim' (reclaim.rs:2296-2328). Decide which STOP wins and test the late-commit interleaving.",
      "ForegroundChildOwner (official_cli_bridge.rs:1663-1792) has its own leave_open_for_reconciliation at :1702 reached with the child possibly still alive, and its Drop deliberately mem::forgets child + lifecycle while the process may be live (:1780-1789). F1 does not touch it, so the login path keeps producing open rows for LIVE processes - precisely what P-4 can then kill. Declare it an explicit non-goal or cover it.",
      "HBR-QUIET-003 gap: ownership metadata is preserved into the STOP (reclaim.rs:2331-2336) but there is NO ownership predicate anywhere in the kill path. Add owner_runtime_instance_id = self on the running-app hook's claim path so the in-app reaper structurally cannot touch another instance's process.",
      "F5 surfacing caveat: production_with_lease's return shape is consumed by two composition roots (main.rs:447 and app/src-tauri/src/commands/swarm_runtime.rs:494); the Tauri path is under forbidden_paths. Prefer tracing::warn! plus a field on ProcessReclaimRuntime read by an existing backend surface over changing the return shape. Also model_manual/content.rs:566-572 documents processes_reclaimed but is not in allowed_paths."
    ],
    "proof_honesty_corrections": [
      "CROSS-CUTTING FALSE-PASS HAZARD: `cargo test --test X <name> -- --exact` exits 0 when the name matches NOTHING (0 passed / N filtered out). Every --exact proof MUST assert a non-zero executed-test count or grep the --nocapture output for the test name. Applies to this MT and to every other MT in this packet.",
      "mt019_running_app_reclaims_...: cannot prove F1 from tests/ because GuardedCliChild is private (official_cli_bridge.rs:2157); an integration test could only call Reclaim::run_process directly, which re-proves MT-007 rather than the new hook. Needs an in-crate test or a cfg(feature=test-utils) seam. NOTE this collides with handshake_compatibility.spec_realism_gate, which forbids mock/fixture-only proof for F1 - resolve in the contract before coding.",
      "mt019_running_app_reap_never_kills_a_live_same_instance_child: will FAIL if written against run_process, because the identity fence matches a live child and the kill proceeds. The negative proof must target the SURFACING layer - assert stale_sessions/restart_sessions do not return the live same-instance session, and that the bridge does not invoke the hook on healthy exit paths.",
      "mt019_production_with_lease_boot_is_fail_open_...: cannot cover the non-Windows unavailable-adapter path named in F3 because the test file is #![cfg(windows)] (line 36). Honest Windows analogue: ProductionSandboxKill::with_registry with a registry lacking HandshakeNative, so owning_adapter errors at reclaim.rs:2539-2547 and yields the identical KillOutcome::Failed shape with the row left open.",
      "mt019_production_with_lease_boot_timeout_...: provable with a ~1ns startup_timeout, but runtime.rs:124 mem::forgets the lease, permanently leaking a UDP socket in the test process. Use a distinct host scope and document the leak.",
      "mt019_boot_reconcile_surfaces_in_progress_sweep_reclaim_error: SOUND - process_ledger_reclaim_tests.rs has no file-level cfg gate and uses a mock SandboxKill, so it runs everywhere."
    ],
    "confirmed_sound": [
      "F3 resilient-boot decision is well-founded on the code: run_claimed on Err releases the claim, clears the fence, and writes NO STOP (reclaim.rs:2891-2903); the adapter is genuinely Windows-only (handshake_native.rs:229-235); production_with_lease really would abort boot on any reconcile error (runtime.rs:117-127).",
      "F5 counter split is low risk: zero read sites, no Serialize derive, derive(Default) means no other construction edit (reclaim.rs:4148, :4181).",
      "F5 doc corrections are warranted - reclaim.rs:4154 names a non-existent 'fenced-closed' outcome, :4173-4176 claims fail-closed semantics that do not hold, runtime.rs:104-107 overclaims coverage.",
      "The instruction NOT to relax stale_sessions None=>false to true is correct and important.",
      "F4 and F7 factual claims verified as stated.",
      "NO MIGRATION REQUIRED holds, including for the P-1 fix."
    ]
  },
  "implementation_progress_2026_07_30": {
    "claimed_by": "KERNEL_BUILDER-20260729-225554",
    "operator_scope_expansion": "Operator expanded scope 2026-07-30 authorising P-1 to land immediately as its own fix rather than waiting for the rest of MT-019.",
    "P-1": {
      "status": "PROVEN_AND_COMMITTED",
      "product_change": "process_ledger/reclaim.rs stale_sessions: added AND lifecycle.parent_session_id IS NOT NULL to the WHERE clause, and replaced the panicking row.get::<String>(\"parent_session_id\") with try_get::<Option<String>> plus an explicit skip and tracing::warn. Comment records why a session-less row cannot participate in a session-level reclaim decision.",
      "regression_test": "Superseded V5 relational regression; V6 replacement is process_reclaim_real_lifecycle_surreal_tests::mt019_stale_source_skips_null_session_without_aborting_valid_sibling.",
      "verified_defect_evidence": [
        "reclaim.rs:4065 used the panicking accessor while the line below already used try_get for record_json",
        "query at reclaim.rs:4040-4043 had no parent_session_id IS NOT NULL filter",
        "migration 0021_kernel_process_lifecycle.sql:8 declares parent_session_id TEXT (nullable)",
        "model_runtime/cloud/access_config.rs:398 sets context.session_id but never parent_session_id",
        "restart_sessions at reclaim.rs:3958 already used try_get::<Option<String>> correctly, so stale_sessions was the sole outlier",
        "active_processes_for_session row.get at reclaim.rs:3349 is safe: its query binds WHERE parent_session_id = $1"
      ],
      "proof_pending_reason": "Build deferred while the MT-018 coder was editing the same crate; an interleaved build would compile its in-progress state and prove nothing. One combined build/proof runs once MT-018 lands.",
      "commit": "c043ee9c",
      "proof_verbatim": "SUPERSEDED_V5_RELATIONAL_RESULT_NOT_ACCEPTED_FOR_V6",
      "proof_note": "The V5 result was produced by a retired storage path and is retained only as historical context. It provides no V6 acceptance evidence.",
      "leak_fix_included": "The retired relation-backed fixture registered each isolated schema at creation and dropped it at process exit, closing the leak that had accumulated 532 schemas and degraded a single migration chain from seconds to hours."
    },
    "P-2_P-3_P-4_P-5": {
      "status": "PROVEN_AND_COMMITTED",
      "commit": "d154833b"
    }
  },
  "implementation_2026_07_30": {
    "implemented_by": "KERNEL_BUILDER-20260729-225554 (delegated coder; plan reworked by pre-implementation adversarial review, diff reviewed by parent)",
    "commit": "d154833b",
    "proved_at_runtime": [
      "A live same-instance official-CLI child is never surfaced or killed (negative proof at the surfacing layer, both children alive, rows still open).",
      "Sibling reclaim_claim stop_reason + metadata_jsonb byte-identical across a single-row reclaim of a different process_uuid in the same session.",
      "Two-sample dead-owner gate: nothing surfaced inside the gap, surfaced across the gap, a live lease-holder never surfaced across three samples.",
      "Running-app reap of a NULL-parent_session_id self-owned orphan asserted by exact process_uuid with KillOutcome::Killed, real child death, durable STOP.",
      "production_with_lease is fail-open on an un-reapable orphan: processes_reclaimed=0, processes_kill_failed=1, child alive, row open, no STOP.",
      "Boot timeout fails closed; the UDP lease is retained while self-owned rows are open and released once they close."
    ],
    "argued_not_proved": [
      "record_stop Recorded/AlreadyStopped branches cannot reach the hook (structural).",
      "Boot pass and periodic tick share one observation map (same Arc, by construction)."
    ],
    "not_covered": [
      "The non-Windows F3 unavailable-adapter path: the proof file is #![cfg(windows)]. The Windows analogue (registry lacking HandshakeNative) was used and the limitation is stated rather than implied covered."
    ],
    "behaviour_change": "With the default 30s corroboration gap a crash orphan is now normally reaped ~30s AFTER boot by the periodic tick rather than during boot. This is the deliberate P-4 trade that prevents one instance killing another instance live children; F2 makes it whole.",
    "cross_lane_consequence": "swarm_runtime.rs:1210 returns CoordinatorFailed when the lease is not released, so draining with open rows now reports shutdown incomplete. Truthful, but the Tauri owner should close its rows before draining or read the new lease_retained_reason. app/src-tauri is out of lane and was not modified.",
    "waiver_requested": "tests/focus_audit_tests.rs, one line, forced by ReclaimableProcess.parent_session_id -> Option<String>. Inside scope.allowed_paths but not owned_files.",
    "regression_fixed_in_same_commit": "b53f475d had added an UNGATED compile anchor on SwarmCoordinator::session_runtime, which is #[cfg(any(test, feature=\"test-utils\"))]. handshake_core therefore failed to build with DEFAULT features (E0599), breaking every dependent crate including the Tauri shell. It escaped review because verification used --features test-utils only. Anchor now carries the same gate as its symbol; both feature configurations verified green."
  },
  "operator_waivers_2026_08_01": [
    {
      "id": "WAIVER-MT019-FOCUS-AUDIT-TESTS",
      "granted_by": "OPERATOR",
      "granted_at_utc": "2026-08-01T02:30:00Z",
      "scope": "src/backend/handshake_core/tests/focus_audit_tests.rs - one line",
      "why": "Forced by the ReclaimableProcess.parent_session_id -> Option<String> type change required by P-1/P-2. The file is inside scope.allowed_paths (tests/) but was not listed in scope.owned_files.",
      "disclosure": "Single line: parent_session_id: Some(...). No behavioural change to the test; it only satisfies the new Option type.",
      "status": "GRANTED"
    },
    {
      "id": "WAIVER-MT019-TAURI-SHUTDOWN-SEMANTICS",
      "granted_by": "OPERATOR",
      "granted_at_utc": "2026-08-01T02:30:00Z",
      "scope": "Behaviour change observable in app/src-tauri/src/commands/swarm_runtime.rs:1210 (NOT modified - app/src-tauri is out of lane)",
      "why": "P-4(a) stops the UDP runtime lease being released while the owning process is still alive, which is what allowed one Handshake instance to conclude another was dead and kill its healthy children. As a consequence, draining while the instance still owns open rows now yields lease_released=false, so that call site reports CoordinatorFailed / shutdown incomplete where it previously reported Completed.",
      "disclosure": "The new report is TRUTHFUL - shutdown genuinely is incomplete while open rows remain. The Tauri owner should either close its rows before draining or read the new lease_retained_reason. Note the Tauri stack is superseded per Codex CX-009-VIS and frozen since 2026-07-23, so the blast radius is the legacy shell.",
      "status": "GRANTED"
    }
  ],
  "v5_proof_recheck_2026_08_05": {
    "headline": "This MT is recorded READY_FOR_VALIDATION_V5, but TWO of its declared proofs FAIL right now and reproduce in isolation. Recorded rather than quietly left, because the recorded status currently overstates the evidence.",
    "results": {
      "process_ledger_reclaim_tests": "31 passed, 0 failed (GREEN)",
      "retired_relation_backed_real_lifecycle_suite": "9 passed, 3 failed"
    },
    "load_vs_logic_separated": {
      "method": "The three failures were re-run ONE AT A TIME with --test-threads=1, because this family spawns real OS processes and was already shown earlier in this packet to be load-sensitive (31/0 at 175s under light load versus 617s and 1404s under load).",
      "mt019_production_with_lease_boot_is_fail_open_on_unreapable_orphan_and_reports_kill_failed": "PASSES alone in 43.7s. Its batch failure was 'process reclaim boot reconciliation exceeded 30000 ms' - a LOAD-INDUCED timeout, not a defect. The 30s budget is simply not survivable on a host running several work packets at once.",
      "mt019_post_boot_staleness_task_resurfaces_restart_orphan": "FAILS alone in 34.2s: 'the periodic restart tick must reclaim a restart orphan without a reboot'. GENUINE.",
      "concurrent_reclaimers_produce_exactly_one_kill_and_one_stop": "FAILS alone in 24.4s: 'exactly one reclaimer may kill + STOP the shared process (no double-STOP)' with left: 0, i.e. ZERO reclaimers acted. GENUINE."
    },
    "not_covered_by_the_waivers": "Checked. operator_waivers_2026_08_01 grants WAIVER-MT019-FOCUS-AUDIT-TESTS (a one-line Option type change in focus_audit_tests.rs) and WAIVER-MT019-TAURI-SHUTDOWN-SEMANTICS (a Tauri call-site reporting change). Neither covers these two failures; the numeric coincidence of 'two waivers / two failures' is exactly that.",
    "provenance_established": {
      "tests_are_not_new": "The retired relation-backed real-lifecycle suite was unmodified versus HEAD, and both failing test functions existed in HEAD. They were not newly-added tests that never passed.",
      "reclaim_engine_untouched": "src/process_ledger/reclaim.rs is UNMODIFIED versus HEAD.",
      "what_did_change_nearby": "src/process_ledger/writer.rs (+116) and batcher.rs (+13) are modified in the working tree by this packet's permanently-open-ledger-rows work."
    },
    "hypothesis_RAISED_AND_REFUTED": "Hypothesis: the writer hardening ('the authoritative STOP upsert only updates a lifecycle row when every immutable identity column of the STOP is byte-identical to the persisted START, otherwise the lifecycle never closes') broke the EXTERNAL reclaim path, because an external reclaimer synthesizing a STOP from its own defaults would now be rejected - which would explain zero kills. REFUTED by reading the code: ProcessReclaim::reclaim_stop builds its STOP from the PERSISTED ROW's own fields (process_uuid, os_pid, parent_session_id, parent_process_id, sandbox ids, engine_kind, started_at, owner/role/wp), not from reclaimer defaults, so it remains byte-identical on every identity column. The tidy causal story is wrong and is recorded here as refuted so nobody re-derives it.",
    "cause_NOT_established": "Deliberately left open rather than guessed. Distinguishing 'pre-existing at the HEAD this MT was closed against' from 'regressed under this packet's uncommitted changes' requires running these two tests against clean HEAD, which needs a separate worktree and a full cold rebuild. That was not self-authorized: MT-019 is already closed, ~8000 lines of uncommitted work from several lanes sit in this tree, and stashing or worktree-splitting it is an operator-scope decision, not a KERNEL_BUILDER one.",
    "recommended_next_step": "Operator decision. Either (a) authorize a clean-HEAD comparison run to classify these two as pre-existing or regression, or (b) route them to whoever owns the reclaim engine. Note also that the 30s boot-reconciliation budget is too tight for a loaded host and produces false failures independently of these two.",
    "checked_by": "KERNEL_BUILDER-20260801-214802",
    "status_note": "Status intentionally NOT changed. Downgrading an operator-closed MT is a scope decision; this record exists so the status is not silently trusted."
  },
  "failure_diagnosed_2026_08_05": {
    "headline": "DIAGNOSED. concurrent_reclaimers_produce_exactly_one_kill_and_one_stop does NOT fail on reclaim logic. The reclaimer selects the process and attempts the kill, but the PRODUCTION IDENTITY LOOKUP TIMES OUT against a hardcoded 5-second budget on a loaded host.",
    "observed": "kill = Failed { error: \"production reclaim identity lookup timed out for process 019fd03c-6425-7732-aa08-9de1362f7391\" }, stop = None. Target process_uuid matched the seeded row and the real child pid (8772) was alive.",
    "how_it_was_obtained": "The assertion previously reported only `left: 0, right: 1`, which cannot distinguish (a) the process was never selected as reclaimable, (b) it was selected but the kill was declined, or (c) it was killed but no STOP was written - three different defects with three different fixes. The assertion was made self-explaining (it now prints the target uuid, the real child pid, and every reclaimed entry with its kill_result and stop_event_kind, plus an explicit note that an empty list means it was never selected). NO assertion was weakened; only the diagnosis was made possible. The answer appeared on the first run afterwards.",
    "the_budget": "src/process_ledger/reclaim.rs uses tokio::time::timeout(Duration::from_secs(5), ...) around the identity resolution in TWO places: line ~2630 (`identity`) and line ~2780 (`kill_operation_status`). The lookup queries OS process identity and hashes the executable image, so it is I/O bound and degrades under host contention.",
    "why_this_explains_ALL_THREE_MT019_failures": "Uniform cause. mt019_production_with_lease_boot_is_fail_open_on_unreapable_orphan_and_reports_kill_failed PASSES alone in 43.7s and failed in the batch with 'process reclaim boot reconciliation exceeded 30000 ms'. The other two fail with this 5s identity timeout. All three are timing budgets that a host running several work packets plus heavy cargo cannot meet. Earlier in this packet the same family was measured at 31/0 in 175s under light load versus 617s and 1404s under load.",
    "classification": "NOT a correctness defect in reclaim logic. It IS a real robustness defect: on a loaded production host the same timeout would abandon a kill and leave an orphan, and the operator would see a reclaim that silently gave up. The current behaviour reports kill_result = Failed with a typed reason rather than lying, which is correct fail-closed behaviour - the budget is what is wrong.",
    "fix_options_NOT_self_authorized": [
      "Raise the 5s budget. Simple, but the number would be arbitrary without knowing the intended worst-case host.",
      "Make the budget configurable (env or SwarmConfig) so a loaded CI/dev host and a production host can differ, defaulting to today's 5s.",
      "Treat an identity-lookup TIMEOUT as retryable rather than terminal for that pass, so the next reclaim tick re-attempts instead of abandoning the orphan. This is the most robust option and changes no budget, but it changes reclaim's terminal semantics and needs MT-019's full proof set to validate."
    ],
    "why_not_applied": "All three options change production reclaim timing or terminal semantics on the orphan-cleanup path, and MT-019 is an already-closed MT with operator-granted waivers. Picking a budget or changing terminal semantics is an operator/owner decision, not a KERNEL_BUILDER one, and this session has already shown what happens when boundary changes land without full validation.",
    "test_improvement_retained": "The self-explaining assertion is kept. It is strictly more informative and cost nothing in strictness.",
    "diagnosed_by": "KERNEL_BUILDER-20260801-214802",
    "diagnosed_at_utc": "2026-08-05T07:45:00Z"
  },
  "integration_validation_v5_2026_08_14": {
    "verdict": "FAIL_V5",
    "validated_by": "INTEGRATION_VALIDATOR-20260814-060230",
    "validated_at_utc": "2026-08-14T06:06:48.4919021Z",
    "source_state": {
      "base_sha": "6519f4e2827840642a6c0401ac31055469f09d3b",
      "head_sha": "df463aa6459d29c82187e7284d86a0bf37575009",
      "worktree": "wtc-multi-model-orchestration-v1",
      "worktree_clean_at_validation_start": true
    },
    "authority_refs": [
      ".GOV/codex/Handshake_Codex_v1.4.md",
      ".GOV/roles/wp_validator/WP_VALIDATOR_PROTOCOL.md",
      ".GOV/roles/integration_validator/INTEGRATION_VALIDATOR_PROTOCOL.md",
      ".GOV/spec/SPEC_CURRENT.md -> master-spec-v02.203",
      ".GOV/spec/master-spec-v02.203/spec-modules/04-llm-infrastructure.md#4.3.9",
      ".GOV/roles_shared/records/HANDSHAKE_BUILD_RULES.json"
    ],
    "method": "Fresh current-source Integration Validator review using authoritative MT contract acceptance criteria, spec-realism, required HBR dispositions, exact proof-command inspection, direct runtime prerequisite probes, adversarial source tracing, and three independent advisory review lanes. Historical evidence was retained as context but not reused as current proof.",
    "fail_report": "The authoritative HBR status is PENDING and the declared git diff check fails. The contract also records unresolved current failures in periodic restart-orphan recovery and concurrent reclaimer behavior.",
    "failed_gates": [
      "HBR status PENDING",
      "declared git diff --check proof",
      "periodic restart-orphan reclaim",
      "concurrent reclaimer determinism"
    ],
    "remediation_plan": [
      "Remove the blank EOF line and pass git diff --check main...HEAD.",
      "Make the periodic restart tick reclaim skipped restart orphans and make concurrent claim/reclaim ownership deterministic without double kill or leaked START rows.",
      "Run the focused embedded-Surreal real-process, exact-scope, periodic-retry, hook, and concurrency proof set under controlled load."
    ],
    "proof_observations": [
      "The V5 validator used a retired relational endpoint; that observation is superseded and cannot satisfy V6.",
      "The shared external Cargo target was D:/Projects/LLM projects/Handshake/Handshake Worktrees/Handshake_Artifacts/handshake-cargo-target.",
      "Fresh git diff --check main...HEAD exited 2 on the blank EOF line; this is a direct declared-proof failure."
    ],
    "prior_lifecycle_proof_status": "SUPERSEDED_V5_RELATIONAL_PROOF: historical green and load-sensitive results are not reusable because the durable provider and acceptance target changed. V6 requires fresh embedded-Surreal proof on current source.",
    "adversarial_review": {
      "DIFF_ATTACK_SURFACES": [
        "Current product diff from main spans storage/migrations, model runtimes, process lifecycle, privacy boundaries, native GUI/Argus, UserManual, and live MicroVM paths.",
        "The exact current source was inspected at df463aa6459d29c82187e7284d86a0bf37575009; historical green results were treated as non-authoritative for materially changed surfaces."
      ],
      "INDEPENDENT_CHECKS_RUN": [
        "git diff --check main...HEAD was run independently and exited 2 on src/backend/handshake_core/tests/pidless_cloud_lifecycle_harness/Cargo.toml:14.",
        "All 105 named exact test filters declared by the ready MT contracts were statically resolved to non-empty Rust test sources; zero missing filters were found.",
        "Three fresh advisory reviewers independently read current authority, contracts, spec, source, and runtime prerequisites for MT-002..008, MT-009..015, and MT-016..023."
      ],
      "COUNTERFACTUAL_CHECKS": [
        "Historical green output was not accepted as proof of current HEAD.",
        "Deterministic proxies were not accepted where the contract requires a production registry, real model, real process, or KVM boundary.",
        "A missing proof or partial/pending HBR row was not converted into PASS merely because source inspection looked fail-closed."
      ],
      "BOUNDARY_PROBES": [
        "Account/session/access-space ownership and derived-resource non-widening were checked at ModelLane, promotion, ContextBundle, cloud launch, recovery, navigation, embedding/Loom, and proposal lifecycle boundaries.",
        "Production GUI proof was distinguished from headless Harness/RecordingBackend proxies, and live MicroVM proof was distinguished from deterministic registry coverage."
      ],
      "NEGATIVE_PATH_CHECKS": [
        "Cross-account, revoked-context, missing-scope, mixed-source, and unavailable-live-resource paths were required to fail closed without metadata leakage or authority mutation.",
        "Exact unavailable prerequisites were observed for Argus/Palmistry, Candle model artifacts, and KVM where applicable."
      ],
      "INDEPENDENT_FINDINGS": "Every ready MT failed at least one mandatory current validation gate; no MT was passed from historical, proxy, partial, pending, timed-out, or missing-resource evidence.",
      "RESIDUAL_UNCERTAINTY": "Several focused Cargo commands timed out or remained incomplete during shared-target compilation. Those runs produced no test verdict and are not claimed as code failures; each affected MT nevertheless has an independent mandatory gate failure documented above."
    }
  },
  "v6_remediation_2026_09_02": {
    "authority": "OPERATOR_DIRECTIVE_2026_09_02_WP1_V6_REMEDIATION_KERNEL_BUILDER_SUBAGENT_LANES",
    "parent_session": "KERNEL_BUILDER-20260902-180244",
    "storage_authority": "Embedded SurrealDB/EventLedger only [CX-503R]; PostgreSQL/SQLite forbidden in production, tests, fixtures, fallbacks, and proof.",
    "artifact_root_correction": "All cargo/test/runtime proof runs through the parent cargo slot with CARGO_TARGET_DIR=${HANDSHAKE_ARTIFACTS_ROOT}/handshake-cargo-target/wp1-mmo-v6-kb0902 and logs under ${HANDSHAKE_ARTIFACTS_ROOT}/handshake-test/wp1-mmo-v6/wp1-mmo-v6-kb0902/. Any older contract text naming Handshake/_Artifacts or Handshake\\_Artifacts is superseded and must not be used.",
    "session_mt_batch": "MT-002..MT-024",
    "substrate_checkpoint": "feat/WP-1-Multi-Model-Orchestration-Lifecycle-Telemetry-v1: substrate compile checkpoint after ef2d165c (see RECEIPTS.jsonl correlation WP1-V6-REMEDIATION-20260902)",
    "history": [
      {
        "at_utc": "2026-09-02T20:42:14.419Z",
        "kind": "CLAIM",
        "note": "Wave 1 lane: ProcessLedger Surreal proof, restart/reopen, orphan-receipt counterfactual, reclaim manual currency fix",
        "prior_status": "BLOCKED_ON_DEPENDENCY",
        "prior_claimed_by": null,
        "prior_blocker": "V6 source and shared main composition are present, but mandatory current runtime evidence is absent. The focused Surreal targets have not run in the parent-owned serialized Cargo slot; no current proof reopens the same durable namespace/database across a real storage shutdown/restart; no dedicated counterfactual seeds a canonical kernel EventLedger receipt without its lifecycle mutation and proves identical retry fails closed; and SurrealProcessLedgerStore lacks the registry lane's required read-only exact-scope ownership inspection API.",
        "prior_missing_resource": "Parent-owned serialized -j 1 Cargo execution under D:\\Projects\\LLM projects\\Handshake\\Handshake Worktrees\\Handshake\\_Artifacts; focused same-namespace shutdown/reopen and orphan-receipt counterfactual coverage; and an assigned provider change exposing exact-scope process-UUID and latest-by-artifact ownership inspection, including stopped rows and canonical EventLedger verification.",
        "prior_proof_status": "SOURCE_COMPLETE_STATIC_ONLY_BLOCKED_ON_DEPENDENCY: inspected production source and renamed tests are embedded-Surreal-only, exact-scope, and composed through one cloned SurrealStorage; no Cargo/build/test/runtime proof was run in this lane."
      }
    ]
  }
}
