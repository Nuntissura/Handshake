# MT-002: 10.11 [ADD v02.162] Dev Command Center work-orchestration state

## METADATA
- WP_ID: WP-1-Dev-Command-Center-Control-Plane-Backend-v1
- MT_ID: MT-002
- CLAUSE: 10.11 [ADD v02.162] Dev Command Center work-orchestration state
- CODE_SURFACES: `locus/task_board.rs`, `workflows.rs`, `runtime_governance.rs`, `terminal/session.rs`
- EXPECTED_TESTS: task-board and micro-task projection tests plus scheduler-related cargo tests
- DEPENDS_ON: MT-001
- RISK_IF_MISSED: routing and planning continue to depend on kanban-only or transcript-derived state

## CODER
- STATUS: DONE
- EVIDENCE:
  - runtime_governance.rs: DccMicroTaskSummary, DccGateState types added; DccWorkflowSummary extended with micro_task_summary + gate_state; DccWorkState extended with ready_queue; DccSessionBinding extended with model_id, backend, bound_micro_task_id
  - workflows.rs: build_dcc_control_plane_snapshot extended with ready-queue computation, micro-task summary aggregation from governance dir, gate state reading from work packet metadata, session occupancy enrichment (model_id, backend, mt_id); new helpers read_micro_task_summaries_for_dcc() and read_gate_states_for_dcc()
  - locus/task_board.rs: read-only integration (no changes needed — TaskBoardEntryRecordV1 fields already sufficient for ready-queue filtering)
- TESTS_RUN:
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib dcc_ready_query_projection_is_backend_backed -- --nocapture => 1 passed
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib dcc_session_binding_projection_matches_runtime_state -- --nocapture => 1 passed
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib dcc_control_plane_projection_keeps_stable_ids -- --nocapture => 1 passed
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib runtime_governance -- --nocapture => 4 passed
  - cargo check --manifest-path src/backend/handshake_core/Cargo.toml => OK (lib compiles clean, 39 pre-existing warnings only)
- REVISION_2_FIXES (validator steer — backend-backed proof):
  - dcc_ready_query_projection_is_backend_backed: exercises real build_dcc_control_plane_snapshot against materialised task-board index, micro-task packet.json files, and gate metadata on disk; asserts ready_queue, micro_task_summary (total/completed/in_progress/blocked/failed), gate_state (pre_work/post_work)
  - dcc_session_binding_projection_matches_runtime_state: exercises real SessionRegistry + worktree allocation + AiJob.workflow_run_id lookup; asserts session binding fields (model_id, backend, bound_micro_task_id, worktree_dir) and workflow_run_id provenance from AiJob
- NOTES:
  - All new fields are read-only projections; no new authority surfaces
  - Ready-queue computed from entries filtered by WorkflowStateFamily::Ready
  - Micro-task summaries read from governance micro_tasks dir structure per WP with packet.json status parsing
  - Gate state read from work_packets dir packet.json gate_statuses metadata
  - Session occupancy enriched with model_id, backend, bound_micro_task_id from ModelSession
  - terminal/session.rs reviewed — TerminalSession is a lighter-weight concept already covered by ModelSession fields; no changes needed

## VALIDATOR
- STATUS: PENDING
- FINDINGS:
- DIRECTION:
