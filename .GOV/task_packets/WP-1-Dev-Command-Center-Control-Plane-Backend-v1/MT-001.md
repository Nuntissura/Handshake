# MT-001: 10.11 [ADD v02.160] Dev Command Center control-plane state

## METADATA
- WP_ID: WP-1-Dev-Command-Center-Control-Plane-Backend-v1
- MT_ID: MT-001
- CLAUSE: 10.11 [ADD v02.160] Dev Command Center control-plane state
- CODE_SURFACES: `runtime_governance.rs`, `workflows.rs`, `capabilities.rs`, `api/workspaces.rs`
- EXPECTED_TESTS: targeted DCC projection tests plus runtime_governance and session scheduler cargo tests
- DEPENDS_ON: NONE
- RISK_IF_MISSED: DCC remains a shell over fragmented subsystem state

## CODER
- STATUS: DONE (revision 3 — addresses validator escalation)
- EVIDENCE:
  - runtime_governance.rs: DccControlPlaneSnapshot, DccWorkState, DccWorkflowSummary, DccSessionState, DccSessionBinding, DccGovernanceState, DccApprovalDecision types; build_dcc_governance_state() reads governance_decisions JSON files into approval_decisions; read_approval_decisions() helper parses GovernanceDecision JSON (decision_id, gate_type, target_ref, decision, timestamp)
  - workflows.rs: build_dcc_control_plane_snapshot() now accepts &dyn Database; workflow_run_id sourced from AiJob.workflow_run_id via db.get_ai_job(ModelSession.job_id) lookup (NOT from ModelSession.job_id directly); model_session_id from ModelSession.session_id
  - api/workspaces.rs: GET /dcc/control-plane passes state.storage.as_ref() (dyn Database) to builder
  - capabilities.rs: read-only axes()/ids() projected into DccGovernanceState
- TESTS_RUN:
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib dcc_control_plane_projection_keeps_stable_ids -- --nocapture => 1 passed
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib runtime_governance -- --nocapture => 4 passed
  - cargo check --manifest-path src/backend/handshake_core/Cargo.toml => OK (lib compiles clean, 39 pre-existing warnings only)
- REVISION_3_FIXES (validator escalation):
  - workflow_run_id: NOW sourced from AiJob.workflow_run_id via db.get_ai_job() lookup (was incorrectly using ModelSession.job_id)
  - approval decision state: DccGovernanceState.approval_decisions added — reads GovernanceDecision JSON files from governance_decisions_dir, projects decision_id, gate_type, target_ref, decision outcome, timestamp
- REVISION_2_FIXES (validator feedback):
  - model_session_id: populated from ModelSession.session_id via wp_id cross-reference (was hardcoded None)
  - effective capability state: DccGovernanceState includes effective_capability_axes and effective_capability_ids from CapabilityRegistry
  - freshness fabrication: read_task_board_index_for_dcc returns "" when index absent (was Utc::now().to_rfc3339())
- NOTES:
  - All types are read-only projections; no new authority surfaces
  - Stable IDs (work_packet_id, task_board_id, workflow_run_id, model_session_id, authority_refs, evidence_refs) are first-class fields
  - Mailbox and ready_query semantics intentionally NOT frozen per validator steer (deferred to MT-003/MT-004)
  - Pre-existing unclosed delimiter issues in flight_recorder/mod.rs, flight_recorder/duckdb.rs, and workflows.rs tests were fixed to unblock compilation

## VALIDATOR
- STATUS: PENDING
- FINDINGS:
- DIRECTION:
