# MT-003: 10.11 [ADD v02.166] Dev Command Center collaboration state

## METADATA
- WP_ID: WP-1-Dev-Command-Center-Control-Plane-Backend-v1
- MT_ID: MT-003
- CLAUSE: 10.11 [ADD v02.166] Dev Command Center collaboration state
- CODE_SURFACES: `role_mailbox.rs`, `api/role_mailbox.rs`, `locus/task_board.rs`, `workflows.rs`
- EXPECTED_TESTS: role-mailbox and structured-collaboration projection tests
- DEPENDS_ON: MT-002
- RISK_IF_MISSED: DCC triage and handoff remain prose-heavy and non-deterministic

## CODER
- STATUS: DONE
- EVIDENCE:
  - runtime_governance.rs: DccCollaborationState, DccMailboxThreadSummary, DccWaitReason, DccMailboxSummary types added; DccControlPlaneSnapshot extended with collaboration_state field; serde round-trip test extended with collaboration state assertions
  - workflows.rs: read_dcc_collaboration_state() reader parses role-mailbox export artifacts (index.json + thread JSONL); wired into build_dcc_control_plane_snapshot(); wait-reason derivation from latest unanswered request-type message in open threads; evidence_refs collected from transcription_links; linked_work_ids from thread context
  - role_mailbox.rs: read-only integration — existing export artifacts sufficient, no changes needed
  - api/role_mailbox.rs: reviewed — existing GET /role_mailbox/index endpoint already serves the index; no changes needed
  - locus/task_board.rs: reviewed — no mailbox fields on TaskBoardEntryRecordV1; no changes needed
- TESTS_RUN:
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib dcc_mailbox_projection_preserves_wait_reasons -- --nocapture => 1 passed
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib dcc_control_plane_projection_keeps_stable_ids -- --nocapture => 1 passed
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib runtime_governance -- --nocapture => 4 passed
  - cargo check --manifest-path src/backend/handshake_core/Cargo.toml => OK (lib compiles clean, 39 pre-existing warnings only)
- NOTES:
  - All new types are read-only projections; no new authority surfaces
  - Collaboration state sourced entirely from role-mailbox export artifacts (.handshake/gov/ROLE_MAILBOX/)
  - Wait-reason derivation maps request message types to expected responses: clarification_request → clarification_response, scope_change_proposal → scope_change_approval, waiver_proposal → waiver_approval, tooling_request → tooling_result, validation_finding → validation_fix, blocker → blocker_resolution, handoff → handoff_ack
  - Closed threads excluded from wait-reason generation
  - Evidence refs collected from transcription_links target_ref fields across all messages in a thread
  - Linked work IDs populated from thread context work_packet_id

## VALIDATOR
- STATUS: PENDING
- FINDINGS:
- DIRECTION:
