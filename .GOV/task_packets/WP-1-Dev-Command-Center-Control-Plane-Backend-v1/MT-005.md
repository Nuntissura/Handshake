# MT-005: 6.3 compact summary contract for DCC and Role Mailbox triage

## METADATA
- WP_ID: WP-1-Dev-Command-Center-Control-Plane-Backend-v1
- MT_ID: MT-005
- CLAUSE: 6.3 compact summary contract for DCC and Role Mailbox triage
- CODE_SURFACES: `locus/types.rs`, `role_mailbox.rs`, DCC projection serializers in runtime or workflow code
- EXPECTED_TESTS: contract-shape tests proving summary-first payloads and stable ids
- DEPENDS_ON: MT-004
- RISK_IF_MISSED: small-model routing and operator views stay expensive and brittle

## CODER
- STATUS: DONE
- EVIDENCE:
  - locus/types.rs: DccCompactSummaryV1 struct added extending StructuredCollaborationSummaryV1 base envelope with DCC stable IDs (work_packet_id, task_board_id, workflow_run_id, model_session_id) and routing hints (pending_wait_count, active_thread_count, session_bound); DCC_COMPACT_SUMMARY_SCHEMA_ID_V1 constant; DccCompactSummary variant in StructuredCollaborationRecordFamily enum with schema descriptor and validation rules
  - runtime_governance.rs: DccControlPlaneSnapshot::compact_summaries() derives bounded title_or_objective (wp_id + state_family + queue_reason_label, 160-char bounded), blockers from wait reasons and blocked-family state, and next_action from allowed_action_ids or state-family defaults; bounded_text(), queue_reason_label(), default_next_action() helpers; shares record_id, project_profile_kind, and authority_refs with detail record for deterministic joins
  - workflows.rs: backend-backed test dcc_compact_summary_contract_preserves_stable_ids exercises full snapshot builder against materialised task-board and role-mailbox artifacts; asserts semantic contract: bounded title_or_objective content includes wp_id + state + queue label and is <= 160 chars; blocker propagation from wait reasons for blocked WP; next_action derivation from allowed actions (ready) and state-family default (blocked); plus stable ID preservation, routing hints, serde round-trip with semantic field survival, and structured collaboration validation
- TESTS_RUN:
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib dcc_compact_summary_contract_preserves_stable_ids -- --nocapture => 1 passed
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib dcc_control_plane_projection_keeps_stable_ids -- --nocapture => 1 passed
  - cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib runtime_governance -- --nocapture => 4 passed
  - cargo check --manifest-path src/backend/handshake_core/Cargo.toml => OK (lib compiles clean, 39 pre-existing warnings only)
- NOTES:
  - All new types are read-only projections; no new authority surfaces
  - DccCompactSummaryV1 reuses base envelope from StructuredCollaborationSummaryV1 (schema_id, record_id, project_profile_kind, authority_refs, evidence_refs) so detail-summary joins work without transcript reconstruction
  - Routing hints (pending_wait_count, active_thread_count, session_bound) derived from collaboration_state and session_state at compact-summary derivation time
  - Local-small-model consumers can route on workflow_state_family + queue_reason_code + routing hints without loading full DCC snapshot or Markdown sidecars
  - STEER fix (6a4e8bad): replaced hardcoded blockers=[] and next_action=None with derived triage content; title_or_objective now carries bounded work_packet_id + state + queue context; blockers carry wait-reason detail; next_action defaults from allowed actions or state-family semantics

## VALIDATOR
- STATUS: PENDING
- FINDINGS:
- DIRECTION:
