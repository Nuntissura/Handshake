# MT-003: 7.5.4.8 hard repo/runtime boundary

## METADATA
- WP_ID: WP-1-Governance-Workflow-Mirror-v1
- MT_ID: MT-003
- CLAUSE: 7.5.4.8 hard repo/runtime boundary
- CODE_SURFACES: `runtime_governance.rs`, any new workflow-mirror service, boundary tests
- EXPECTED_TESTS: negative-path tests proving `.GOV/` access is rejected and runtime roots stay under `.handshake/gov/`
- DEPENDS_ON: MT-002
- RISK_IF_MISSED: the implementation violates a hard spec boundary

## CODER
- STATUS: DONE
- EVIDENCE:
  - `src/backend/handshake_core/src/runtime_governance.rs:330` keeps workflow-mirror authority refs bounded to the runtime governance root.
  - `src/backend/handshake_core/src/runtime_governance.rs:546` proves `.GOV/validator_gates` runtime roots are rejected.
  - `src/backend/handshake_core/src/role_mailbox.rs:876` enforces runtime-only linked artifacts on the workflow-facing Spec Session Log append path.
  - `src/backend/handshake_core/src/role_mailbox.rs:2066` proves `.GOV/...` workflow linked artifacts are rejected while `.handshake/gov/...` runtime refs are accepted.
- TESTS_RUN:
  - `cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib role_mailbox::tests::governance_workflow_mirror_spec_session_log_enforces_runtime_artifact_boundary -- --exact --nocapture`
  - `cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib governance_workflow_mirror_ -- --nocapture`
  - `node .GOV/roles_shared/checks/phase-check.mjs HANDOFF WP-1-Governance-Workflow-Mirror-v1 CODER`
- NOTES:
  - The runtime boundary proof is now explicit at both layers required by the clause: runtime path resolution rejects `.GOV/` roots, and the workflow-facing append surface rejects `.GOV/...` linked artifacts even when a caller tries to inject them directly.
  - Accepted workflow linked artifacts remain bounded to `.handshake/gov/validator_gates/...` and other runtime-owned display paths exposed by `RuntimeGovernancePaths`.

## VALIDATOR
- STATUS: PENDING
- FINDINGS:
- DIRECTION:
