# MT-002: 2.6.8.8 Spec Session Log + 2.6.8.9 integration hooks

## METADATA
- WP_ID: WP-1-Governance-Workflow-Mirror-v1
- MT_ID: MT-002
- CLAUSE: 2.6.8.8 Spec Session Log + 2.6.8.9 integration hooks
- CODE_SURFACES: `role_mailbox.rs`, workflow-mirror adapter in `workflows.rs` or adjacent runtime-governance service
- EXPECTED_TESTS: session-log tests proving append/query behavior and stable `spec_id`/`task_board_id`/`work_packet_id` linkage
- DEPENDS_ON: MT-001
- RISK_IF_MISSED: operators and models lose the required parallel planning ledger

## CODER
- STATUS: DONE
- EVIDENCE:
  - `src/backend/handshake_core/src/role_mailbox.rs:876` exposes workflow-only spec-session-log append with canonical task-board/runtime-artifact validation.
  - `src/backend/handshake_core/src/role_mailbox.rs:927` exposes workflow-only spec-session-log query filtered away from mailbox-only events.
  - `src/backend/handshake_core/src/workflows.rs:4080` builds activation/gate workflow log requests from work-packet mirror state.
  - `src/backend/handshake_core/src/workflows.rs:4149` appends those workflow entries through `RoleMailbox`.
  - `src/backend/handshake_core/src/workflows.rs:4183` hooks the append path into work-packet runtime materialization.
  - `src/backend/handshake_core/src/workflows.rs:24378` proves stable `spec_id`/`task_board_id`/`work_packet_id` linkage and workflow-only query behavior.
- TESTS_RUN:
  - `cargo test --manifest-path src/backend/handshake_core/Cargo.toml governance_workflow_mirror_spec_session_log_keeps_stable_ids --no-run`
  - `cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib workflows::tests::governance_workflow_mirror_spec_session_log_keeps_stable_ids -- --exact --nocapture`
  - `cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib governance_workflow_mirror_ -- --nocapture`
  - `node .GOV/roles_shared/checks/phase-check.mjs HANDOFF WP-1-Governance-Workflow-Mirror-v1 CODER`
- NOTES:
  - Workflow append is intentionally narrow: activation entries emit on `CreateWp`/`UpdateWp`, gate entries emit on `GateWp`.
  - The workflow-facing API only accepts `workflow_*` event types, enforces the canonical runtime `task_board_id`, and rejects linked artifacts outside `.handshake/gov/`.
  - Querying the workflow surface filters out mailbox-only `mailbox_*` entries so the session-log view stays separately queryable from mailbox traffic.

## VALIDATOR
- STATUS: PENDING
- FINDINGS:
- DIRECTION:
