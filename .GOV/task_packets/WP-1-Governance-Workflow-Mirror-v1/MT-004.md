# MT-004: 7.5.4.9 Governance Check Runner additive overlay rule and storage boundary

## METADATA
- WP_ID: WP-1-Governance-Workflow-Mirror-v1
- MT_ID: MT-004
- CLAUSE: 7.5.4.9 Governance Check Runner additive overlay rule and storage boundary
- CODE_SURFACES: `governance_check_runner.rs`, `governance_artifact_registry.rs`, `storage/mod.rs`, workflow-mirror linkage surfaces
- EXPECTED_TESTS: check-linkage tests proving result/evidence refs are persisted and projected without direct SQLite bypass
- DEPENDS_ON: MT-003
- RISK_IF_MISSED: governed check state drifts from the runtime mirror or bypasses the storage contract

## CODER
- STATUS: DONE
- EVIDENCE:
  - `src/backend/handshake_core/src/workflows.rs:3243` normalizes work-packet evidence refs by folding gate `validation_report_ref` values into the projected runtime evidence set.
  - `src/backend/handshake_core/src/workflows.rs:3280` derives workflow-mirror `check_refs` from the gate validation-report payloads.
  - `src/backend/handshake_core/src/workflows.rs:4552` now applies `work_packet_evidence_refs(&tracked_wp)` inside the structured runtime work-packet emitter so persisted packet/summary projections inherit check refs instead of only overlay refs.
  - `src/backend/handshake_core/src/workflows.rs:24379` proves gate check refs and evidence refs survive the public `LocusOperation::GateWp` path into the runtime gate artifact and projected work-packet artifacts.
- TESTS_RUN:
  - `cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib workflows::tests::governance_workflow_mirror_check_result_links_to_evidence -- --exact --nocapture`
  - `cargo test --manifest-path src/backend/handshake_core/Cargo.toml --lib governance_workflow_mirror_ -- --nocapture`
  - `node .GOV/roles_shared/checks/phase-check.mjs HANDOFF WP-1-Governance-Workflow-Mirror-v1 CODER`
- NOTES:
  - The initial MT-004 tripwire failed because the structured runtime work-packet emitter only preserved overlay artifact refs. This slice fixes that by folding gate validation-report refs into the projected `evidence_refs` before serializing the runtime packet/summary view.
  - On this baseline, the linkage fix lives in the workflow-mirror projection surface in `workflows.rs`; there is no standalone `governance_check_runner.rs` or `governance_artifact_registry.rs` module at the packet path names.

## VALIDATOR
- STATUS: PENDING
- FINDINGS:
- DIRECTION:
