{
  "schema_id": "hsk.microtask_contract@1",
  "schema_version": "microtask_contract_v1",
  "contract_authority": "PRIMARY_MACHINE_READABLE",
  "artifact_policy": {
    "authority_surface": "MACHINE_CONTRACT",
    "legacy_markdown_policy": "SAFETY_NET_ONLY_DO_NOT_COPY_FORWARD",
    "projection_creation": "ON_OPERATOR_REQUEST_ONLY",
    "operator_facing_authority": false,
    "model_created_markdown_authority_allowed": false
  },
  "wp_id": "WP-KERNEL-012-Native-Editors-Obsidian-VSCode-Parity-v1",
  "mt_id": "MT-061",
  "title": "E3-MT-061-CanvasCardResizeSectionsInlineEdit",
  "group": "E3",
  "created_at_utc": "2026-06-21T00:00:00Z",
  "updated_at_utc": "2026-06-21T00:00:00Z",
  "authority_files": {
    "microtask_contract": ".GOV/task_packets/WP-KERNEL-012-Native-Editors-Obsidian-VSCode-Parity-v1/MT-061.json",
    "packet_contract": ".GOV/task_packets/WP-KERNEL-012-Native-Editors-Obsidian-VSCode-Parity-v1/packet.json"
  },
  "markdown_projection": {
    "path": "NOT_GENERATED_BY_DEFAULT",
    "status": "NOT_GENERATED_BY_DEFAULT",
    "generator": "NONE_JSON_AUTHORITY_ONLY"
  },
  "lifecycle": {
    "status": "PENDING",
    "active": false,
    "depends_on": ["MT-026"],
    "blocks": ["MT-044"],
    "validator_verdict": "PENDING"
  },
  "scope": {
    "summary": "Goal: extend the native egui Loom canvas built in MT-026 to full Obsidian-Canvas card parity. MT-026 delivered a draggable/zoomable canvas surface (src/frontend/handshake_native/src/canvas_board.rs) that renders placement cards over an infinite pan/zoom viewport, mutates state through the handshake_core canvas API, and refreshes via the mutate-then-getCanvasBoard cycle. This MT adds the three Obsidian-Canvas features still missing: (1) drag-resize handles on placement cards that persist a new width/height; (2) labeled section/group FRAMES rendered as titled rounded-rectangle containers that cards can be dropped into, promoting the abstract group_id into a visible named frame; (3) in-place editing of free-text card title/body directly on the canvas, persisted through the text-card endpoint. Reference (not copy) semantics for block-backed cards are preserved throughout: resizing, sectioning, or moving a block card NEVER duplicates the underlying loom block; it only mutates the placement record.\n\nFiles to EDIT:\n  - src/frontend/handshake_native/src/canvas_board.rs  — the MT-026 canvas. Add the resize-handle interaction layer and the in-place text-card editor. Wire both into the existing mutate-then-getCanvasBoard refresh cycle. Add the AccessKit registrations for resize handles and for section frames.\n\nFiles to CREATE:\n  - src/frontend/handshake_native/src/canvas_sections.rs  — new module that owns section/group frame rendering and hit-testing. Defines SectionFrame { id: String, label: String, rect: egui::Rect, color: egui::Color32 }; a SectionLayer that derives frames from the placements' group_id values returned by getCanvasBoard (one frame per distinct group_id, with the group label and bounding rect computed from member cards or from a stored frame geometry if the board payload carries one); and hit-testing helpers (which_section(pos) -> Option<group_id>) used during drag-drop to assign a dropped card to a section. Add `pub mod canvas_sections;` to src/frontend/handshake_native/src/lib.rs (the crate module-declaration file; there is no loom/ subdir — canvas modules are flat under handshake_native::).\n\nStep-by-step implementation notes:\n  1. RESIZE HANDLES (canvas_board.rs):\n     a. For each rendered placement card, after drawing the card body, draw a small grab handle in the bottom-right corner (an egui::Rect of ~12x12 logical px, scaled by the current canvas zoom). Use ui.interact(handle_rect, handle_id, egui::Sense::drag()) to get a Response.\n     b. handle_id = egui::Id::new((\"canvas.placement.resize\", &placement.id)) so it is stable per placement.\n     c. On response.dragged(): accumulate response.drag_delta() into the card's in-flight width/height (convert screen delta to canvas-space delta by dividing by zoom). Clamp to a minimum card size (e.g. 80x48 canvas units). Render the card live at the in-flight size so the resize is visually immediate.\n     d. On response.drag_stopped(): commit by calling updateCanvasPlacement (PATCH endpoint, see binds_backend_api) with the final {w, h}. Debounce: only one PATCH per drag-stop (do not PATCH every frame during the drag). After the PATCH resolves, run the standard getCanvasBoard refresh so server-truth geometry replaces the optimistic local value.\n     e. Mouse cursor: set ui.output_mut(|o| o.cursor_icon = egui::CursorIcon::ResizeNwSe) while hovering the handle.\n  2. SECTION / GROUP FRAMES (canvas_sections.rs + canvas_board.rs):\n     a. After fetching the board, build the SectionLayer: group placements by their group_id; for each distinct non-null group_id, compute the frame rect (union of member card rects, padded; or use frame geometry if the payload provides it) and read the section label (from the board payload's section/group metadata; if absent, fall back to the group_id string).\n     b. Render section frames BEHIND the cards (draw frames first, then cards on top) as titled rounded rectangles: ui.painter().rect_filled with a translucent fill, ui.painter().rect_stroke for the border, and a title label painted at the top-left of the frame. Use a per-section color.\n     c. Register an AccessKit node per frame with author_id format \"canvas.section.{id}\" (id = group_id), role = egui's container/group role, and the section label as the accessible name.\n     d. Drag-drop assignment: during a card-move drag (the existing MT-026 move interaction), on drag_stopped call SectionLayer::which_section(drop_pos). If the drop falls inside a section frame, include that section/group_id in the updateCanvasPlacement PATCH (section/group assignment field). If the drop is outside all frames, send a null/clear group assignment. This reuses the SAME PATCH endpoint as resize.\n     e. Editing a section's title is OUT OF SCOPE for this MT unless trivially free; the focus is creating/assigning frames from existing group_id, not a section-CRUD UI.\n  3. IN-PLACE TEXT-CARD EDITING (canvas_board.rs):\n     a. Free-text cards (cards whose backing is a text card, NOT a block reference) get an inline editor. On double-click of a text card (response.double_clicked()), switch that card into edit mode: render an egui::TextEdit::multiline bound to a local editing buffer instead of the static label, sized to the card rect.\n     b. Track editing state per card: editing_card_id: Option<String> on the canvas struct. Only one card edits at a time.\n     c. On commit (focus lost OR Ctrl+Enter), call the text-card create/edit endpoint (POST .../cards, see binds_backend_api) with the new title/body, then refresh via getCanvasBoard. On Escape, discard the buffer and exit edit mode without a server call.\n     d. Block-backed cards are NOT inline-editable on the canvas (editing the block content happens in its own editor pane); double-clicking a block card opens/navigates to the block, it does not turn into a text editor. Guard the inline editor strictly to text cards to preserve reference semantics.\n  4. REFERENCE-NOT-COPY INVARIANT: none of the three features may duplicate a loom block. Resize and section-assignment only mutate the placement record; text editing only mutates the text card's own content. Add a debug assertion / test that resizing or sectioning a block-backed card does not change the set of underlying block_ids on the board.\n  5. AccessKit ids to register this MT: \"canvas.section.{id}\" for each section frame, and \"canvas.placement.{id}.resize\" for each card's resize handle. These extend (do not replace) the MT-026 canvas AccessKit ids (e.g. \"canvas.placement.{id}\").\n\nReact reference: app/src/components/LoomCanvasBoard.tsx — port the card geometry model (x/y/w/h placement record), the group/section concept, and the free-text card body editing. The React board persisted resize and section changes through the same canvas placement update path; mirror that contract in Rust against handshake_core.\n\nBackend binding: bind the EXISTING handshake_core canvas APIs only — do NOT add or rewrite backend endpoints. Use PATCH /workspaces/{workspace_id}/loom/canvas/placements/{placement_id} for both resize (w/h) and section/group assignment, and POST /workspaces/{workspace_id}/loom/canvas/{canvas_block_id}/cards for text-card create/edit. If either endpoint lacks a field this MT needs (e.g. the PATCH body has no w/h or no group_id field, or the cards endpoint cannot edit an existing card), STOP and emit a typed blocker describing the exact missing field/route rather than rewriting the backend. PostgreSQL/EventLedger is the only persistence path — no SQLite anywhere. Reuse the WP-011 shell primitives: the command bus is command_registry.rs + event_bus.rs and the AccessKit id registry is accessibility/registry.rs (with accessibility/snapshot.rs and accessibility/live.rs); pane integration uses pane_registry.rs. Do not invent parallel infrastructure.\n\nDependencies/unblock conditions: MT-026 (native canvas board: draggable cards, pan/zoom, mutate-then-getCanvasBoard refresh, AccessKit \"canvas.placement.{id}\") must be merged first — this MT extends that module in place.\nRisk if missed: the canvas falls short of Obsidian Canvas parity — cards stay fixed-size, groups remain invisible abstractions, and free-text cards cannot be edited on the canvas. This also blocks MT-044, which depends on the resize/section/inline-edit surface being present.",
    "allowed_paths": [
      "src/frontend/handshake_native/src/canvas_board.rs",
      "src/frontend/handshake_native/src/canvas_sections.rs",
      "src/frontend/handshake_native/src/lib.rs"
    ],
    "forbidden_paths": [
      "app/src/** (legacy React, reference only)",
      ".GOV/**",
      "src/backend/** (reuse via API only)"
    ],
    "acceptance_criteria": [
      "AC-061-1: Dragging a card's bottom-right resize handle changes the card's rendered width/height live, and on drag-stop issues exactly one debounced updateCanvasPlacement (PATCH) carrying the final {w, h}; after the PATCH the board re-fetches via getCanvasBoard so server geometry is authoritative — proven by cargo test asserting the queued PATCH count == 1 per drag and the body carries the resized w/h.",
      "AC-061-2: Distinct non-null group_id values from the board render as titled section/group FRAMES (translucent rounded rectangles with a visible label) drawn behind the cards — proven by kittest screenshot/AccessKit dump showing a 'canvas.section.{id}' node with the section label.",
      "AC-061-3: Dropping a card inside a section frame assigns that section/group_id via the SAME placement PATCH endpoint; dropping outside all frames clears the assignment — proven by cargo test asserting the PATCH body's group/section field equals the target frame's group_id on drop-inside and null on drop-outside.",
      "AC-061-4: Double-clicking a free-text card enters in-place edit mode (egui TextEdit::multiline); committing persists title/body via the cards endpoint and the new content survives a getCanvasBoard refresh; Escape discards without a server call — proven by kittest editing a text card body in place and asserting it persists and re-renders after refresh.",
      "AC-061-5: Block-backed cards are NOT turned into inline text editors and resizing/sectioning a block card does not change the set of underlying block_ids on the board (reference, not copy) — proven by a cargo test asserting block_id set invariance across a resize + section-assign cycle.",
      "AC-061-6: AccessKit nodes 'canvas.section.{id}' (per frame) and 'canvas.placement.{id}.resize' (per card resize handle) are present in the exported tree — proven by kittest AccessKit tree dump.",
      "AC-061-7: cargo test for the loom canvas module passes with no failures; no SQLite is introduced and no src/backend/** files are modified."
    ],
    "proof_targets": [
      "PT-061-1: cargo test -p handshake-native canvas_resize -- --nocapture => dragging a resize handle updates card w/h and queues exactly one debounced updateCanvasPlacement with the final geometry; all GREEN.",
      "PT-061-2: cargo test -p handshake-native canvas_section_drop -- --nocapture => dropping a card inside a section frame assigns the section/group id via the placement PATCH; drop-outside clears it; all GREEN.",
      "PT-061-3: kittest harness canvas_text_card_inline_edit (src/frontend/handshake_native/tests/test_canvas_text_card_inline_edit.rs) => double-click a text card, edit body in place, commit, run getCanvasBoard refresh, assert edited body persists and re-renders.",
      "PT-061-4: kittest AccessKit tree dump canvas_section_resize_accesskit (src/frontend/handshake_native/tests/test_canvas_section_resize_accesskit.rs) => assert nodes 'canvas.section.{id}' and 'canvas.placement.{id}.resize' present with correct roles.",
      "PT-061-5: cargo test -p handshake-native canvas_reference_invariant => resize + section-assign a block-backed card; assert the board's underlying block_id set is unchanged (no copy)."
    ],
    "implementation_notes": [
      "Resize handle hit-area: handle_id = egui::Id::new((\"canvas.placement.resize\", &placement.id)); convert screen drag_delta to canvas-space by dividing by the current zoom factor before applying to w/h; clamp to a sensible minimum (e.g. 80x48 canvas units).",
      "Debounce the resize PATCH to drag_stopped() only — do NOT PATCH on every dragged() frame; render the in-flight size optimistically, then reconcile with getCanvasBoard after the PATCH resolves.",
      "Draw order: section frames first (painter rect_filled + rect_stroke + title label), then placement cards on top, so frames read as containers behind their member cards.",
      "Derive SectionLayer from the placements' group_id returned by getCanvasBoard — one frame per distinct group_id; compute the frame rect from member-card bounds (padded) or from frame geometry if the board payload supplies it; label from board section metadata, falling back to the group_id string.",
      "Inline text editor: gate strictly to free-text cards via a card-kind check; block-backed cards open/navigate the block instead of becoming editable, preserving reference-not-copy semantics. Track a single editing_card_id: Option<String>.",
      "Reuse the MT-026 mutate-then-getCanvasBoard refresh cycle for ALL three mutations (resize PATCH, section-assign PATCH, text-card POST); do not invent a second refresh path.",
      "Reuse WP-011 shell primitives: register resize handles and section frames through the existing AccessKit id registry (accessibility/registry.rs, with accessibility/snapshot.rs and accessibility/live.rs) and route any canvas commands through the existing command bus (command_registry.rs + event_bus.rs) rather than ad-hoc dispatch.",
      "Cross-WP dependency: this MT extends the MT-026 canvas surface which itself builds on WP-KERNEL-011 shell primitives (command_registry.rs, event_bus.rs, accessibility/registry.rs, pane_registry.rs); those WP-011 modules must already be present in handshake_native::.",
      "If the PATCH body lacks w/h or a group/section field, or the cards endpoint cannot edit an existing card, emit a typed blocker naming the exact missing route/field — do NOT add or rewrite a backend endpoint."
    ],
    "ports_from_react": [
      "app/src/components/LoomCanvasBoard.tsx (card geometry, group, card body) — reference-only port (app/src/** is forbidden for edits) of the x/y/w/h placement record, the group/section concept and frame rendering, and the free-text card body editing; mirror the resize + section persistence path against handshake_core in src/frontend/handshake_native/src/canvas_board.rs and src/frontend/handshake_native/src/canvas_sections.rs."
    ],
    "binds_backend_api": [
      "PATCH /workspaces/{workspace_id}/loom/canvas/placements/{placement_id} (resize w/h + section/group assignment) — existing handshake_core endpoint; bind only, do not rewrite. If the body lacks w/h or a group/section field, emit a typed blocker.",
      "POST /workspaces/{workspace_id}/loom/canvas/{canvas_block_id}/cards (text card create/edit) — existing handshake_core endpoint; bind only, do not rewrite. If it cannot edit an existing card, emit a typed blocker."
    ]
  },
  "hbr_obligations": ["HBR-INT", "HBR-SWARM", "HBR-VIS", "HBR-QUIET"],
  "handoff": {
    "coder_session": null,
    "wp_validator_session": null
  },
  "red_team": {
    "required": true,
    "profile": "NATIVE_EDITORS_PARITY_V1",
    "risks": [
      "RISK-061-1: Resize PATCH fired every dragged() frame floods the backend and the EventLedger with intermediate geometries, causing lag and ledger bloat.",
      "RISK-061-2: Optimistic in-flight resize/move diverges from server truth if the PATCH fails or the getCanvasBoard refresh is skipped, leaving the canvas showing geometry the backend never stored.",
      "RISK-061-3: Inline text editing accidentally enabled on a block-backed card would let canvas edits fork or copy the underlying block, breaking the reference-not-copy invariant.",
      "RISK-061-4: Section frames derived from member-card bounds can overlap or nest ambiguously, so which_section(drop_pos) assigns a dropped card to the wrong group_id.",
      "RISK-061-5: A required field (w/h on the PATCH body, group/section assignment field, or edit-existing-card support on the cards endpoint) may be missing from the existing handshake_core API, tempting a backend rewrite that violates the HARD no-backend-rewrite constraint.",
      "RISK-061-6: AccessKit id collisions if 'canvas.placement.{id}.resize' or 'canvas.section.{id}' overlap an MT-026 id or repeat across re-renders, breaking swarm addressability."
    ],
    "minimum_controls": [
      "MC-061-1: Debounce resize and move persistence to drag_stopped() only — one PATCH per gesture; cargo test asserts the queued PATCH count == 1 per drag.",
      "MC-061-2: Always run the mutate-then-getCanvasBoard refresh after every PATCH/POST; on PATCH failure, roll back the optimistic geometry to the last server-confirmed value.",
      "MC-061-3: Gate the inline editor with an explicit card-kind check so only free-text cards become editable; add a cargo test asserting the underlying block_id set is unchanged after resize + section-assign on a block card.",
      "MC-061-4: Define deterministic section hit-testing precedence (e.g. smallest/topmost enclosing frame wins) and cover overlap/nesting cases in canvas_section_drop tests.",
      "MC-061-5: If any required API field/route is absent, emit a typed blocker naming the exact missing field/route and STOP — never add or modify src/backend/** and never introduce SQLite.",
      "MC-061-6: Register resize and section AccessKit ids through the WP-011 AccessKit id registry (accessibility/registry.rs) with the stable formats 'canvas.placement.{id}.resize' and 'canvas.section.{id}'; kittest asserts uniqueness and presence."
    ]
  }
}
