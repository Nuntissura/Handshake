{
  "schema_id": "hsk.microtask_contract@1",
  "schema_version": "microtask_contract_v1",
  "contract_authority": "PRIMARY_MACHINE_READABLE",
  "artifact_policy": {
    "authority_surface": "MACHINE_CONTRACT",
    "legacy_markdown_policy": "SAFETY_NET_ONLY_DO_NOT_COPY_FORWARD",
    "projection_creation": "ON_OPERATOR_REQUEST_ONLY",
    "operator_facing_authority": false,
    "model_created_markdown_authority_allowed": false
  },
  "wp_id": "WP-KERNEL-012-Native-Editors-Obsidian-VSCode-Parity-v1",
  "mt_id": "MT-031",
  "title": "E5-MT-031-SharedSelectionClipboardCommandBus",
  "group": "E5 Interconnection / melt-together",
  "created_at_utc": "2026-06-19T00:00:00Z",
  "updated_at_utc": "2026-06-19T00:00:00Z",
  "authority_files": {
    "microtask_contract": ".GOV/task_packets/WP-KERNEL-012-Native-Editors-Obsidian-VSCode-Parity-v1/MT-031.json",
    "packet_contract": ".GOV/task_packets/WP-KERNEL-012-Native-Editors-Obsidian-VSCode-Parity-v1/packet.json"
  },
  "markdown_projection": {
    "path": "NOT_GENERATED_BY_DEFAULT",
    "status": "NOT_GENERATED_BY_DEFAULT",
    "generator": "NONE_JSON_AUTHORITY_ONLY"
  },
  "lifecycle": {
    "status": "PENDING",
    "active": false,
    "depends_on": ["MT-001", "MT-011", "MT-021"],
    "blocks": ["MT-032", "MT-033", "MT-034", "MT-035"],
    "validator_verdict": "PENDING"
  },
  "scope": {
    "summary": "Goal: implement a single app-wide SharedInteractionBus — the substrate that lets all editor panes (code, rich-text, graph, canvas) share one selection model, one clipboard, and one command registry. The expected diff shape is: (a) one new Rust module src/frontend/handshake_native/src/interop/interaction_bus.rs (~350–500 lines) defining the bus types and an egui-integrated singleton, building on the existing WP-011 command_registry.rs + event_bus.rs rather than reinventing a command/dispatch substrate, (b) thin adapter shims in each existing pane module (src/frontend/handshake_native/src/code_editor/mod.rs, rich_text/mod.rs, graph/mod.rs, and the existing canvas_board.rs) that register into the bus rather than own ad-hoc clipboard/command state, (c) a kittest integration test src/frontend/handshake_native/tests/test_interaction_bus.rs proving cross-pane selection propagation and clipboard round-trip.\n\nFiles to CREATE:\n  - src/frontend/handshake_native/src/interop/interaction_bus.rs  — defines SharedSelection, ClipboardPayload, CommandBus, CommandDescriptor, CommandHandler; holds an Arc<Mutex<InteractionBus>> accessible through egui's app data storage via Context::data_mut. Reuses command_registry.rs (command descriptor / registration substrate) and event_bus.rs (cross-pane event/notification fan-out) instead of defining a parallel command or event system.\n  - src/frontend/handshake_native/src/interop/mod.rs  — declares the interop subdir module(s) including interaction_bus (if not already created by an earlier interop MT).\n\nFiles to EDIT:\n  - src/frontend/handshake_native/src/lib.rs  — add `pub mod interop;` (or extend the existing interop module declaration).\n  - src/frontend/handshake_native/src/code_editor/mod.rs  — replace per-pane clipboard + keybinding dispatch with bus calls; call bus.clipboard_write / bus.clipboard_read; register editor commands (Copy, Cut, Paste, SelectAll, Find) via the bus, routing registration through the existing command_registry.rs.\n  - src/frontend/handshake_native/src/rich_text/mod.rs  — same pattern.\n  - src/frontend/handshake_native/src/graph/mod.rs  — register node-selection into SharedSelection; clipboard writes (copy node ref as loom:// URI). Reuse loom_graph.rs for graph node identity/selection state.\n  - src/frontend/handshake_native/src/canvas_board.rs  — same as graph; canvas node selection feeds SharedSelection (reuse the existing canvas_board.rs surface rather than a new canvas pane).\n\nStep-by-step implementation notes:\n  1. Define SharedSelection as an enum: None | TextRange { pane_id: PaneId, start: usize, end: usize, text: String } | BlockRef { pane_id: PaneId, block_id: String } | NodeRef { pane_id: PaneId, node_id: String }. Pane that holds focus owns the active variant; others observe. PaneId comes from the existing pane_registry.rs.\n  2. Define ClipboardPayload as an enum: PlainText(String) | LoomBlockRef(String) | AtelierRef(String) | RichContent { mime: String, bytes: Vec<u8> }. Write through arboard (add to src/frontend/handshake_native/Cargo.toml if not present; it is pure Rust, MIT licensed). On read, return the richest available variant the pane can consume.\n  3. Define CommandDescriptor { id: &'static str, label: String, keybind: Option<egui::KeyboardShortcut>, handler: CommandHandler } where CommandHandler = Arc<dyn Fn(&mut egui::Ui, &mut InteractionBus) + Send + Sync>. Register descriptors through the existing command_registry.rs rather than holding a parallel Vec; CommandBus wraps/extends the registry for the cross-pane dispatch surface.\n  4. The command palette is the existing WP-011 command_palette.rs driven by the command bus: collect all registered descriptors from command_registry.rs, display in a fuzzy-searchable egui modal with AccessKit id \"command-palette-list\" + role List; on confirm call handler. Extend command_palette.rs rather than building a second palette.\n  5. Store the InteractionBus in egui Context app data: ctx.data_mut(|d| d.insert_temp(egui::Id::new(\"handshake_interaction_bus\"), bus.clone())). Every pane retrieves it the same way.\n  6. Keybind dispatch: each pane, in its update() call, calls bus.dispatch_keybind(ctx, ui) after its own event handling. The bus iterates registered descriptors and checks egui::InputState::consume_shortcut.\n  7. AccessKit: register the command palette trigger button with AccessKit author_id=\"command-palette-trigger\", role=Button, actions=[Press]. Register the search input with author_id=\"command-palette-search\", role=TextField. Each command list item uses author_id=\"cmd-{descriptor.id}\", role=ListItem. Use the existing accessibility/ subsystem (accessibility/registry.rs, accessibility/live.rs) for node registration.\n  8. Add a pane_focus_changed notification: when a pane receives click focus, it publishes through event_bus.rs and calls bus.set_focus_owner(pane_id) so SharedSelection.pane_id tracks which pane currently owns selection.\n  9. Cross-pane Copy: when the user presses Ctrl+C in any pane, that pane builds a ClipboardPayload and calls bus.clipboard_write(payload). The bus calls arboard::Clipboard::set_text for PlainText, and also caches the richest variant in-memory for same-session cross-pane Paste.\n  10. Tests: src/frontend/handshake_native/tests/test_interaction_bus.rs — (a) unit: build a bus, register two panes, simulate selection change in pane A, read SharedSelection and assert it reflects pane A; (b) kittest harness: open code pane, select text, Ctrl+C, focus rich-text pane, Ctrl+V, assert pasted text equals source; (c) command bus: register a test command, call dispatch_command by id, assert handler invoked.\n\nDependencies / unblock conditions:\n  - MT-001 (code editor pane exists at src/frontend/handshake_native/src/code_editor/) must be merged first.\n  - MT-011 (rich-text editor pane) must be merged.\n  - MT-021 (graph pane) must be merged.\n  - arboard crate (^3.x, pure Rust, MIT) must be added to src/frontend/handshake_native/Cargo.toml if not already present.\n  - Reuses the WP-011 substrate: command_registry.rs, event_bus.rs, command_palette.rs, pane_registry.rs, canvas_board.rs, loom_graph.rs, and accessibility/* (these already exist from WP-011 and are not created by this MT).\n\nRisk if missed: every downstream E5 MT (MT-032 through MT-035) requires the bus as a common substrate. Without it, each subsequent MT would build its own ad-hoc selection/clipboard, fragmenting the 'melt-together' invariant and making the parity proof suite impossible.",
    "allowed_paths": [
      "src/frontend/handshake_native/src/interop/interaction_bus.rs",
      "src/frontend/handshake_native/src/interop/mod.rs",
      "src/frontend/handshake_native/src/lib.rs",
      "src/frontend/handshake_native/src/code_editor/mod.rs",
      "src/frontend/handshake_native/src/rich_text/mod.rs",
      "src/frontend/handshake_native/src/graph/mod.rs",
      "src/frontend/handshake_native/src/canvas_board.rs",
      "src/frontend/handshake_native/tests/test_interaction_bus.rs",
      "src/frontend/handshake_native/Cargo.toml"
    ],
    "forbidden_paths": [
      "app/src/** (legacy React, reference only)",
      ".GOV/**",
      "src/backend/** (reuse via API only)"
    ],
    "acceptance_criteria": [
      "SharedSelection propagates: selecting text in the code pane and reading SharedSelection from any other pane returns the correct TextRange with matching text and pane_id — proven by kittest harness.",
      "Clipboard round-trip: Ctrl+C in code pane then Ctrl+V in rich-text pane pastes the identical text — proven by kittest harness; arboard::Clipboard also receives the write (verified by reading system clipboard in the test).",
      "Command palette modal opens with Ctrl+Shift+P (or OS-appropriate shortcut), lists all registered commands, filters by fuzzy query, and fires the selected command handler — proven by kittest interaction test; the palette is the existing command_palette.rs extended to read command_registry.rs.",
      "All four pane types (code, rich-text, graph, canvas) register at least one command into the bus and at least one clipboard write path — proven by grep of bus.register_command and bus.clipboard_write call sites.",
      "AccessKit nodes present: 'command-palette-trigger' (Button), 'command-palette-search' (TextField), and at least one 'cmd-{id}' (ListItem) — proven by AccessKit tree dump in kittest.",
      "cargo test -p handshake-native passes with no failures across all interaction-bus tests.",
      "No per-pane ad-hoc clipboard state or separate keybind dispatch tables remain after the refactor (old ad-hoc code removed, not duplicated alongside)."
    ],
    "proof_targets": [
      "cargo test -p handshake-native interaction_bus -- --nocapture  =>  all tests PASS, no panics",
      "kittest harness: simulate Ctrl+C in code pane, Ctrl+V in rich-text pane, assert pasted text equals source selection",
      "kittest harness: open command palette via Ctrl+Shift+P, type partial command name, assert filtered list, press Enter, assert handler side-effect",
      "AccessKit tree snapshot: assert nodes 'command-palette-trigger', 'command-palette-search', 'cmd-Copy' present with correct roles"
    ],
    "implementation_notes": [
      "Reuse the WP-011 substrate: command_registry.rs is the canonical command descriptor/registration store; event_bus.rs is the canonical cross-pane event/notification fan-out. The interaction_bus wraps and coordinates these — it must NOT define a parallel command list or a parallel event system.",
      "Cross-WP dependency: this MT depends on WP-KERNEL-011 having shipped command_registry.rs, event_bus.rs, command_palette.rs, pane_registry.rs, canvas_board.rs, loom_graph.rs, and accessibility/* under src/frontend/handshake_native/src/. These are reused, not recreated, by this WP-012 MT.",
      "Use egui::Id::new(\"handshake_interaction_bus\") as the stable key for ctx.data_mut storage; every pane retrieves the bus by this id in its update() fn.",
      "arboard ^3 is cross-platform (Windows/macOS/Linux) pure Rust; add under [dependencies] in src/frontend/handshake_native/Cargo.toml.",
      "CommandDescriptor.keybind uses egui::KeyboardShortcut { modifiers, key }; standard VS Code bindings: Copy=Ctrl+C, Cut=Ctrl+X, Paste=Ctrl+V, SelectAll=Ctrl+A, Find=Ctrl+F, CommandPalette=Ctrl+Shift+P.",
      "Reuse command_palette.rs for the modal surface; if it currently anchors via egui::Window::new(\"Command Palette\").anchor(egui::Align2::CENTER_TOP, [0.0, 48.0]).show(ctx, ...), drive its open state from bus.command_palette_open rather than introducing a new window.",
      "Pane focus: each pane's update() fn calls bus.set_focus_owner(self.pane_id) only when ui.memory(|m| m.has_focus(self.pane_egui_id)) is true, avoiding spurious resets; pane_id is sourced from pane_registry.rs.",
      "AccessKit author_id registration goes through the existing accessibility/registry.rs (and accessibility/live.rs for live announcements); do not bypass the accessibility subsystem.",
      "React reference: CommandPalette.tsx defines CommandPaletteAction { id, label, description, keywords, disabled, stableId }; match this shape in CommandDescriptor; stableId maps to AccessKit author_id."
    ],
    "ports_from_react": [
      "app/src/components/CommandPalette.tsx — CommandPaletteAction shape, fuzzy filter logic (matchesQuery), keyboard nav (ArrowUp/Down/Enter/Escape), overlay modal pattern; port the data shape and filter behavior into CommandDescriptor and the existing command_palette.rs surface"
    ],
    "binds_backend_api": [
      "None — this MT is pure frontend bus; clipboard write uses arboard (OS clipboard); no backend API calls required"
    ]
  },
  "hbr_obligations": ["HBR-INT", "HBR-SWARM", "HBR-QUIET"],
  "handoff": {
    "coder_session": null,
    "wp_validator_session": null
  },
  "red_team": {
    "required": true,
    "profile": "NATIVE_GUI_V1",
    "risks": [
      "Arc<Mutex<InteractionBus>> contention: multiple panes calling bus methods in the same egui frame could deadlock if a handler re-enters the lock.",
      "arboard fails on headless CI (no clipboard daemon on Linux); tests that assert system clipboard content will hang or fail.",
      "Keybind conflicts: registering Ctrl+C in the bus conflicts with egui's default text-widget copy if panes do not suppress the default handler.",
      "Stale pane_id after a pane is closed: SharedSelection may reference a closed pane; consumers must guard against dangling pane_id.",
      "AccessKit author_id collisions if two command palette instances are ever rendered (e.g. in kittest harness with multiple windows)."
    ],
    "minimum_controls": [
      "Use a try_lock pattern in update() paths and log a warning on contention; never block the egui frame thread.",
      "Gate arboard system-clipboard assertions behind a #[cfg(not(ci))] feature flag or check for DISPLAY env var; fallback to in-memory clipboard assertion only in CI.",
      "Each pane calls ui.input_mut(|i| i.consume_shortcut(&shortcut)) before bus.dispatch_keybind to suppress egui default handling.",
      "SharedSelection read must check pane_id exists in the live pane_registry.rs before use; return None if pane is gone.",
      "Ensure the egui::Id for the command palette modal is scoped to the window id so multi-window harness tests do not collide."
    ]
  }
}
