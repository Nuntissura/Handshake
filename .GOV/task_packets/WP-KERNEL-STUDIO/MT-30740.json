{
 "schema_id": "hsk.microtask_contract@1",
 "schema_version": "microtask_contract_v3_spec_derived",
 "contract_authority": "PRIMARY_MACHINE_READABLE",
 "wp_id": "WP-KERNEL-STUDIO",
 "mt_id": "MT-30740",
 "created_at_utc": "2026-09-04T20:07:52+00:00",
 "generated_by": "handshake.greenroom.spec_derive.v1",
 "derivation": {
  "rule": "One microtask per derivation unit declared by the module's own Microtask Derivation sub-section. The Master Spec is the only input.",
  "derivation_key": "STU-LAY-134#parameter_table#0",
  "unit_kind": "parameter_table",
  "spec_module": "14-studio-creative-suite.md",
  "spec_heading": "14.6.4 Frames and Placed Graphics",
  "spec_line": 6142,
  "reproducible": "Re-running spec-derive-microtasks.py against an unchanged spec reproduces this contract with this same mt_id."
 },
 "lifecycle": {
  "status": "PENDING",
  "depends_on": [],
  "blocks": [],
  "active": false,
  "validator_verdict": "PENDING"
 },
 "clause": "Layout object-effect targeting and parameter contract - The captured effect set and its declared bounds parameter bounds",
 "spec_anchor": [
  "STU-LAY-134"
 ],
 "spec_anchor_status": "RESOLVED: derived from the assembled Master Spec module, not provisional",
 "domain": "layout",
 "scope": {
  "summary": "Layout object-effect targeting and parameter contract.** [STU-LAY-032] required opacity, blend mode and each effect to be independently targetable to Object, Fill, Stroke or Text of a single frame, presented as an effects tree. The captured object-style effect category objects confirm exactly FOUR targets -- object, fill, stroke and content -- each carrying its own fourteen- field effect settings block, so the four-target model is storage, not presentation. Studio MUST carry four independent effect stacks per frame. The captured effect set and its declared bounds: *Derivation: parameter table, taken whole; yields 1 microtask whose acceptance criteria are its rows, one bound-set per row.* | Effect | Parameter | hard_min | hard_max | soft_min | soft_max | default | unit | precision | |---|---|---|---|---|---|---|---|---| | Drop shadow | `opacity` | 0 | 100 | unknown | unknown | unknown | `percent` | unknown | | Drop shadow | `noise` | 0 | 100 | unknown | unknown | unknown | `percent` | unknown | | Drop shadow | `spread` | 0 | 100 | unknown | unknown | unknown | `percent` | unknown | | Drop shadow | `size` | 0 | 144 | unknown | unknown | unknown | `pt` | unknown | | Drop shadow | `y_offset` | -1000 | 1000 | unknown | unknown | unknown | `pt` | unknown | | Drop shadow | `x_offset` | -1000 | 1000 | unknown | unknown | unknown | `pt` | unknown | | Inner shadow | `choke_amount` | 0 | [...]",
  "spec_text_is_authority": "The clause text above is quoted from the Master Spec. Where this contract and the spec differ, the spec wins and this contract is regenerated."
 },
 "acceptance_criteria": [
  {
   "id": "AC-001",
   "criterion": "The parameter `Drop shadow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 1"
  },
  {
   "id": "AC-002",
   "criterion": "The parameter `Drop shadow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 2"
  },
  {
   "id": "AC-003",
   "criterion": "The parameter `Drop shadow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 3"
  },
  {
   "id": "AC-004",
   "criterion": "The parameter `Drop shadow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=144, soft_min=unknown, soft_max=unknown, default=unknown, unit=`pt`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 4"
  },
  {
   "id": "AC-005",
   "criterion": "The parameter `Drop shadow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=unknown, soft_max=unknown, default=unknown, unit=`pt`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 5"
  },
  {
   "id": "AC-006",
   "criterion": "The parameter `Drop shadow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=unknown, soft_max=unknown, default=unknown, unit=`pt`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 6"
  },
  {
   "id": "AC-007",
   "criterion": "The parameter `Inner shadow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 7"
  },
  {
   "id": "AC-008",
   "criterion": "The parameter `Inner shadow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 8"
  },
  {
   "id": "AC-009",
   "criterion": "The parameter `Inner shadow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 9"
  },
  {
   "id": "AC-010",
   "criterion": "The parameter `Inner shadow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-360, hard_max=360, soft_min=unknown, soft_max=unknown, default=unknown, unit=`deg`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 10"
  },
  {
   "id": "AC-011",
   "criterion": "The parameter `Outer glow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 11"
  },
  {
   "id": "AC-012",
   "criterion": "The parameter `Outer glow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 12"
  },
  {
   "id": "AC-013",
   "criterion": "The parameter `Outer glow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 13"
  },
  {
   "id": "AC-014",
   "criterion": "The parameter `Inner glow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 14"
  },
  {
   "id": "AC-015",
   "criterion": "The parameter `Inner glow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 15"
  },
  {
   "id": "AC-016",
   "criterion": "The parameter `Inner glow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 16"
  },
  {
   "id": "AC-017",
   "criterion": "The parameter `Bevel and emboss` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=90, soft_min=unknown, soft_max=unknown, default=unknown, unit=`deg`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 17"
  },
  {
   "id": "AC-018",
   "criterion": "The parameter `Bevel and emboss` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-180, hard_max=180, soft_min=unknown, soft_max=unknown, default=unknown, unit=`deg`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 18"
  },
  {
   "id": "AC-019",
   "criterion": "The parameter `Bevel and emboss` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 19"
  },
  {
   "id": "AC-020",
   "criterion": "The parameter `Bevel and emboss` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 20"
  },
  {
   "id": "AC-021",
   "criterion": "The parameter `Bevel and emboss` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 21"
  },
  {
   "id": "AC-022",
   "criterion": "The parameter `Satin` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-360, hard_max=360, soft_min=unknown, soft_max=unknown, default=unknown, unit=`deg`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 22"
  },
  {
   "id": "AC-023",
   "criterion": "The parameter `Satin` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 23"
  },
  {
   "id": "AC-024",
   "criterion": "The parameter `Basic feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 24"
  },
  {
   "id": "AC-025",
   "criterion": "The parameter `Basic feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 25"
  },
  {
   "id": "AC-026",
   "criterion": "The parameter `Basic feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=unknown, soft_max=unknown, default=unknown, unit=`pt`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 26"
  },
  {
   "id": "AC-027",
   "criterion": "The parameter `Directional feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-180, hard_max=180, soft_min=unknown, soft_max=unknown, default=unknown, unit=`deg`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 27"
  },
  {
   "id": "AC-028",
   "criterion": "The parameter `Directional feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 28"
  },
  {
   "id": "AC-029",
   "criterion": "The parameter `Directional feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 29"
  },
  {
   "id": "AC-030",
   "criterion": "The parameter `Directional feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.2, hard_max=250, soft_min=unknown, soft_max=unknown, default=unknown, unit=`pt`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 30"
  },
  {
   "id": "AC-031",
   "criterion": "The parameter `Directional feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.2, hard_max=250, soft_min=unknown, soft_max=unknown, default=unknown, unit=`pt`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 31"
  },
  {
   "id": "AC-032",
   "criterion": "The parameter `Directional feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.2, hard_max=250, soft_min=unknown, soft_max=unknown, default=unknown, unit=`pt`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 32"
  },
  {
   "id": "AC-033",
   "criterion": "The parameter `Directional feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.2, hard_max=250, soft_min=unknown, soft_max=unknown, default=unknown, unit=`pt`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 33"
  },
  {
   "id": "AC-034",
   "criterion": "The parameter `Gradient feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 34"
  },
  {
   "id": "AC-035",
   "criterion": "The parameter `Gradient feather` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=13, hard_max=87, soft_min=unknown, soft_max=unknown, default=unknown, unit=`percent`, precision=unknown. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-LAY-134 parameter table row 35"
  }
 ],
 "acceptance_criteria_count": 35,
 "implementation_notes": {
  "extracted_behaviour": [],
  "behaviour_record_count": 0,
  "naming": "Vendor product names are provenance only. Studio ships Handshake-native names per [STU-SECTION-003].",
  "database": "SurrealDB with the EventLedger is the only durable authority. SQLite, libSQL, Turso and PostgreSQL are forbidden, including in tests and dev caches.",
  "engine_split": "GPU, WGSL and compute live in the studio-engine crate behind its traits. handshake_core must never gain a GPU dependency.",
  "routing": "Navigation extends the existing address and bus seams. No new router."
 },
 "gui_obligation": {
  "operator_surface_required": "YES",
  "argus_required": "YES"
 },
 "user_manual_obligation": {
  "required": true,
  "same_change_update_required": "YES"
 },
 "resource_privacy_obligation": {
  "applies": true
 },
 "hbr_obligations": [
  "HBR-INT-001",
  "HBR-INT-003",
  "HBR-INT-009",
  "HBR-VIS-001",
  "HBR-VIS-003",
  "HBR-MAN-001",
  "HBR-MAN-004",
  "HBR-PRIV-001"
 ],
 "validator_focus": [
  "Confirm every acceptance row traces to a sentence or table row in the cited spec clause. A row with no spec basis is a fabrication and fails the contract.",
  "Reject scaffold-only proof: at least one proof command must drive the executable runtime rather than a fixture the implementer authored.",
  "For a parameter row, confirm hard and soft bounds are stored as separate values and an unknown bound is stored as unknown rather than mirrored from its opposite."
 ],
 "handoff": {
  "coder_session": null,
  "wp_validator_session": null
 }
}