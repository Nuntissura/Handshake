{
 "schema_id": "hsk.microtask_contract@1",
 "schema_version": "microtask_contract_v3_spec_derived",
 "contract_authority": "PRIMARY_MACHINE_READABLE",
 "wp_id": "WP-KERNEL-STUDIO",
 "mt_id": "MT-35151",
 "created_at_utc": "2026-09-04T13:36:10+00:00",
 "generated_by": "handshake.greenroom.spec_derive.v1",
 "derivation": {
  "rule": "One microtask per derivation unit declared by the module's own Microtask Derivation sub-section. The Master Spec is the only input.",
  "derivation_key": "STU-FX-133a#parameter_table#0",
  "unit_kind": "parameter_table",
  "spec_module": "14-studio-creative-suite.md",
  "spec_heading": "14.9.6 Catalogue: the video-editing effect set",
  "spec_line": 16536,
  "reproducible": "Re-running spec-derive-microtasks.py against an unchanged spec reproduces this contract with this same mt_id."
 },
 "lifecycle": {
  "status": "PENDING",
  "depends_on": [],
  "blocks": [],
  "active": false,
  "validator_verdict": "PENDING"
 },
 "clause": "Every bounded parameter in the video-editing effect surface - parameter bounds",
 "spec_anchor": [
  "STU-FX-133a"
 ],
 "spec_anchor_status": "RESOLVED: derived from the assembled Master Spec module, not provisional",
 "domain": "effects",
 "scope": {
  "summary": "Every bounded parameter in the video-editing effect surface.** These 631 rows are the complete set that declares a bound. Note the separate `soft_min`/`soft_max` columns: the same hard/soft split of [STU-FX-105] is present here under different source field names, and it must survive. Every parameter of every effect NOT in this table is `unbounded_in_source` and MUST be implemented without a range check. `precision` is `--` on every row: this surface declares bounds, defaults and units but no decimal count, and the column is present and empty rather than absent, so that the seven fields of [STU-FX-105] are addressable on every row of every parameter table in this sub-section. An implementer authors the decimal count deliberately under [STU-FX-109]; it is never inferred from the number of digits a default happens to print. *Derivation: parameter table, taken whole; yields 1 microtask whose acceptance criteria are its rows, one bound-set per row.* | Effect | Parameter | Type | hard_min | hard_max | soft_min | soft_max | default | unit | precision | keyframable | |---|---|---|---|---|---|---|---|---|---|---| | 2D Text Box | Measure At Time [sec] | float | -1000000 | 1000000 | 0 | 100 | 0 | -- | -- | true | | 2D Text Box | Scale X | float | -1000000 | 1000000 | 0 | 100 | 100 | percent | -- | true | | 2D Text Box | Scale Y | float | -1000000 | 1000000 | 0 | 100 | 100 | percent | -- | [...]",
  "spec_text_is_authority": "The clause text above is quoted from the Master Spec. Where this contract and the spec differ, the spec wins and this contract is regenerated."
 },
 "acceptance_criteria": [
  {
   "id": "AC-001",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 1"
  },
  {
   "id": "AC-002",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 2"
  },
  {
   "id": "AC-003",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 3"
  },
  {
   "id": "AC-004",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=35, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 4"
  },
  {
   "id": "AC-005",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 5"
  },
  {
   "id": "AC-006",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 6"
  },
  {
   "id": "AC-007",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 7"
  },
  {
   "id": "AC-008",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 8"
  },
  {
   "id": "AC-009",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 9"
  },
  {
   "id": "AC-010",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 10"
  },
  {
   "id": "AC-011",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=20, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 11"
  },
  {
   "id": "AC-012",
   "criterion": "The parameter `2D Text Box` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 12"
  },
  {
   "id": "AC-013",
   "criterion": "The parameter `Adaptive Noise Reduction` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=40, soft_min=--, soft_max=--, default=20, unit=reduce_noise, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 13"
  },
  {
   "id": "AC-014",
   "criterion": "The parameter `Adaptive Noise Reduction` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=30, unit=noisiness, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 14"
  },
  {
   "id": "AC-015",
   "criterion": "The parameter `Adaptive Noise Reduction` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-10, hard_max=10, soft_min=--, soft_max=--, default=2, unit=fine_tune_noise_floor, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 15"
  },
  {
   "id": "AC-016",
   "criterion": "The parameter `Adaptive Noise Reduction` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-20, hard_max=20, soft_min=--, soft_max=--, default=2.5, unit=signal_threshold, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 16"
  },
  {
   "id": "AC-017",
   "criterion": "The parameter `Adaptive Noise Reduction` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=750, soft_min=--, soft_max=--, default=140.00032, unit=spectral_decay, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 17"
  },
  {
   "id": "AC-018",
   "criterion": "The parameter `Adaptive Noise Reduction` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=500, soft_min=--, soft_max=--, default=100, unit=broadband_preservation, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 18"
  },
  {
   "id": "AC-019",
   "criterion": "The parameter `Animated Shape Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-10000, hard_max=10000, soft_min=0, soft_max=100, default=10, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 19"
  },
  {
   "id": "AC-020",
   "criterion": "The parameter `Animated Shape Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-10000, hard_max=10000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 20"
  },
  {
   "id": "AC-021",
   "criterion": "The parameter `Animated Shape Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=10000, soft_min=0, soft_max=100, default=10, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 21"
  },
  {
   "id": "AC-022",
   "criterion": "The parameter `Animated Shape Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-10000, hard_max=10000, soft_min=0, soft_max=100, default=10, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 22"
  },
  {
   "id": "AC-023",
   "criterion": "The parameter `Animated Shape Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-10000, hard_max=10000, soft_min=0, soft_max=100, default=10, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 23"
  },
  {
   "id": "AC-024",
   "criterion": "The parameter `AutoClickRemover` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=100, soft_min=--, soft_max=--, default=29.999971, unit=threshold, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 24"
  },
  {
   "id": "AC-025",
   "criterion": "The parameter `AutoClickRemover` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=100, soft_min=--, soft_max=--, default=15.999985, unit=complexity, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 25"
  },
  {
   "id": "AC-026",
   "criterion": "The parameter `Autoscroll - horizontal` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-10000, hard_max=10000, soft_min=-1000, soft_max=1000, default=100, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 26"
  },
  {
   "id": "AC-027",
   "criterion": "The parameter `Autoscroll - vertical` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-10000, hard_max=10000, soft_min=-1000, soft_max=1000, default=-25, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 27"
  },
  {
   "id": "AC-028",
   "criterion": "The parameter `Bevel Edges` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=0.5, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 28"
  },
  {
   "id": "AC-029",
   "criterion": "The parameter `Bevel Edges` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-32768, hard_max=32767, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 29"
  },
  {
   "id": "AC-030",
   "criterion": "The parameter `Bevel Edges` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1.8446744073709552e+19, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 30"
  },
  {
   "id": "AC-031",
   "criterion": "The parameter `Bevel Edges` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 31"
  },
  {
   "id": "AC-032",
   "criterion": "The parameter `Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=250, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 32"
  },
  {
   "id": "AC-033",
   "criterion": "The parameter `Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 33"
  },
  {
   "id": "AC-034",
   "criterion": "The parameter `Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 34"
  },
  {
   "id": "AC-035",
   "criterion": "The parameter `Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 35"
  },
  {
   "id": "AC-036",
   "criterion": "The parameter `Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 36"
  },
  {
   "id": "AC-037",
   "criterion": "The parameter `Bounce At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=250, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 37"
  },
  {
   "id": "AC-038",
   "criterion": "The parameter `Bounce At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 38"
  },
  {
   "id": "AC-039",
   "criterion": "The parameter `Bounce At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 39"
  },
  {
   "id": "AC-040",
   "criterion": "The parameter `Bounce On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=250, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 40"
  },
  {
   "id": "AC-041",
   "criterion": "The parameter `Bounce On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 41"
  },
  {
   "id": "AC-042",
   "criterion": "The parameter `Bounce On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 42"
  },
  {
   "id": "AC-043",
   "criterion": "The parameter `Bounce On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=15, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 43"
  },
  {
   "id": "AC-044",
   "criterion": "The parameter `Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=250, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 44"
  },
  {
   "id": "AC-045",
   "criterion": "The parameter `Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 45"
  },
  {
   "id": "AC-046",
   "criterion": "The parameter `Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 46"
  },
  {
   "id": "AC-047",
   "criterion": "The parameter `Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 47"
  },
  {
   "id": "AC-048",
   "criterion": "The parameter `Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 48"
  },
  {
   "id": "AC-049",
   "criterion": "The parameter `Card Wipe Master Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 49"
  },
  {
   "id": "AC-050",
   "criterion": "The parameter `Chaser Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-10000, hard_max=10000, soft_min=0, soft_max=100, default=10, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 50"
  },
  {
   "id": "AC-051",
   "criterion": "The parameter `Chaser Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=1000, soft_min=1, soft_max=100, default=10, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 51"
  },
  {
   "id": "AC-052",
   "criterion": "The parameter `Chaser Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 52"
  },
  {
   "id": "AC-053",
   "criterion": "The parameter `Color Swirl` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 53"
  },
  {
   "id": "AC-054",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30, hard_max=30, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 54"
  },
  {
   "id": "AC-055",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30, hard_max=30, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 55"
  },
  {
   "id": "AC-056",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30, hard_max=30, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 56"
  },
  {
   "id": "AC-057",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30, hard_max=30, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 57"
  },
  {
   "id": "AC-058",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30, hard_max=30, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 58"
  },
  {
   "id": "AC-059",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30, hard_max=30, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 59"
  },
  {
   "id": "AC-060",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30, hard_max=30, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 60"
  },
  {
   "id": "AC-061",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30, hard_max=30, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 61"
  },
  {
   "id": "AC-062",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30, hard_max=30, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 62"
  },
  {
   "id": "AC-063",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-32768, hard_max=32767, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 63"
  },
  {
   "id": "AC-064",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-32768, hard_max=32767, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 64"
  },
  {
   "id": "AC-065",
   "criterion": "The parameter `Convolution Kernel` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 65"
  },
  {
   "id": "AC-066",
   "criterion": "The parameter `Corner Reveal` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 66"
  },
  {
   "id": "AC-067",
   "criterion": "The parameter `Corner Reveal` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 67"
  },
  {
   "id": "AC-068",
   "criterion": "The parameter `Counter Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=100, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 68"
  },
  {
   "id": "AC-069",
   "criterion": "The parameter `Counter Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=2, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 69"
  },
  {
   "id": "AC-070",
   "criterion": "The parameter `Counter Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 70"
  },
  {
   "id": "AC-071",
   "criterion": "The parameter `Counter Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 71"
  },
  {
   "id": "AC-072",
   "criterion": "The parameter `Counter Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 72"
  },
  {
   "id": "AC-073",
   "criterion": "The parameter `Counter Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 73"
  },
  {
   "id": "AC-074",
   "criterion": "The parameter `Cracked Tiles` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=33, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 74"
  },
  {
   "id": "AC-075",
   "criterion": "The parameter `Cracked Tiles` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=2, hard_max=2000, soft_min=2, soft_max=200, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 75"
  },
  {
   "id": "AC-076",
   "criterion": "The parameter `Crop Edges` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=50, soft_min=--, soft_max=--, default=5, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 76"
  },
  {
   "id": "AC-077",
   "criterion": "The parameter `Crop Edges` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 77"
  },
  {
   "id": "AC-078",
   "criterion": "The parameter `Currency Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 78"
  },
  {
   "id": "AC-079",
   "criterion": "The parameter `Currency Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 79"
  },
  {
   "id": "AC-080",
   "criterion": "The parameter `Currency Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 80"
  },
  {
   "id": "AC-081",
   "criterion": "The parameter `Currency Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=2, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 81"
  },
  {
   "id": "AC-082",
   "criterion": "The parameter `Currency Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 82"
  },
  {
   "id": "AC-083",
   "criterion": "The parameter `Currency Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 83"
  },
  {
   "id": "AC-084",
   "criterion": "The parameter `Currency Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 84"
  },
  {
   "id": "AC-085",
   "criterion": "The parameter `Dissolve - unmelt` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 85"
  },
  {
   "id": "AC-086",
   "criterion": "The parameter `Dissolve - unmelt` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-10000, hard_max=10000, soft_min=0, soft_max=5000, default=100, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 86"
  },
  {
   "id": "AC-087",
   "criterion": "The parameter `Dissolve Master Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 87"
  },
  {
   "id": "AC-088",
   "criterion": "The parameter `Drift Over Time` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-10000, hard_max=10000, soft_min=-1000, soft_max=1000, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 88"
  },
  {
   "id": "AC-089",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=500, soft_min=--, soft_max=--, default=0, unit=attack, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 89"
  },
  {
   "id": "AC-090",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=2000, soft_min=--, soft_max=--, default=0, unit=release, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 90"
  },
  {
   "id": "AC-091",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=500, soft_min=--, soft_max=--, default=0, unit=attack, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 91"
  },
  {
   "id": "AC-092",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=2000, soft_min=--, soft_max=--, default=0, unit=release, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 92"
  },
  {
   "id": "AC-093",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=500, soft_min=--, soft_max=--, default=0, unit=attack, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 93"
  },
  {
   "id": "AC-094",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-48, hard_max=48, soft_min=--, soft_max=--, default=-48, unit=decibel, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 94"
  },
  {
   "id": "AC-095",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-48, hard_max=48, soft_min=--, soft_max=--, default=-48, unit=decibel, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 95"
  },
  {
   "id": "AC-096",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=sample_rate / 2, soft_min=--, soft_max=--, default=--, unit=filter, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 96"
  },
  {
   "id": "AC-097",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=sample_rate / 2, soft_min=--, soft_max=--, default=--, unit=filter, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 97"
  },
  {
   "id": "AC-098",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 98"
  },
  {
   "id": "AC-099",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 99"
  },
  {
   "id": "AC-100",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 100"
  },
  {
   "id": "AC-101",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 101"
  },
  {
   "id": "AC-102",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 102"
  },
  {
   "id": "AC-103",
   "criterion": "The parameter `Dynamics` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=makeup, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 103"
  },
  {
   "id": "AC-104",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=0, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 104"
  },
  {
   "id": "AC-105",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=0, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 105"
  },
  {
   "id": "AC-106",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=0, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 106"
  },
  {
   "id": "AC-107",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30000, soft_min=0, soft_max=500, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 107"
  },
  {
   "id": "AC-108",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 108"
  },
  {
   "id": "AC-109",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 109"
  },
  {
   "id": "AC-110",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=0, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 110"
  },
  {
   "id": "AC-111",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=0, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 111"
  },
  {
   "id": "AC-112",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 112"
  },
  {
   "id": "AC-113",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 113"
  },
  {
   "id": "AC-114",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=0, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 114"
  },
  {
   "id": "AC-115",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=0, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 115"
  },
  {
   "id": "AC-116",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=0, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 116"
  },
  {
   "id": "AC-117",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=0, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 117"
  },
  {
   "id": "AC-118",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 118"
  },
  {
   "id": "AC-119",
   "criterion": "The parameter `Face Measurements` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 119"
  },
  {
   "id": "AC-120",
   "criterion": "The parameter `Face Track Points` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-500, soft_max=500, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 120"
  },
  {
   "id": "AC-121",
   "criterion": "The parameter `Face Track Points` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-359.9999, hard_max=359.9999, soft_min=-359.9999, soft_max=359.9999, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 121"
  },
  {
   "id": "AC-122",
   "criterion": "The parameter `Face Track Points` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-359.9999, hard_max=359.9999, soft_min=-359.9999, soft_max=359.9999, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 122"
  },
  {
   "id": "AC-123",
   "criterion": "The parameter `Face Track Points` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-359.9999, hard_max=359.9999, soft_min=-359.9999, soft_max=359.9999, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 123"
  },
  {
   "id": "AC-124",
   "criterion": "The parameter `Fade In+Out - frames` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=--, soft_max=--, default=15, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 124"
  },
  {
   "id": "AC-125",
   "criterion": "The parameter `Fade In+Out - frames` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=--, soft_max=--, default=15, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 125"
  },
  {
   "id": "AC-126",
   "criterion": "The parameter `Fade In+Out - msec` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30000, soft_min=0, soft_max=10000, default=500, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 126"
  },
  {
   "id": "AC-127",
   "criterion": "The parameter `Fade In+Out - msec` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30000, soft_min=0, soft_max=10000, default=500, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 127"
  },
  {
   "id": "AC-128",
   "criterion": "The parameter `Fade Master Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 128"
  },
  {
   "id": "AC-129",
   "criterion": "The parameter `Fast Blur` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=32767, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 129"
  },
  {
   "id": "AC-130",
   "criterion": "The parameter `Fast Blur` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=2, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 130"
  },
  {
   "id": "AC-131",
   "criterion": "The parameter `Fast Blur` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 131"
  },
  {
   "id": "AC-132",
   "criterion": "The parameter `Flanger` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=20, soft_min=--, soft_max=--, default=0, unit=delay, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 132"
  },
  {
   "id": "AC-133",
   "criterion": "The parameter `Flanger` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=20, soft_min=--, soft_max=--, default=0, unit=delay, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 133"
  },
  {
   "id": "AC-134",
   "criterion": "The parameter `Flanger` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=360, soft_min=--, soft_max=--, default=0, unit=phase, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 134"
  },
  {
   "id": "AC-135",
   "criterion": "The parameter `Flanger` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.001, hard_max=60, soft_min=--, soft_max=--, default=0.001, unit=cycles, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 135"
  },
  {
   "id": "AC-136",
   "criterion": "The parameter `Flanger` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=mix, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 136"
  },
  {
   "id": "AC-137",
   "criterion": "The parameter `Flanger` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=feedback, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 137"
  },
  {
   "id": "AC-138",
   "criterion": "The parameter `Flanger` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.03333, hard_max=2000, soft_min=--, soft_max=--, default=0.03333, unit=beats, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 138"
  },
  {
   "id": "AC-139",
   "criterion": "The parameter `Fly to Inset` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 139"
  },
  {
   "id": "AC-140",
   "criterion": "The parameter `Fly to Inset` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=33.3, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 140"
  },
  {
   "id": "AC-141",
   "criterion": "The parameter `Fly to Inset` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=0, soft_max=30, default=8, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 141"
  },
  {
   "id": "AC-142",
   "criterion": "The parameter `Follow` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=10, default=0.1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 142"
  },
  {
   "id": "AC-143",
   "criterion": "The parameter `Getting Jiggy` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-50, hard_max=50, soft_min=--, soft_max=--, default=5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 143"
  },
  {
   "id": "AC-144",
   "criterion": "The parameter `Getting Jiggy` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-50, hard_max=50, soft_min=2, soft_max=8, default=5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 144"
  },
  {
   "id": "AC-145",
   "criterion": "The parameter `Getting Jiggy` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 145"
  },
  {
   "id": "AC-146",
   "criterion": "The parameter `Getting Jiggy` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1, soft_min=--, soft_max=--, default=0.5, unit=pixels, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 146"
  },
  {
   "id": "AC-147",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=10, hard_max=8192, soft_min=--, soft_max=--, default=999.997454, unit=accuracy, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 147"
  },
  {
   "id": "AC-148",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-64, hard_max=64, soft_min=--, soft_max=--, default=0, unit=gain, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 148"
  },
  {
   "id": "AC-149",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1.5, hard_max=120, soft_min=--, soft_max=--, default=47.808615, unit=range, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 149"
  },
  {
   "id": "AC-150",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 150"
  },
  {
   "id": "AC-151",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 151"
  },
  {
   "id": "AC-152",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 152"
  },
  {
   "id": "AC-153",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 153"
  },
  {
   "id": "AC-154",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 154"
  },
  {
   "id": "AC-155",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 155"
  },
  {
   "id": "AC-156",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 156"
  },
  {
   "id": "AC-157",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 157"
  },
  {
   "id": "AC-158",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 158"
  },
  {
   "id": "AC-159",
   "criterion": "The parameter `GraphicEQ10Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 159"
  },
  {
   "id": "AC-160",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=10, hard_max=8192, soft_min=--, soft_max=--, default=999.997454, unit=accuracy, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 160"
  },
  {
   "id": "AC-161",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-64, hard_max=64, soft_min=--, soft_max=--, default=0, unit=gain, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 161"
  },
  {
   "id": "AC-162",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1.5, hard_max=120, soft_min=--, soft_max=--, default=47.808615, unit=range, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 162"
  },
  {
   "id": "AC-163",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 163"
  },
  {
   "id": "AC-164",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 164"
  },
  {
   "id": "AC-165",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 165"
  },
  {
   "id": "AC-166",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 166"
  },
  {
   "id": "AC-167",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 167"
  },
  {
   "id": "AC-168",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 168"
  },
  {
   "id": "AC-169",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 169"
  },
  {
   "id": "AC-170",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 170"
  },
  {
   "id": "AC-171",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 171"
  },
  {
   "id": "AC-172",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 172"
  },
  {
   "id": "AC-173",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 173"
  },
  {
   "id": "AC-174",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 174"
  },
  {
   "id": "AC-175",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 175"
  },
  {
   "id": "AC-176",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 176"
  },
  {
   "id": "AC-177",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 177"
  },
  {
   "id": "AC-178",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 178"
  },
  {
   "id": "AC-179",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 179"
  },
  {
   "id": "AC-180",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 180"
  },
  {
   "id": "AC-181",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 181"
  },
  {
   "id": "AC-182",
   "criterion": "The parameter `GraphicEQ20Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 182"
  },
  {
   "id": "AC-183",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=10, hard_max=8192, soft_min=--, soft_max=--, default=999.997454, unit=accuracy, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 183"
  },
  {
   "id": "AC-184",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-64, hard_max=64, soft_min=--, soft_max=--, default=0, unit=gain, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 184"
  },
  {
   "id": "AC-185",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1.5, hard_max=120, soft_min=--, soft_max=--, default=47.808615, unit=range, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 185"
  },
  {
   "id": "AC-186",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 186"
  },
  {
   "id": "AC-187",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 187"
  },
  {
   "id": "AC-188",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 188"
  },
  {
   "id": "AC-189",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 189"
  },
  {
   "id": "AC-190",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 190"
  },
  {
   "id": "AC-191",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 191"
  },
  {
   "id": "AC-192",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 192"
  },
  {
   "id": "AC-193",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 193"
  },
  {
   "id": "AC-194",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 194"
  },
  {
   "id": "AC-195",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 195"
  },
  {
   "id": "AC-196",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 196"
  },
  {
   "id": "AC-197",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 197"
  },
  {
   "id": "AC-198",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 198"
  },
  {
   "id": "AC-199",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 199"
  },
  {
   "id": "AC-200",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 200"
  },
  {
   "id": "AC-201",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 201"
  },
  {
   "id": "AC-202",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 202"
  },
  {
   "id": "AC-203",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 203"
  },
  {
   "id": "AC-204",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 204"
  },
  {
   "id": "AC-205",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 205"
  },
  {
   "id": "AC-206",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 206"
  },
  {
   "id": "AC-207",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 207"
  },
  {
   "id": "AC-208",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 208"
  },
  {
   "id": "AC-209",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 209"
  },
  {
   "id": "AC-210",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 210"
  },
  {
   "id": "AC-211",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 211"
  },
  {
   "id": "AC-212",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 212"
  },
  {
   "id": "AC-213",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 213"
  },
  {
   "id": "AC-214",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 214"
  },
  {
   "id": "AC-215",
   "criterion": "The parameter `GraphicEQ30Bands` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-70, hard_max=70, soft_min=--, soft_max=--, default=0, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 215"
  },
  {
   "id": "AC-216",
   "criterion": "The parameter `Grid Wipe` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 216"
  },
  {
   "id": "AC-217",
   "criterion": "The parameter `Grid Wipe` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=2000, soft_min=--, soft_max=--, default=20, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 217"
  },
  {
   "id": "AC-218",
   "criterion": "The parameter `Grid Wipe` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 218"
  },
  {
   "id": "AC-219",
   "criterion": "The parameter `Hard Limiter` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-100, hard_max=0, soft_min=--, soft_max=--, default=-50, unit=max_amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 219"
  },
  {
   "id": "AC-220",
   "criterion": "The parameter `Hard Limiter` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-100, hard_max=50, soft_min=--, soft_max=--, default=20, unit=input_boost, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 220"
  },
  {
   "id": "AC-221",
   "criterion": "The parameter `Hard Limiter` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=5, hard_max=20, soft_min=--, soft_max=--, default=7.1, unit=lookahead_time, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 221"
  },
  {
   "id": "AC-222",
   "criterion": "The parameter `Hard Limiter` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=40, hard_max=200, soft_min=--, soft_max=--, default=100, unit=release_time, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 222"
  },
  {
   "id": "AC-223",
   "criterion": "The parameter `Inset Video - framed` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=50, soft_min=--, soft_max=--, default=5, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 223"
  },
  {
   "id": "AC-224",
   "criterion": "The parameter `Inset Video - framed` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=0, soft_max=30, default=8, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 224"
  },
  {
   "id": "AC-225",
   "criterion": "The parameter `Inset Video - torn edges` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=50, soft_min=--, soft_max=--, default=5, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 225"
  },
  {
   "id": "AC-226",
   "criterion": "The parameter `Iris Wipe Master Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 226"
  },
  {
   "id": "AC-227",
   "criterion": "The parameter `Iris Wipe Master Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 227"
  },
  {
   "id": "AC-228",
   "criterion": "The parameter `Jiggle` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=100, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 228"
  },
  {
   "id": "AC-229",
   "criterion": "The parameter `Jiggle` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 229"
  },
  {
   "id": "AC-230",
   "criterion": "The parameter `Jiggle` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 230"
  },
  {
   "id": "AC-231",
   "criterion": "The parameter `Jiggle` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 231"
  },
  {
   "id": "AC-232",
   "criterion": "The parameter `Jiggle` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 232"
  },
  {
   "id": "AC-233",
   "criterion": "The parameter `Jiggle At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=100, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 233"
  },
  {
   "id": "AC-234",
   "criterion": "The parameter `Jiggle At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 234"
  },
  {
   "id": "AC-235",
   "criterion": "The parameter `Jiggle At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 235"
  },
  {
   "id": "AC-236",
   "criterion": "The parameter `Jiggle On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=100, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 236"
  },
  {
   "id": "AC-237",
   "criterion": "The parameter `Jiggle On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 237"
  },
  {
   "id": "AC-238",
   "criterion": "The parameter `Jiggle On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 238"
  },
  {
   "id": "AC-239",
   "criterion": "The parameter `Jiggle On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=15, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 239"
  },
  {
   "id": "AC-240",
   "criterion": "The parameter `Jiggle Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=100, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 240"
  },
  {
   "id": "AC-241",
   "criterion": "The parameter `Jiggle Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 241"
  },
  {
   "id": "AC-242",
   "criterion": "The parameter `Jiggle Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 242"
  },
  {
   "id": "AC-243",
   "criterion": "The parameter `Jiggle Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 243"
  },
  {
   "id": "AC-244",
   "criterion": "The parameter `Jiggle Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 244"
  },
  {
   "id": "AC-245",
   "criterion": "The parameter `Light Leaks - layer markers` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=10000, soft_min=1, soft_max=1000, default=200, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 245"
  },
  {
   "id": "AC-246",
   "criterion": "The parameter `Light Leaks - random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 246"
  },
  {
   "id": "AC-247",
   "criterion": "The parameter `Light Leaks - random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=10000, soft_min=1, soft_max=500, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 247"
  },
  {
   "id": "AC-248",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 248"
  },
  {
   "id": "AC-249",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 249"
  },
  {
   "id": "AC-250",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 250"
  },
  {
   "id": "AC-251",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 251"
  },
  {
   "id": "AC-252",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 252"
  },
  {
   "id": "AC-253",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 253"
  },
  {
   "id": "AC-254",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=998, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 254"
  },
  {
   "id": "AC-255",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=100, hard_max=1000, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 255"
  },
  {
   "id": "AC-256",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 256"
  },
  {
   "id": "AC-257",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1.8446744073709552e+19, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 257"
  },
  {
   "id": "AC-258",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-150, hard_max=150, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 258"
  },
  {
   "id": "AC-259",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-150, hard_max=150, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 259"
  },
  {
   "id": "AC-260",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 260"
  },
  {
   "id": "AC-261",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 261"
  },
  {
   "id": "AC-262",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-7, hard_max=7, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 262"
  },
  {
   "id": "AC-263",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-150, hard_max=150, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 263"
  },
  {
   "id": "AC-264",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-150, hard_max=150, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 264"
  },
  {
   "id": "AC-265",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-150, hard_max=150, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 265"
  },
  {
   "id": "AC-266",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-150, hard_max=150, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 266"
  },
  {
   "id": "AC-267",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-150, hard_max=150, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 267"
  },
  {
   "id": "AC-268",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-150, hard_max=150, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 268"
  },
  {
   "id": "AC-269",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 269"
  },
  {
   "id": "AC-270",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 270"
  },
  {
   "id": "AC-271",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 271"
  },
  {
   "id": "AC-272",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=300, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 272"
  },
  {
   "id": "AC-273",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 273"
  },
  {
   "id": "AC-274",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 274"
  },
  {
   "id": "AC-275",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 275"
  },
  {
   "id": "AC-276",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 276"
  },
  {
   "id": "AC-277",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 277"
  },
  {
   "id": "AC-278",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=998, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 278"
  },
  {
   "id": "AC-279",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=200, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 279"
  },
  {
   "id": "AC-280",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 280"
  },
  {
   "id": "AC-281",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=150, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 281"
  },
  {
   "id": "AC-282",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-100, hard_max=100, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 282"
  },
  {
   "id": "AC-283",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-100, hard_max=100, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 283"
  },
  {
   "id": "AC-284",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=300, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 284"
  },
  {
   "id": "AC-285",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 285"
  },
  {
   "id": "AC-286",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-150, hard_max=150, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 286"
  },
  {
   "id": "AC-287",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 287"
  },
  {
   "id": "AC-288",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 288"
  },
  {
   "id": "AC-289",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 289"
  },
  {
   "id": "AC-290",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 290"
  },
  {
   "id": "AC-291",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 291"
  },
  {
   "id": "AC-292",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=100, hard_max=10000, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 292"
  },
  {
   "id": "AC-293",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 293"
  },
  {
   "id": "AC-294",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 294"
  },
  {
   "id": "AC-295",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 295"
  },
  {
   "id": "AC-296",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 296"
  },
  {
   "id": "AC-297",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 297"
  },
  {
   "id": "AC-298",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 298"
  },
  {
   "id": "AC-299",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 299"
  },
  {
   "id": "AC-300",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 300"
  },
  {
   "id": "AC-301",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=100, hard_max=1000, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 301"
  },
  {
   "id": "AC-302",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 302"
  },
  {
   "id": "AC-303",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 303"
  },
  {
   "id": "AC-304",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 304"
  },
  {
   "id": "AC-305",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 305"
  },
  {
   "id": "AC-306",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 306"
  },
  {
   "id": "AC-307",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1.8446744073709552e+19, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 307"
  },
  {
   "id": "AC-308",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1.8446744073709552e+19, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 308"
  },
  {
   "id": "AC-309",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1.8446744073709552e+19, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 309"
  },
  {
   "id": "AC-310",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 310"
  },
  {
   "id": "AC-311",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 311"
  },
  {
   "id": "AC-312",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=2, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 312"
  },
  {
   "id": "AC-313",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 313"
  },
  {
   "id": "AC-314",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 314"
  },
  {
   "id": "AC-315",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 315"
  },
  {
   "id": "AC-316",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 316"
  },
  {
   "id": "AC-317",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 317"
  },
  {
   "id": "AC-318",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 318"
  },
  {
   "id": "AC-319",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 319"
  },
  {
   "id": "AC-320",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 320"
  },
  {
   "id": "AC-321",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 321"
  },
  {
   "id": "AC-322",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 322"
  },
  {
   "id": "AC-323",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 323"
  },
  {
   "id": "AC-324",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-300, hard_max=300, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 324"
  },
  {
   "id": "AC-325",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-300, hard_max=300, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 325"
  },
  {
   "id": "AC-326",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-150, hard_max=150, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 326"
  },
  {
   "id": "AC-327",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-100, hard_max=100, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 327"
  },
  {
   "id": "AC-328",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=300, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 328"
  },
  {
   "id": "AC-329",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 329"
  },
  {
   "id": "AC-330",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 330"
  },
  {
   "id": "AC-331",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 331"
  },
  {
   "id": "AC-332",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 332"
  },
  {
   "id": "AC-333",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-5, hard_max=5, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 333"
  },
  {
   "id": "AC-334",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 334"
  },
  {
   "id": "AC-335",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-100, hard_max=100, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 335"
  },
  {
   "id": "AC-336",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 336"
  },
  {
   "id": "AC-337",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 337"
  },
  {
   "id": "AC-338",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 338"
  },
  {
   "id": "AC-339",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 339"
  },
  {
   "id": "AC-340",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 340"
  },
  {
   "id": "AC-341",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 341"
  },
  {
   "id": "AC-342",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 342"
  },
  {
   "id": "AC-343",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=false, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 343"
  },
  {
   "id": "AC-344",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 344"
  },
  {
   "id": "AC-345",
   "criterion": "The parameter `Color Grade` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 345"
  },
  {
   "id": "AC-346",
   "criterion": "The parameter `Mask Fade Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 346"
  },
  {
   "id": "AC-347",
   "criterion": "The parameter `Mask Fade Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 347"
  },
  {
   "id": "AC-348",
   "criterion": "The parameter `Mood Lighting - amorphous` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=-500, soft_max=500, default=150, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 348"
  },
  {
   "id": "AC-349",
   "criterion": "The parameter `Mood Lighting - amorphous` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=5000, soft_min=10, soft_max=500, default=75, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 349"
  },
  {
   "id": "AC-350",
   "criterion": "The parameter `Mood Lighting - amorphous` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 350"
  },
  {
   "id": "AC-351",
   "criterion": "The parameter `Mood Lighting - digital` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=-500, soft_max=500, default=100, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 351"
  },
  {
   "id": "AC-352",
   "criterion": "The parameter `Mood Lighting - digital` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=5000, soft_min=20, soft_max=1000, default=250, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 352"
  },
  {
   "id": "AC-353",
   "criterion": "The parameter `Mood Lighting - digital` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=75, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 353"
  },
  {
   "id": "AC-354",
   "criterion": "The parameter `Mood Lighting - streaks` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=-500, soft_max=500, default=200, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 354"
  },
  {
   "id": "AC-355",
   "criterion": "The parameter `Mood Lighting - streaks` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=1000, soft_min=10, soft_max=500, default=75, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 355"
  },
  {
   "id": "AC-356",
   "criterion": "The parameter `Mood Lighting - streaks` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=75, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 356"
  },
  {
   "id": "AC-357",
   "criterion": "The parameter `Motion` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 357"
  },
  {
   "id": "AC-358",
   "criterion": "The parameter `Motion` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=600, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 358"
  },
  {
   "id": "AC-359",
   "criterion": "The parameter `Motion` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=600, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 359"
  },
  {
   "id": "AC-360",
   "criterion": "The parameter `Motion` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 360"
  },
  {
   "id": "AC-361",
   "criterion": "The parameter `Motion` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-32768, hard_max=32767, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 361"
  },
  {
   "id": "AC-362",
   "criterion": "The parameter `Motion` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=--, hard_max=--, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "hard_min",
    "hard_max",
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 362"
  },
  {
   "id": "AC-363",
   "criterion": "The parameter `Opacity Flash - layer markers` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=10000, soft_min=1, soft_max=2000, default=500, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 363"
  },
  {
   "id": "AC-364",
   "criterion": "The parameter `Opacity Flash - random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 364"
  },
  {
   "id": "AC-365",
   "criterion": "The parameter `Opacity Flash - random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=10000, soft_min=1, soft_max=500, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 365"
  },
  {
   "id": "AC-366",
   "criterion": "The parameter `Opacity Pulse` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 366"
  },
  {
   "id": "AC-367",
   "criterion": "The parameter `Opacity Pulse` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=100, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 367"
  },
  {
   "id": "AC-368",
   "criterion": "The parameter `Opacity Pulse` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=10, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 368"
  },
  {
   "id": "AC-369",
   "criterion": "The parameter `Opacity Pulse` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 369"
  },
  {
   "id": "AC-370",
   "criterion": "The parameter `Opacity Pulse` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0.01, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 370"
  },
  {
   "id": "AC-371",
   "criterion": "The parameter `Opacity Pulse` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 371"
  },
  {
   "id": "AC-372",
   "criterion": "The parameter `Opacity Pulse At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 372"
  },
  {
   "id": "AC-373",
   "criterion": "The parameter `Opacity Pulse At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=100, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 373"
  },
  {
   "id": "AC-374",
   "criterion": "The parameter `Opacity Pulse At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=10, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 374"
  },
  {
   "id": "AC-375",
   "criterion": "The parameter `Opacity Pulse At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 375"
  },
  {
   "id": "AC-376",
   "criterion": "The parameter `Opacity Pulse On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 376"
  },
  {
   "id": "AC-377",
   "criterion": "The parameter `Opacity Pulse On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=100, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 377"
  },
  {
   "id": "AC-378",
   "criterion": "The parameter `Opacity Pulse On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=10, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 378"
  },
  {
   "id": "AC-379",
   "criterion": "The parameter `Opacity Pulse On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 379"
  },
  {
   "id": "AC-380",
   "criterion": "The parameter `Opacity Pulse On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=15, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 380"
  },
  {
   "id": "AC-381",
   "criterion": "The parameter `Opacity Pulse Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 381"
  },
  {
   "id": "AC-382",
   "criterion": "The parameter `Opacity Pulse Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=100, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 382"
  },
  {
   "id": "AC-383",
   "criterion": "The parameter `Opacity Pulse Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=10, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 383"
  },
  {
   "id": "AC-384",
   "criterion": "The parameter `Opacity Pulse Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 384"
  },
  {
   "id": "AC-385",
   "criterion": "The parameter `Opacity Pulse Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=0.75, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 385"
  },
  {
   "id": "AC-386",
   "criterion": "The parameter `Opacity Pulse Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 386"
  },
  {
   "id": "AC-387",
   "criterion": "The parameter `Orbit` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=3000, soft_min=--, soft_max=500, default=150, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 387"
  },
  {
   "id": "AC-388",
   "criterion": "The parameter `Orbit` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30, hard_max=30, soft_min=-15, soft_max=15, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 388"
  },
  {
   "id": "AC-389",
   "criterion": "The parameter `Orbit 3D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=3000, soft_min=--, soft_max=500, default=150, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 389"
  },
  {
   "id": "AC-390",
   "criterion": "The parameter `Orbit 3D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30, hard_max=30, soft_min=-15, soft_max=15, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 390"
  },
  {
   "id": "AC-391",
   "criterion": "The parameter `Orbit 3D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-3000, hard_max=3000, soft_min=-500, soft_max=500, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 391"
  },
  {
   "id": "AC-392",
   "criterion": "The parameter `Oscillate` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=150, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 392"
  },
  {
   "id": "AC-393",
   "criterion": "The parameter `Oscillate` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 393"
  },
  {
   "id": "AC-394",
   "criterion": "The parameter `Oscillate` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 394"
  },
  {
   "id": "AC-395",
   "criterion": "The parameter `Oscillate` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 395"
  },
  {
   "id": "AC-396",
   "criterion": "The parameter `Oscillate` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 396"
  },
  {
   "id": "AC-397",
   "criterion": "The parameter `Oscillate At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=150, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 397"
  },
  {
   "id": "AC-398",
   "criterion": "The parameter `Oscillate At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 398"
  },
  {
   "id": "AC-399",
   "criterion": "The parameter `Oscillate At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 399"
  },
  {
   "id": "AC-400",
   "criterion": "The parameter `Oscillate On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=150, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 400"
  },
  {
   "id": "AC-401",
   "criterion": "The parameter `Oscillate On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 401"
  },
  {
   "id": "AC-402",
   "criterion": "The parameter `Oscillate On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 402"
  },
  {
   "id": "AC-403",
   "criterion": "The parameter `Oscillate On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=15, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 403"
  },
  {
   "id": "AC-404",
   "criterion": "The parameter `Oscillate Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=150, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 404"
  },
  {
   "id": "AC-405",
   "criterion": "The parameter `Oscillate Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 405"
  },
  {
   "id": "AC-406",
   "criterion": "The parameter `Oscillate Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 406"
  },
  {
   "id": "AC-407",
   "criterion": "The parameter `Oscillate Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=0.75, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 407"
  },
  {
   "id": "AC-408",
   "criterion": "The parameter `Oscillate Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 408"
  },
  {
   "id": "AC-409",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 409"
  },
  {
   "id": "AC-410",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=sample_rate / 2, soft_min=--, soft_max=--, default=--, unit=filter, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 410"
  },
  {
   "id": "AC-411",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-48, hard_max=48, soft_min=--, soft_max=--, default=-48, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 411"
  },
  {
   "id": "AC-412",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=sample_rate / 2, soft_min=--, soft_max=--, default=--, unit=filter, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 412"
  },
  {
   "id": "AC-413",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-48, hard_max=48, soft_min=--, soft_max=--, default=-48, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 413"
  },
  {
   "id": "AC-414",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=sample_rate / 2, soft_min=--, soft_max=--, default=--, unit=filter, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 414"
  },
  {
   "id": "AC-415",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-48, hard_max=48, soft_min=--, soft_max=--, default=-48, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 415"
  },
  {
   "id": "AC-416",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 416"
  },
  {
   "id": "AC-417",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=sample_rate / 2, soft_min=--, soft_max=--, default=--, unit=filter, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 417"
  },
  {
   "id": "AC-418",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-48, hard_max=48, soft_min=--, soft_max=--, default=-48, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 418"
  },
  {
   "id": "AC-419",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 419"
  },
  {
   "id": "AC-420",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=sample_rate / 2, soft_min=--, soft_max=--, default=--, unit=filter, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 420"
  },
  {
   "id": "AC-421",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-48, hard_max=48, soft_min=--, soft_max=--, default=-48, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 421"
  },
  {
   "id": "AC-422",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 422"
  },
  {
   "id": "AC-423",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=sample_rate / 2, soft_min=--, soft_max=--, default=--, unit=filter, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 423"
  },
  {
   "id": "AC-424",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-48, hard_max=48, soft_min=--, soft_max=--, default=-48, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 424"
  },
  {
   "id": "AC-425",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 425"
  },
  {
   "id": "AC-426",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=sample_rate / 2, soft_min=--, soft_max=--, default=--, unit=filter, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 426"
  },
  {
   "id": "AC-427",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-48, hard_max=48, soft_min=--, soft_max=--, default=-48, unit=amp, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 427"
  },
  {
   "id": "AC-428",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 428"
  },
  {
   "id": "AC-429",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-96, hard_max=48, soft_min=--, soft_max=--, default=-96, unit=gain, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 429"
  },
  {
   "id": "AC-430",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 430"
  },
  {
   "id": "AC-431",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 431"
  },
  {
   "id": "AC-432",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 432"
  },
  {
   "id": "AC-433",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 433"
  },
  {
   "id": "AC-434",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 434"
  },
  {
   "id": "AC-435",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 435"
  },
  {
   "id": "AC-436",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.0001, hard_max=10000, soft_min=--, soft_max=--, default=0.0001, unit=q, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 436"
  },
  {
   "id": "AC-437",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.0001, hard_max=10000, soft_min=--, soft_max=--, default=0.0001, unit=q, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 437"
  },
  {
   "id": "AC-438",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.0001, hard_max=10000, soft_min=--, soft_max=--, default=0.0001, unit=q, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 438"
  },
  {
   "id": "AC-439",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.0001, hard_max=10000, soft_min=--, soft_max=--, default=0.0001, unit=q, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 439"
  },
  {
   "id": "AC-440",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.0001, hard_max=10000, soft_min=--, soft_max=--, default=0.0001, unit=q, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 440"
  },
  {
   "id": "AC-441",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.0001, hard_max=10000, soft_min=--, soft_max=--, default=0.0001, unit=q, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 441"
  },
  {
   "id": "AC-442",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.0001, hard_max=10000, soft_min=--, soft_max=--, default=0.0001, unit=q, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 442"
  },
  {
   "id": "AC-443",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.0001, hard_max=10000, soft_min=--, soft_max=--, default=0.0001, unit=q, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 443"
  },
  {
   "id": "AC-444",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.0001, hard_max=10000, soft_min=--, soft_max=--, default=0.0001, unit=q, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 444"
  },
  {
   "id": "AC-445",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.0001, hard_max=10000, soft_min=--, soft_max=--, default=0.0001, unit=q, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 445"
  },
  {
   "id": "AC-446",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 446"
  },
  {
   "id": "AC-447",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=sample_rate / 2, soft_min=--, soft_max=--, default=--, unit=filter, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 447"
  },
  {
   "id": "AC-448",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=false, hard_max=true, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 448"
  },
  {
   "id": "AC-449",
   "criterion": "The parameter `Parametric Equalizer` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=sample_rate / 2, soft_min=--, soft_max=--, default=--, unit=filter, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 449"
  },
  {
   "id": "AC-450",
   "criterion": "The parameter `Pattern Template` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 450"
  },
  {
   "id": "AC-451",
   "criterion": "The parameter `Pattern Template` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 451"
  },
  {
   "id": "AC-452",
   "criterion": "The parameter `Pattern Template` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 452"
  },
  {
   "id": "AC-453",
   "criterion": "The parameter `Pattern Template` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 453"
  },
  {
   "id": "AC-454",
   "criterion": "The parameter `Pendulum` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-180, hard_max=180, soft_min=--, soft_max=--, default=45, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 454"
  },
  {
   "id": "AC-455",
   "criterion": "The parameter `Pendulum` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 455"
  },
  {
   "id": "AC-456",
   "criterion": "The parameter `Pendulum` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 456"
  },
  {
   "id": "AC-457",
   "criterion": "The parameter `Pendulum` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 457"
  },
  {
   "id": "AC-458",
   "criterion": "The parameter `Pendulum` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 458"
  },
  {
   "id": "AC-459",
   "criterion": "The parameter `Pendulum At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-180, hard_max=180, soft_min=--, soft_max=--, default=45, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 459"
  },
  {
   "id": "AC-460",
   "criterion": "The parameter `Pendulum At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 460"
  },
  {
   "id": "AC-461",
   "criterion": "The parameter `Pendulum At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 461"
  },
  {
   "id": "AC-462",
   "criterion": "The parameter `Pendulum On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-180, hard_max=180, soft_min=--, soft_max=--, default=45, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 462"
  },
  {
   "id": "AC-463",
   "criterion": "The parameter `Pendulum On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 463"
  },
  {
   "id": "AC-464",
   "criterion": "The parameter `Pendulum On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 464"
  },
  {
   "id": "AC-465",
   "criterion": "The parameter `Pendulum On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=15, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 465"
  },
  {
   "id": "AC-466",
   "criterion": "The parameter `Pendulum Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-180, hard_max=180, soft_min=--, soft_max=--, default=45, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 466"
  },
  {
   "id": "AC-467",
   "criterion": "The parameter `Pendulum Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 467"
  },
  {
   "id": "AC-468",
   "criterion": "The parameter `Pendulum Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 468"
  },
  {
   "id": "AC-469",
   "criterion": "The parameter `Pendulum Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=0.75, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 469"
  },
  {
   "id": "AC-470",
   "criterion": "The parameter `Pendulum Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 470"
  },
  {
   "id": "AC-471",
   "criterion": "The parameter `Percentage Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 471"
  },
  {
   "id": "AC-472",
   "criterion": "The parameter `Percentage Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 472"
  },
  {
   "id": "AC-473",
   "criterion": "The parameter `Percentage Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 473"
  },
  {
   "id": "AC-474",
   "criterion": "The parameter `Percentage Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 474"
  },
  {
   "id": "AC-475",
   "criterion": "The parameter `Percentage Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 475"
  },
  {
   "id": "AC-476",
   "criterion": "The parameter `Percentage Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 476"
  },
  {
   "id": "AC-477",
   "criterion": "The parameter `Percentage Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 477"
  },
  {
   "id": "AC-478",
   "criterion": "The parameter `Pulse` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-100, hard_max=300, soft_min=--, soft_max=100, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 478"
  },
  {
   "id": "AC-479",
   "criterion": "The parameter `Pulse` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 479"
  },
  {
   "id": "AC-480",
   "criterion": "The parameter `Pulse` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0.01, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 480"
  },
  {
   "id": "AC-481",
   "criterion": "The parameter `Pulse` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 481"
  },
  {
   "id": "AC-482",
   "criterion": "The parameter `Pulse At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-100, hard_max=300, soft_min=--, soft_max=100, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 482"
  },
  {
   "id": "AC-483",
   "criterion": "The parameter `Pulse At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 483"
  },
  {
   "id": "AC-484",
   "criterion": "The parameter `Pulse On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-100, hard_max=300, soft_min=--, soft_max=100, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 484"
  },
  {
   "id": "AC-485",
   "criterion": "The parameter `Pulse On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 485"
  },
  {
   "id": "AC-486",
   "criterion": "The parameter `Pulse On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=15, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 486"
  },
  {
   "id": "AC-487",
   "criterion": "The parameter `Pulse Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-100, hard_max=300, soft_min=--, soft_max=100, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 487"
  },
  {
   "id": "AC-488",
   "criterion": "The parameter `Pulse Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=4, hard_max=25, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 488"
  },
  {
   "id": "AC-489",
   "criterion": "The parameter `Pulse Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 489"
  },
  {
   "id": "AC-490",
   "criterion": "The parameter `Pulse Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 490"
  },
  {
   "id": "AC-491",
   "criterion": "The parameter `Radial Wipe Master Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 491"
  },
  {
   "id": "AC-492",
   "criterion": "The parameter `Radial Wipe Master Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 492"
  },
  {
   "id": "AC-493",
   "criterion": "The parameter `Random Fill Color` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 493"
  },
  {
   "id": "AC-494",
   "criterion": "The parameter `Random Fill Color` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=5, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 494"
  },
  {
   "id": "AC-495",
   "criterion": "The parameter `Random Fill Color` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=180, soft_min=--, soft_max=--, default=60, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 495"
  },
  {
   "id": "AC-496",
   "criterion": "The parameter `Random Fill Color` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 496"
  },
  {
   "id": "AC-497",
   "criterion": "The parameter `Random Fill Color` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 497"
  },
  {
   "id": "AC-498",
   "criterion": "The parameter `Random Motion` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 498"
  },
  {
   "id": "AC-499",
   "criterion": "The parameter `Random Motion` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=5, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 499"
  },
  {
   "id": "AC-500",
   "criterion": "The parameter `Random Motion` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=3000, soft_min=--, soft_max=1000, default=300, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 500"
  },
  {
   "id": "AC-501",
   "criterion": "The parameter `Random Motion` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=3000, soft_min=--, soft_max=1000, default=220, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 501"
  },
  {
   "id": "AC-502",
   "criterion": "The parameter `Random Motion 1D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 502"
  },
  {
   "id": "AC-503",
   "criterion": "The parameter `Random Motion 1D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=5, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 503"
  },
  {
   "id": "AC-504",
   "criterion": "The parameter `Random Motion 1D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=3000, soft_min=--, soft_max=1000, default=300, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 504"
  },
  {
   "id": "AC-505",
   "criterion": "The parameter `Random Opacity` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 505"
  },
  {
   "id": "AC-506",
   "criterion": "The parameter `Random Opacity` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=5, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 506"
  },
  {
   "id": "AC-507",
   "criterion": "The parameter `Random Opacity` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=10, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 507"
  },
  {
   "id": "AC-508",
   "criterion": "The parameter `Random Opacity` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=100, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 508"
  },
  {
   "id": "AC-509",
   "criterion": "The parameter `Random Rotation` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 509"
  },
  {
   "id": "AC-510",
   "criterion": "The parameter `Random Rotation` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=5, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 510"
  },
  {
   "id": "AC-511",
   "criterion": "The parameter `Random Rotation` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-3600, hard_max=3600, soft_min=-1000, soft_max=1000, default=-360, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 511"
  },
  {
   "id": "AC-512",
   "criterion": "The parameter `Random Rotation` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-3600, hard_max=3600, soft_min=-1000, soft_max=1000, default=360, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 512"
  },
  {
   "id": "AC-513",
   "criterion": "The parameter `Random Rotation 3D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 513"
  },
  {
   "id": "AC-514",
   "criterion": "The parameter `Random Rotation 3D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=5, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 514"
  },
  {
   "id": "AC-515",
   "criterion": "The parameter `Random Rotation 3D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-3600, hard_max=3600, soft_min=-1000, soft_max=1000, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 515"
  },
  {
   "id": "AC-516",
   "criterion": "The parameter `Random Rotation 3D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-3600, hard_max=3600, soft_min=-1000, soft_max=1000, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 516"
  },
  {
   "id": "AC-517",
   "criterion": "The parameter `Random Rotation 3D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-3600, hard_max=3600, soft_min=-1000, soft_max=1000, default=-360, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 517"
  },
  {
   "id": "AC-518",
   "criterion": "The parameter `Random Rotation 3D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-3600, hard_max=3600, soft_min=-1000, soft_max=1000, default=360, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 518"
  },
  {
   "id": "AC-519",
   "criterion": "The parameter `Random Rotation 3D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-3600, hard_max=3600, soft_min=-1000, soft_max=1000, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 519"
  },
  {
   "id": "AC-520",
   "criterion": "The parameter `Random Rotation 3D` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-3600, hard_max=3600, soft_min=-1000, soft_max=1000, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 520"
  },
  {
   "id": "AC-521",
   "criterion": "The parameter `Random Scale` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 521"
  },
  {
   "id": "AC-522",
   "criterion": "The parameter `Random Scale` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=5, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 522"
  },
  {
   "id": "AC-523",
   "criterion": "The parameter `Random Scale` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=--, soft_max=500, default=25, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 523"
  },
  {
   "id": "AC-524",
   "criterion": "The parameter `Random Scale` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=--, soft_max=500, default=100, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 524"
  },
  {
   "id": "AC-525",
   "criterion": "The parameter `Sample Image` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.01, hard_max=10000, soft_min=0.01, soft_max=2048, default=0.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 525"
  },
  {
   "id": "AC-526",
   "criterion": "The parameter `Scale Bounce - layer markers` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=10000, soft_min=1, soft_max=2000, default=500, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 526"
  },
  {
   "id": "AC-527",
   "criterion": "The parameter `Scale Bounce - layer markers` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=10000, soft_min=0, soft_max=1000, default=200, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 527"
  },
  {
   "id": "AC-528",
   "criterion": "The parameter `Scale Bounce - random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 528"
  },
  {
   "id": "AC-529",
   "criterion": "The parameter `Scale Bounce - random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=10000, soft_min=1, soft_max=500, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 529"
  },
  {
   "id": "AC-530",
   "criterion": "The parameter `Scale Bounce - random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=10000, soft_min=0, soft_max=1000, default=200, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 530"
  },
  {
   "id": "AC-531",
   "criterion": "The parameter `Separate XYZ Position` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-1000, soft_max=1000, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 531"
  },
  {
   "id": "AC-532",
   "criterion": "The parameter `Separate XYZ Position` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-1000, soft_max=1000, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 532"
  },
  {
   "id": "AC-533",
   "criterion": "The parameter `Separate XYZ Position` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-30000, hard_max=30000, soft_min=-1000, soft_max=1000, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 533"
  },
  {
   "id": "AC-534",
   "criterion": "The parameter `Separate XYZ Scale` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=-100, soft_max=100, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 534"
  },
  {
   "id": "AC-535",
   "criterion": "The parameter `Separate XYZ Scale` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=-100, soft_max=100, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 535"
  },
  {
   "id": "AC-536",
   "criterion": "The parameter `Separate XYZ Scale` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=-100, soft_max=100, default=100, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 536"
  },
  {
   "id": "AC-537",
   "criterion": "The parameter `Slide - variable` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 537"
  },
  {
   "id": "AC-538",
   "criterion": "The parameter `Slide - variable` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=500, soft_min=0, soft_max=200, default=25, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 538"
  },
  {
   "id": "AC-539",
   "criterion": "The parameter `Slide Master Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 539"
  },
  {
   "id": "AC-540",
   "criterion": "The parameter `Solarize` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=254, soft_min=--, soft_max=--, default=--, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "default",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 540"
  },
  {
   "id": "AC-541",
   "criterion": "The parameter `Stereo 3D Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=0, soft_max=100, default=3, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 541"
  },
  {
   "id": "AC-542",
   "criterion": "The parameter `Stereo 3D Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-50000, hard_max=50000, soft_min=-1000, soft_max=1000, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 542"
  },
  {
   "id": "AC-543",
   "criterion": "The parameter `Stretch Master Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 543"
  },
  {
   "id": "AC-544",
   "criterion": "The parameter `Stretch Master Control (edge)` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 544"
  },
  {
   "id": "AC-545",
   "criterion": "The parameter `Stretch Master Control(corner)` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 545"
  },
  {
   "id": "AC-546",
   "criterion": "The parameter `Studio Reverb` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=4000, soft_min=--, soft_max=--, default=879.9984, unit=lowcut, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 546"
  },
  {
   "id": "AC-547",
   "criterion": "The parameter `Studio Reverb` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=20, hard_max=20000, soft_min=--, soft_max=--, default=13800.0062, unit=highcut, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 547"
  },
  {
   "id": "AC-548",
   "criterion": "The parameter `Studio Reverb` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=100, soft_min=--, soft_max=--, default=70.00003, unit=roomsize, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 548"
  },
  {
   "id": "AC-549",
   "criterion": "The parameter `Studio Reverb` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=25, unit=width, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 549"
  },
  {
   "id": "AC-550",
   "criterion": "The parameter `Studio Reverb` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=diffusion, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 550"
  },
  {
   "id": "AC-551",
   "criterion": "The parameter `Studio Reverb` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=damping, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 551"
  },
  {
   "id": "AC-552",
   "criterion": "The parameter `Studio Reverb` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=200, hard_max=10000, soft_min=--, soft_max=--, default=2500.0012, unit=decay, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 552"
  },
  {
   "id": "AC-553",
   "criterion": "The parameter `Studio Reverb` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=52, unit=earlyreflections, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 553"
  },
  {
   "id": "AC-554",
   "criterion": "The parameter `Studio Reverb` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=75, unit=dry, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 554"
  },
  {
   "id": "AC-555",
   "criterion": "The parameter `Studio Reverb` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=25, unit=wet, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 555"
  },
  {
   "id": "AC-556",
   "criterion": "The parameter `Swarm` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=--, soft_max=500, default=100, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 556"
  },
  {
   "id": "AC-557",
   "criterion": "The parameter `Swarm` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 557"
  },
  {
   "id": "AC-558",
   "criterion": "The parameter `Timer Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=9.9999, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 558"
  },
  {
   "id": "AC-559",
   "criterion": "The parameter `Timer Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=32, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 559"
  },
  {
   "id": "AC-560",
   "criterion": "The parameter `Timer Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 560"
  },
  {
   "id": "AC-561",
   "criterion": "The parameter `Timer Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000000, hard_max=1000000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 561"
  },
  {
   "id": "AC-562",
   "criterion": "The parameter `Trace Path` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=0, soft_max=100, default=0, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 562"
  },
  {
   "id": "AC-563",
   "criterion": "The parameter `Transition Master Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 563"
  },
  {
   "id": "AC-564",
   "criterion": "The parameter `Wiggle - gelatin` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 564"
  },
  {
   "id": "AC-565",
   "criterion": "The parameter `Wiggle - gelatin` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=70, soft_min=--, soft_max=--, default=20, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 565"
  },
  {
   "id": "AC-566",
   "criterion": "The parameter `Wiggle - position` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 566"
  },
  {
   "id": "AC-567",
   "criterion": "The parameter `Wiggle - position` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=10000, soft_min=0, soft_max=500, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 567"
  },
  {
   "id": "AC-568",
   "criterion": "The parameter `Wiggle - rotation` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 568"
  },
  {
   "id": "AC-569",
   "criterion": "The parameter `Wiggle - scale` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 569"
  },
  {
   "id": "AC-570",
   "criterion": "The parameter `Wiggle - scale` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=10000, soft_min=0, soft_max=1000, default=10, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 570"
  },
  {
   "id": "AC-571",
   "criterion": "The parameter `Wiggle - scale` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=10000, soft_min=0, soft_max=1000, default=10, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 571"
  },
  {
   "id": "AC-572",
   "criterion": "The parameter `Wiggle - shear` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 572"
  },
  {
   "id": "AC-573",
   "criterion": "The parameter `Wiggle - shear` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=70, soft_min=--, soft_max=--, default=20, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 573"
  },
  {
   "id": "AC-574",
   "criterion": "The parameter `Wigglerama` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 574"
  },
  {
   "id": "AC-575",
   "criterion": "The parameter `Wigglerama` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=1, hard_max=20, soft_min=--, soft_max=--, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 575"
  },
  {
   "id": "AC-576",
   "criterion": "The parameter `Wigglerama` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=10000, soft_min=0, soft_max=500, default=25, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 576"
  },
  {
   "id": "AC-577",
   "criterion": "The parameter `Wigglerama` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=10000, soft_min=0, soft_max=1000, default=15, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 577"
  },
  {
   "id": "AC-578",
   "criterion": "The parameter `Wigglerama` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=10000, soft_min=0, soft_max=1000, default=10, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 578"
  },
  {
   "id": "AC-579",
   "criterion": "The parameter `Wipe Master Control` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 579"
  },
  {
   "id": "AC-580",
   "criterion": "The parameter `Wipe Master Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 580"
  },
  {
   "id": "AC-581",
   "criterion": "The parameter `Wipe Master Controls` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=1000, soft_min=0, soft_max=100, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 581"
  },
  {
   "id": "AC-582",
   "criterion": "The parameter `Wobble Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=250, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 582"
  },
  {
   "id": "AC-583",
   "criterion": "The parameter `Wobble Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 583"
  },
  {
   "id": "AC-584",
   "criterion": "The parameter `Wobble Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=0.7, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 584"
  },
  {
   "id": "AC-585",
   "criterion": "The parameter `Wobble Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=100, default=35, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 585"
  },
  {
   "id": "AC-586",
   "criterion": "The parameter `Wobble Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 586"
  },
  {
   "id": "AC-587",
   "criterion": "The parameter `Wobble Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=0.7, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 587"
  },
  {
   "id": "AC-588",
   "criterion": "The parameter `Wobble Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 588"
  },
  {
   "id": "AC-589",
   "criterion": "The parameter `Wobble Bounce` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 589"
  },
  {
   "id": "AC-590",
   "criterion": "The parameter `Wobble Bounce At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=250, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 590"
  },
  {
   "id": "AC-591",
   "criterion": "The parameter `Wobble Bounce At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 591"
  },
  {
   "id": "AC-592",
   "criterion": "The parameter `Wobble Bounce At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=0.7, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 592"
  },
  {
   "id": "AC-593",
   "criterion": "The parameter `Wobble Bounce At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=100, default=35, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 593"
  },
  {
   "id": "AC-594",
   "criterion": "The parameter `Wobble Bounce At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 594"
  },
  {
   "id": "AC-595",
   "criterion": "The parameter `Wobble Bounce At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=0.7, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 595"
  },
  {
   "id": "AC-596",
   "criterion": "The parameter `Wobble Bounce On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=250, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 596"
  },
  {
   "id": "AC-597",
   "criterion": "The parameter `Wobble Bounce On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 597"
  },
  {
   "id": "AC-598",
   "criterion": "The parameter `Wobble Bounce On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=0.7, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 598"
  },
  {
   "id": "AC-599",
   "criterion": "The parameter `Wobble Bounce On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=100, default=35, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 599"
  },
  {
   "id": "AC-600",
   "criterion": "The parameter `Wobble Bounce On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 600"
  },
  {
   "id": "AC-601",
   "criterion": "The parameter `Wobble Bounce On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=0.7, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 601"
  },
  {
   "id": "AC-602",
   "criterion": "The parameter `Wobble Bounce On Beat` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=15, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 602"
  },
  {
   "id": "AC-603",
   "criterion": "The parameter `Wobble Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=0, soft_max=--, default=250, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 603"
  },
  {
   "id": "AC-604",
   "criterion": "The parameter `Wobble Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=1, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 604"
  },
  {
   "id": "AC-605",
   "criterion": "The parameter `Wobble Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=0.7, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 605"
  },
  {
   "id": "AC-606",
   "criterion": "The parameter `Wobble Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=100, default=35, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 606"
  },
  {
   "id": "AC-607",
   "criterion": "The parameter `Wobble Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=4, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 607"
  },
  {
   "id": "AC-608",
   "criterion": "The parameter `Wobble Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=0.7, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 608"
  },
  {
   "id": "AC-609",
   "criterion": "The parameter `Wobble Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=0.75, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 609"
  },
  {
   "id": "AC-610",
   "criterion": "The parameter `Wobble Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0.1, hard_max=100, soft_min=--, soft_max=10, default=1.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 610"
  },
  {
   "id": "AC-611",
   "criterion": "The parameter `Wobble Bounce Random` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 611"
  },
  {
   "id": "AC-612",
   "criterion": "The parameter `Z Spring` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=--, soft_max=--, default=-500, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 612"
  },
  {
   "id": "AC-613",
   "criterion": "The parameter `Z Spring` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 613"
  },
  {
   "id": "AC-614",
   "criterion": "The parameter `Z Spring` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 614"
  },
  {
   "id": "AC-615",
   "criterion": "The parameter `Z Spring` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=500, soft_min=--, soft_max=--, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 615"
  },
  {
   "id": "AC-616",
   "criterion": "The parameter `Z Spring` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-360, hard_max=360, soft_min=--, soft_max=--, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 616"
  },
  {
   "id": "AC-617",
   "criterion": "The parameter `Z Spring` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=1.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 617"
  },
  {
   "id": "AC-618",
   "criterion": "The parameter `Z Spring` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 618"
  },
  {
   "id": "AC-619",
   "criterion": "The parameter `Z Spring` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=0, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 619"
  },
  {
   "id": "AC-620",
   "criterion": "The parameter `Z Spring At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-1000, hard_max=1000, soft_min=--, soft_max=--, default=-500, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 620"
  },
  {
   "id": "AC-621",
   "criterion": "The parameter `Z Spring At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 621"
  },
  {
   "id": "AC-622",
   "criterion": "The parameter `Z Spring At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=25, soft_min=--, soft_max=15, default=3, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 622"
  },
  {
   "id": "AC-623",
   "criterion": "The parameter `Z Spring At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=500, soft_min=--, soft_max=--, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 623"
  },
  {
   "id": "AC-624",
   "criterion": "The parameter `Z Spring At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=-360, hard_max=360, soft_min=--, soft_max=--, default=50, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 624"
  },
  {
   "id": "AC-625",
   "criterion": "The parameter `Z Spring At Marker` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=30, soft_min=--, soft_max=15, default=1.5, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 625"
  },
  {
   "id": "AC-626",
   "criterion": "The parameter `Zoom - 2D spin` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 626"
  },
  {
   "id": "AC-627",
   "criterion": "The parameter `Zoom - 3D tumble` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 627"
  },
  {
   "id": "AC-628",
   "criterion": "The parameter `Zoom - bubble` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 628"
  },
  {
   "id": "AC-629",
   "criterion": "The parameter `Zoom - spiral` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 629"
  },
  {
   "id": "AC-630",
   "criterion": "The parameter `Zoom - spiral` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=2000, soft_min=--, soft_max=--, default=200, unit=--, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "unit",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 630"
  },
  {
   "id": "AC-631",
   "criterion": "The parameter `Zoom - wobble` accepts and stores every one of its seven declared fields as SEPARATE values, exactly as the spec table states them: hard_min=0, hard_max=100, soft_min=--, soft_max=--, default=50, unit=percent, precision=--. A field the spec marks unknown is stored as unknown and is NOT set equal to its opposite bound, because collapsing hard and soft bounds destroys information that cannot be recovered without re-deriving it from the captures.",
   "evidence_kind": "runtime_assertion",
   "unknown_fields": [
    "soft_min",
    "soft_max",
    "precision"
   ],
   "source": "14-studio-creative-suite.md STU-FX-133a parameter table row 631"
  }
 ],
 "acceptance_criteria_count": 631,
 "implementation_notes": {
  "extracted_behaviour": [],
  "behaviour_record_count": 0,
  "naming": "Vendor product names are provenance only. Studio ships Handshake-native names per [STU-SECTION-003].",
  "database": "SurrealDB with the EventLedger is the only durable authority. SQLite, libSQL, Turso and PostgreSQL are forbidden, including in tests and dev caches.",
  "engine_split": "GPU, WGSL and compute live in the studio-engine crate behind its traits. handshake_core must never gain a GPU dependency.",
  "routing": "Navigation extends the existing address and bus seams. No new router."
 },
 "gui_obligation": {
  "operator_surface_required": "YES",
  "argus_required": "YES"
 },
 "user_manual_obligation": {
  "required": true,
  "same_change_update_required": "YES"
 },
 "resource_privacy_obligation": {
  "applies": true
 },
 "hbr_obligations": [
  "HBR-INT-001",
  "HBR-INT-003",
  "HBR-INT-009",
  "HBR-VIS-001",
  "HBR-VIS-003",
  "HBR-MAN-001",
  "HBR-MAN-004",
  "HBR-PRIV-001"
 ],
 "validator_focus": [
  "Confirm every acceptance row traces to a sentence or table row in the cited spec clause. A row with no spec basis is a fabrication and fails the contract.",
  "Reject scaffold-only proof: at least one proof command must drive the executable runtime rather than a fixture the implementer authored.",
  "For a parameter row, confirm hard and soft bounds are stored as separate values and an unknown bound is stored as unknown rather than mirrored from its opposite."
 ],
 "handoff": {
  "coder_session": null,
  "wp_validator_session": null
 }
}