# MT-006: MCP/provider adapter guidance plus read-only mode

## METADATA
- WP_ID: WP-1-Calendar-Sync-Engine-v2
- MT_ID: MT-006
- CLAUSE: MCP/provider adapter guidance plus read-only mode
- CODE_SURFACES: ../handshake_main/src/backend/handshake_core/src/workflows.rs; ../handshake_main/src/backend/handshake_core/mechanical_engines.json; ../handshake_main/src/backend/handshake_core/tests/mex_tests.rs
- EXPECTED_TESTS: cargo test --manifest-path ../handshake_main/src/backend/handshake_core/Cargo.toml mex_tests; cargo test --manifest-path ../handshake_main/src/backend/handshake_core/Cargo.toml
- DEPENDS_ON: MT-005
- RISK_IF_MISSED: provider access and write posture drift from the calendar law contract

## CODER
- STATUS: IMPLEMENTED_WITH_BLOCKED_PROOF
- EVIDENCE:
  - Repair commit `a0a355c3` fixes the MT-006 validator seam in `src/backend/handshake_core/src/workflows.rs` so `run_calendar_sync_job` no longer unconditionally loads `calendar_sync_result` before handling `EngineStatus::Denied`.
  - `src/backend/handshake_core/src/workflows.rs`: added deny-safe workflow payload construction that emits `provider_access`, source/write-policy parity, and engine error context even when no `calendar_sync_result` artifact is present.
  - `src/backend/handshake_core/src/workflows.rs`: added bounded workflow coverage for a denied engine result with no result artifact, asserting provider-access guidance and fail-closed mutation posture still surface in workflow output.
  - Commit `579ef5b4` lands the MT-006 product diff on the declared surfaces only.
  - `src/backend/handshake_core/mechanical_engines.json`: added governed `calendar_sync` guidance for provider access routing plus `read_only_import` fail-closed posture.
  - `src/backend/handshake_core/src/workflows.rs`: added read-only mutation blocking in `CalendarSyncEngineAdapter`, surfaced provider/write-policy guidance in workflow output, and added workflow coverage for read-only mutation denial plus output parity.
  - `src/backend/handshake_core/tests/mex_tests.rs`: added registry guidance coverage for governed provider path and read-only fail-closed metadata.
- TESTS_RUN:
  - `cargo test --manifest-path src/backend/handshake_core/Cargo.toml mex_tests` -> exit `101`; blocked by the pre-existing parser failure in `src/backend/handshake_core/src/flight_recorder/mod.rs:6180`.
  - `cargo test --manifest-path src/backend/handshake_core/Cargo.toml mex_tests` after repair commit `a0a355c3` -> exit `101`; blocked by the same untouched parser failure in `src/backend/handshake_core/src/flight_recorder/mod.rs:6180` before crate tests can exercise MT-006.
  - `rustfmt --edition 2021 --check src/backend/handshake_core/src/workflows.rs` -> reached formatting-only diffs in `workflows.rs`; no repair-local syntax failure surfaced in the edited MT-006 file.
- NOTES:
  - Proof remains blocked outside MT-006 by `src/backend/handshake_core/src/flight_recorder/mod.rs:6180`; per steer, the whole-crate retry was not repeated after the same parser error reproduced.
  - Repair scope stayed inside `src/backend/handshake_core/src/workflows.rs`; `mechanical_engines.json` was left unchanged because the defect was a workflow deny-path seam, not a registry-contract problem.
  - Risk focus for validator overlap review: provider-access guidance, read-only fail-closed behavior, and workflow output / `provider_access` parity on true `EngineStatus::Denied` results without a `calendar_sync_result` artifact.

## VALIDATOR
- STATUS: PASS
- FINDINGS:
  - Governed `REVIEW_RESPONSE` recorded at `2026-04-21T12:36:18.002Z` on correlation `review:WP-1-Calendar-Sync-Engine-v2:review_request:mt-006:a0a355c3`.
  - Repair commit `a0a355c3` makes the workflow-side deny path artifact-safe: `run_calendar_sync_job` now emits `provider_access`, source/write-policy guidance, and engine error context for `EngineStatus::Denied` even when no `calendar_sync_result` artifact is present.
  - Bounded overlap review confirmed MT-006 remains on the declared surfaces only: `src/backend/handshake_core/src/workflows.rs`, `src/backend/handshake_core/mechanical_engines.json`, and `src/backend/handshake_core/tests/mex_tests.rs`.
- DIRECTION:
  - Carry the final MT-006 PASS state into the terminal whole-WP `CODER_HANDOFF` and committed validator handoff evidence for head `a0a355c359656eedea3098692fc89f3546a59719`.
