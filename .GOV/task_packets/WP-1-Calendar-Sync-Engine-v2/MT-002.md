# MT-002: workflow capability profile and required-capabilities contract

## METADATA
- WP_ID: WP-1-Calendar-Sync-Engine-v2
- MT_ID: MT-002
- CLAUSE: workflow capability profile and required-capabilities contract
- CODE_SURFACES: ../handshake_main/src/backend/handshake_core/src/capabilities.rs; ../handshake_main/src/backend/handshake_core/src/workflows.rs; ../handshake_main/src/backend/handshake_core/src/mex/gates.rs; ../handshake_main/src/backend/handshake_core/tests/mex_tests.rs
- EXPECTED_TESTS: cargo test --manifest-path ../handshake_main/src/backend/handshake_core/Cargo.toml mex_tests; cargo test --manifest-path ../handshake_main/src/backend/handshake_core/Cargo.toml
- DEPENDS_ON: MT-001
- RISK_IF_MISSED: `calendar_sync` remains blocked by the wrong capability contract or by `HSK-4001 UnknownCapability`

## CODER
- STATUS: IMPLEMENTED_WITH_BLOCKED_PROOF
- EVIDENCE:
  - `src/backend/handshake_core/src/capabilities.rs`: added the `calendar_sync` workflow protocol override, the `CalendarSync` capability profile, and the `calendar.sync.read` / `calendar.sync.write` required-capability contract.
  - `src/backend/handshake_core/src/workflows.rs`: added workflow-boundary tests that allow `CalendarSync` and deny `Analyst` for `workflow_run` + `calendar_sync`; also repaired a pre-existing missing `}` in the local test module so the allowed MT-002 surface parses.
  - `src/backend/handshake_core/src/mex/gates.rs` and `src/backend/handshake_core/tests/mex_tests.rs`: added gate/runtime coverage proving registered calendar-sync capabilities no longer fail as `HSK-4001`, while the wrong profile still fails closed.
- TESTS_RUN:
  - `cargo test --manifest-path src/backend/handshake_core/Cargo.toml mex_tests` -> exit `101`; blocked by a pre-existing parser failure in `src/backend/handshake_core/src/flight_recorder/mod.rs`.
  - `cargo test --manifest-path src/backend/handshake_core/Cargo.toml` -> exit `101`; same out-of-scope parser failure in `src/backend/handshake_core/src/flight_recorder/mod.rs`.
- NOTES:
  - Product diff is confined to the four MT-002 code surfaces.
  - Proof closure is currently blocked outside MT-002 by `src/backend/handshake_core/src/flight_recorder/mod.rs:6180`.

## VALIDATOR
- STATUS: PENDING
- FINDINGS:
- DIRECTION:
