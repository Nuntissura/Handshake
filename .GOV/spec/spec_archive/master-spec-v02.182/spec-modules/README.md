# Modules

STATUS: CURRENT_VERBATIM_MODULE_SET

This folder holds verbatim indexed Master Spec modules generated from the
current source baseline.

Modules are authoritative through `../indexed-spec-manifest.json`, the
machine-readable module resolver at `../INDEX.json`, their hashes, their
source line ranges, and the archived concordance rows under
`../_transfer/concordance/`.
