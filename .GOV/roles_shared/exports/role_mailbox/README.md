# Role Mailbox Export

Authoritative governance export path for leak-safe role-mailbox metadata.

Contents:

- `index.json`
- `export_manifest.json`
- `threads/*.jsonl` when thread exports exist

Validation:

- `just role-mailbox-export-check`
