# Shared Exports

This folder holds machine-owned, leak-safe governance exports that are shared across roles.

Current authoritative export surfaces:

- `role_mailbox/`

Deprecated compatibility surfaces remain outside this folder only to avoid breaking existing gates during migration.
