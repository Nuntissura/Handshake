{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://handshake.local/schemas/policy-waiver-ledger.schema.json",
  "title": "Policy Waiver Ledger",
  "type": "object",
  "required": ["raw", "entries", "activeEntries", "activeCoverageTokens"],
  "properties": {
    "raw": {
      "type": "string"
    },
    "entries": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["waiverId", "status", "coverage", "raw"],
        "properties": {
          "waiverId": { "type": "string" },
          "status": {
            "type": "string",
            "enum": ["ACTIVE", "EXPIRED", "REVOKED", "CLOSED", "UNSIGNED"],
            "description": "UNSIGNED: entry declared ACTIVE but SIGNATURE is missing, malformed, or not registered in roles_shared/records/SIGNATURE_AUDIT.md [VPX-006]; never counts toward activeEntries/activeCoverageTokens."
          },
          "coverage": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "SCOPE",
                "PROOF",
                "INTEGRATION",
                "DOMAIN",
                "PROTECTED_SURFACE",
                "TEST",
                "ENVIRONMENT",
                "HEURISTIC",
                "SPEC",
                "GOVERNANCE"
              ]
            }
          },
          "scope": { "type": "string" },
          "justification": { "type": "string" },
          "approver": { "type": "string" },
          "expires": { "type": "string" },
          "signature": {
            "type": "string",
            "description": "Operator one-time signature from the SIGNATURE= (alias USER_SIGNATURE=) pipe field; format {username}{DDMMYYYYHHMM}; empty when absent."
          },
          "signatureValid": {
            "type": "boolean",
            "description": "true only when signature matches ^[a-z][a-z0-9_.-]*\\d{12}$ AND is registered in SIGNATURE_AUDIT.md."
          },
          "raw": { "type": "string" }
        },
        "additionalProperties": false
      }
    },
    "activeEntries": {
      "type": "array"
    },
    "activeCoverageTokens": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "SCOPE",
          "PROOF",
          "INTEGRATION",
          "DOMAIN",
          "PROTECTED_SURFACE",
          "TEST",
          "ENVIRONMENT",
          "HEURISTIC",
          "SPEC",
          "GOVERNANCE"
        ]
      }
    }
  },
  "additionalProperties": false
}
