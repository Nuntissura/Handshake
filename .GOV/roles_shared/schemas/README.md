# Shared Governance Schemas

This folder contains shared machine-readable governance schemas.

Use this location for JSON Schemas that validate:

- WP communication artifacts
- role session registry artifacts
- session-control request/result ledgers
- other shared runtime governance contracts

These schemas are shared governance contracts and do not belong at `/.GOV/` root.
